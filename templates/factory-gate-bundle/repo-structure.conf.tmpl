# Render into .factory/repo-structure.conf in the consumer repository.
required={{SOURCE_DIR}}
required={{TEST_DIR}}
allowed_top=.factory
allowed_top=.git
allowed_top=.githooks
allowed_top=.agents
allowed_top={{SOURCE_DIR}}
allowed_top={{TEST_DIR}}
allowed_top={{PROJECT_METADATA_FILE}}
