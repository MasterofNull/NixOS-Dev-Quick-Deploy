# Agent instructions — {{PROJECT_NAME}}

Render `{{SHARED_RULES}}` here verbatim. It is the behavioral and role-contract source of truth for every lane.

Repository layout: `{{REPOSITORY_LAYOUT}}`

Required checks are fail-closed until explicitly configured. Read the install
receipt and the gate output for the configuration status; this notice is not an
executable command.
