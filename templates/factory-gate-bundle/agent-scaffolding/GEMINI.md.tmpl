# Gemini lane — {{PROJECT_NAME}}

Canonical rules: `{{SHARED_RULES}}` (render verbatim; do not fork lane policy).
Role for this lane: `{{GEMINI_ROLE}}`.
Follow `{{WORKFLOW_CANON_PATH}}` and the same hooks and gate runner used by every committer.
