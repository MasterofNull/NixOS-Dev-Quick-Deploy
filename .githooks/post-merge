#!/usr/bin/env bash

set -euo pipefail

repo_root="$(git rev-parse --show-toplevel 2>/dev/null || true)"
[[ -n "$repo_root" ]] || exit 0
freshness_script="${repo_root}/scripts/ai/aq-ctx-freshness"
[[ -x "$freshness_script" ]] || exit 0

"$freshness_script" --ensure || true
exit 0
