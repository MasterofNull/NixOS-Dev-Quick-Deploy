# Fable Flagship Review — C0.5 Tiered Policy Architecture Pass

Reviewer: `claude-fable-5`
Dispatch: `claude-20260716-140014-4vkvqn`
Role: independent flagship reviewer
Baseline: architecture + security + SRE
Subject: exact five-file staged policy candidate

All seven requested invariants are enforced: flat peer reasoning, cheapest healthy eligible bounded
implementers, independent flagship re-review after reviewer edits, orchestrator-only submission,
three local modalities under shared contracts, poisoning-resistant recursive propagation, and C0.5
sequenced before C1.

Non-blocking precision findings for C0.5:

1. Change the local payload statement from a false present-state claim to a normative `must` with an
   enforcement pointer; the known direct path is not yet proven to use `build_llama_payload()`.
2. Make the C0.5 learning-candidate taxonomy the SSOT because prompt/profile/tool/router/index/skill/
   embedded-corpus enumerations currently vary between documents.
3. Add the archived Antigravity C0 acceptance receipt path to the C0 completion record.
4. Define or remove the currently undefined `capability-certified` flagship qualifier.

VERDICT: PASS — all seven invariants correctly enforced across the five staged surfaces; only minor terminology/evidence-pointer gaps that C0.5 contract definition will resolve.
