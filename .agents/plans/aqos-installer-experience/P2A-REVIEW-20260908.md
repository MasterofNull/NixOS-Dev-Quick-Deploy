# P2a review — untrusted AI-proposal validator

**Reviewer:** Codex (independent, non-author binding security review) — task codex-20260908-133910.
**Subject:** feat/aqos-p2a-proposal-schema @ 0f320b88.
**Verdict transcribed by the orchestrator (Codex process ended "orphaned" before persisting its own doc; verdict captured from its output).**

1. FAIL-CLOSED: **PASS** — 10,010 adversarial byte inputs (16 MiB, deep nesting, truncation, non-UTF-8, arrays, null) all returned {ok:False, reason} without raising. (aqos_ai_proposal.py:61-113)
2. NO ESCAPE: **FAIL** — frozen catalog blocks all escapes, but poisoned-catalog entries containing `$HOME`, `>`, or a secret-shaped value returned ok:True. The unsafe denylist omits plain shell-variable expansion, redirection, and secret heuristics. (aqos_ai_proposal.py:20-26,115-127)
3. DEFENSE-IN-DEPTH: **PASS** — test 17 genuinely exercises the scan; disabling _scan_unsafe makes exactly that test fail (17/18). (test:190-204)
4. SECRET/PII + resolver-subset: **PASS** — no secrets/PII; normalized output = {golden_profile, roles, include_local_ai}, matching the resolver subset; schema enums match the catalog.

Supplied suite 18/18; py_compile + git diff --check PASS.

**OVERALL: FAIL** — incomplete shell/secret unsafe-token enforcement (defense-in-depth layer).

**Disposition:** fix dispatched — replace the incomplete denylist with a complete-by-construction ALLOWLIST (`^[a-z0-9._-]+$`, the catalog-ID charset) + regression tests for $HOME/redirection/secret-shaped/whitespace. Re-review after fix before merge to main.

## Re-review (Codex, codex-20260908-194051) — fix 29828409

- Allowlist fix verified: suite 24/24, real IDs 11/11, fail-closed 10010/10010, defense layer + resolver subset PASS.
- **OVERALL: FAIL** — a SECOND, narrower finding: a lowercase secret-shaped poisoned-catalog ID matching `[a-z0-9._-]+` (no injection chars) still returns ok:true rather than unsafe_token. (Codex could not write this file — sandbox read-only on .agents; transcribed by orchestrator.)

**Orchestrator assessment:** low severity (the allowlist already blocks ALL injection — the real threat; a lowercase inert value cannot inject, and fails catalog-membership + downstream Nix resolution; the poisoned-catalog model assumes config/ is already compromised). But the fix is cheap and MORE precise — require the real catalog-ID structure. **Fixing, not overriding the FAIL.**
**Fix dispatched:** tighten `_ALLOWED_STRING` to `^(profile|role)\.[a-z0-9._-]+$` (require the profile./role. prefix) + regression tests for lowercase secret-shaped values. Re-review before merge.

## Re-review 2 (Codex, codex-20260908-201319) — structural fix e99afab1
- Suite 27/27; poisoned UNPREFIXED values -> unsafe_token (2nd finding CLOSED).
- Selection-key bypass attempts -> unknown_field/json_invalid (values-only _scan_unsafe is safe; keys closed-set validated in Step 2).
- All 11 catalog IDs -> ok:true (no over-rejection). Fail-closed, defense-in-depth, resolver-subset intact.
- **OVERALL: PASS** (mergeable). Transcribed by orchestrator (Codex sandbox blocks .agents writes).
