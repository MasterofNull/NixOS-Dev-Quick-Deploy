# P2c approval surface — independent review

Author: Claude sub-agent (sonnet lane; Rule 18 substitution — Codex usage-limited until 4:56 PM).
Reviewer: Claude Opus 4.8 (orchestrator, NON-AUTHOR). Cross-lane confirmatory queued for Codex on return.
Verified:
- No inline resolve: aqos-approve-install only validate_proposal -> render diff -> AR.create_request (pending); never calls resolve_plan (grep-confirmed the invariant comment + call sites).
- Gating: aqos_install_runbook registers `aqos-install-resolve` into approval_request.RUNBOOK_REGISTRY; effect (normalize_adapter("ai")->resolve_plan->compile_projection) runs ONLY when approval_executor sees status=="approved" (contract untouched); stays inert at P0 (executable:false).
- ACP reuse: create_request/transition/execute_request — no parallel approval mechanism.
- Tests 5/5: no-resolve-before-approval; APPROVED == manual resolve byte-identical (jcs_bytes) -> approve==manual==one engine; denied/expired apply nothing; diff plain-language (no hex/sha/secrets).
- NO regressions from mutating the shared RUNBOOK_REGISTRY: approval-request 8/8, adapter-parity 4/4, aq-approve-headless 21/21.
Verdict: PASS. Confirmatory audit queued (advisory).
