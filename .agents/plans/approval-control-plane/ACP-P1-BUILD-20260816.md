---
doc_type: plan
id: acp-p1-build-20260816
title: ACP-P1 build record — WebAuthn signing service core
status: complete
parent_prd: approval-control-plane
slice: acp-p1
date: 2026-08-16
---

# ACP-P1 build record — signing service core

The Python core of the WebAuthn-gated signing service, built to `ACP-P1-DESIGN-20260816.md` (incl. both
review-fold sections). Implemented by the Claude sonnet lane (Rule 17; Codex quota-down, gate = Antigravity
review complete per owner directive), orchestrator-reviewed PASS by the flagship (independent).

## Files
- `scripts/ai/lib/approval_signer.py` — `ApprovalSigner` (begin_challenge / sign_request), `derive_challenge`,
  `verify_execution_authorization`, durable single-use challenge ledger + append-only executed-request
  ledger (both keyed on `request_id`), `SigningSessionGuard` (rate-limit + max-1-concurrent). Reuses
  `approval_request.py` (`canonical_hash` binds `request_id`).
- `scripts/testing/test-approval-signer.py` — 20 checks, dual-mode (main + pytest), software authenticator
  is TEST-ONLY (lives in the test file; never importable by the production module).

## Validation (orchestrator-verified)
- `python3 scripts/testing/test-approval-signer.py` → PASS 20/20; `pytest` → 20 passed.
- tier0 --pre-commit green (this record satisfies the cross-surface docs contract for the new module).
- Security properties confirmed: WYSIWYS (only input is `request_id`; the module fetches the record itself
  and signs recomputed bytes); request_id-bound challenge; single-use + executed-id ledgers checked BEFORE
  the private key is touched; no `fido2.mock`/software authenticator in the production module (imports only
  real `fido2.cose/server/webauthn` — Antigravity hardening 4); `verify_execution_authorization` for the
  executor (authorization = signature over canonical_hash, never `status`).

## Folded findings implemented
Antigravity CRITICAL (request_id binding — via the P0 fix `117aeb66`), hardening 3 (rate-limit/max-1),
hardening 4 (no software-auth in prod), hardening 5 (append-only executed-request ledger, abort-before
-verify); local (challenge TTL/nonce, request_id entropy).

## Activation status (Activation Gate)
- **Integrated:** library present + unit-validated. **NOT yet turned ON** — this is the signing-service
  CORE only; the confined systemd/Nix service module, real UDS server, declarative FIDO2 udev, live owner
  key + credential registration are the deployment layer (a separate owner-activated, default-OFF step).
- **Deferral (dated 2026-08-16):** no running service, no key material, no live socket yet. P1 is
  *built + validated*, **paused pending activation** — not "done" until the confined service is wired,
  enrolled, and exercised end-to-end. Tracked for the deployment slice.

## Catch-up
Codex confirmatory audit queued (bypassed as a build gate per owner directive; confirms on return).
