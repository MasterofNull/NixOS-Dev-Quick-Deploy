---
doc_type: plan
id: acp-p3-build-20260817
title: ACP-P3 build record — runbook automation engine core
status: complete
parent_prd: approval-control-plane
slice: acp-p3
date: 2026-08-17
---

# ACP-P3 build record — runbook automation engine core

Built to `ACP-P3-DESIGN-20260816.md` (incl. review-fold) by the Claude sonnet lane (Rule 17; gate =
Antigravity review complete per owner directive), orchestrator-reviewed PASS (independent).

## Files
- `scripts/ai/lib/approval_request.py` — EXTENDED `RUNBOOK_REGISTRY` (+4 runbooks: rotate-key,
  epoch-revoke, emit-grant, activate-foundation-c-slice) using the prep doc's plain-language copy, strict
  param patterns, declared_effects, P0-style single-shot stub effects. Existing 2 runbooks byte-unchanged
  (GOLDEN_CANONICAL_HASH still matches → P0 8/8).
- `scripts/ai/lib/approval_runbook_engine.py` (new) — `execute_runbook` (authorize → hash-verify →
  scope-check → ordered step loop with ledger-hit / pre-check-skip / apply / halt), `RunbookStepSpec`
  (typed atom + scope-projected param_names + verify pre-check + action), `MemoryRunLedger`/`FileRunLedger`
  (durable, fsync'd, atomic-replace), `make_signature_authorization_check` (wraps P1
  verify_execution_authorization + a P3-level freshness TTL for retry-reauth), `STEP_REGISTRY` (5 runbooks,
  20 stub steps; atoms named from the design set). Import-time `_validate_registry()` asserts step-effect
  order == declared_effects.
- `scripts/testing/test-approval-runbook-engine.py` (new) — 16 tests, dual-mode.

## Validation (orchestrator-verified, combined tree)
16/16 + pytest; P0 8/8, P1 20/20, P2 22/22 all green together; tier0 26/0. Security: authorization runs
BEFORE any step and re-runs on every retry (stale/expired rejected); atom pre-check gates execution
(idempotent-resume, no double-apply independent of the step log); scope-bound to hashed params +
declared_effects; no shell=True (array-arg atoms); authorized-once (copied signature on a new request_id
fails verify + the executed-id ledger); activate-* uses the declarative-update step kind, not a transient
command.

## Notes flagged
- Retry-freshness TTL (default 15 min) is a P3-level addition (P1 has no signature-age concept) — a
  bounded follow-up could move age into the signed authorization if desired.
- `KIND_SIGNER_CALL` defined + dispatch-supported but unused by the 5 runbooks (their key-touch is the
  single WebAuthn tap gating run entry) — documented contract, not a placeholder.

## Activation (Activation Gate)
Built + validated, atoms STUBBED, default-OFF. NOT turned on — real atom wiring (aq-provision-signer-key,
aq-epoch-bump, aq-event, nixos-rebuild), live authorization, and end-to-end exercise are the deployment
layer (separate owner-activated step). Dated deferral 2026-08-17. Codex confirmatory audit queued.
