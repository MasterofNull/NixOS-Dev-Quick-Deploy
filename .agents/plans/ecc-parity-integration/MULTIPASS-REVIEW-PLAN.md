# ECC exhaustive parity review

Reference:8321021c54d670126ce3b2969d5deb880b4b0c2a. Existing PRD is SSOT.
Canonical existing clone (Antigravity-owned; reuse read-only):
`/home/hyperd/.gemini/antigravity-ide/brain/a1a06f9b-6fe8-4046-bcb9-6fcc310002c0/scratch/ecc`.
Verified HEAD matches the reference, origin is affaan-m/ecc, and working tree is
clean. Existing advisory report:
`/home/hyperd/.gemini/antigravity-ide/brain/a1a06f9b-6fe8-4046-bcb9-6fcc310002c0/ecc_parity_and_gap_analysis.md`.
Report existence is not verified feature parity. Before any future acquisition,
check project references and the contributing agent's scratch/task records;
reuse a matching clone without fetching, switching HEAD or changing its files.
Every source lane must begin by reporting the absolute Git toplevel, exact HEAD
and porcelain status from that path. Cwd-relative Git evidence is invalid for
this review; a mismatch stops the lane without analysis or credit.
Owner requires deep source reading and multiple comparison passes, not a
README scan, bulk installation or feature-count equivalence. Runtime disabled.

Read ledger: every tracked artifact has path/blob/type/size, source family,
reader/pass, unread/partial/read state and evidence pointer. Track source, tests,
docs, agents/rules/prompts, manifests/lockfiles, examples and integrations.
Generated/binary material explicitly classified; metadata acquisition/hashing
is not semantic review and must not be called fully read. No source executes.
Machine artifact ledger: `SOURCE-READ-LEDGER.json` (3,716 entries with explicit
cataloged/cataloged-semantic/partial/full semantics and no runtime-PASS claim).
Attributable pass scopes/readers/limitations: `PASS-EVIDENCE.md`; every machine
row includes source family, reader, evidence pointer and conservative content
classification.

Pass1 source/features/tests: real behavior, prerequisites and adapters versus
README claims. Pass2 security/supply-chain/lifecycle: licenses, permissions,
data/secret flow, execution/hooks, loop budget, suspend/crash and recovery.
Pass3 architecture/native parity/product: exact local source/QA/dashboard
evidence and equivalent/stronger-local/partial/missing/unsuitable/approval-blocked
verdict; native SSOT reuse rather than duplicate authority. Pass4 adversarial
isolated execution/local-agent eval: actual accepted tools under malformed inputs,
missing permissions, stale state, cancellations, suspend and bounded edit tasks;
baseline/after correctness, tool success, latency/resources/tokens and completion
truth. Include GitHub CI/CD enforcement/provenance/activation evidence.

Every participating model uses the pass's same expert baseline. One synthesis
then frozen bounded queue; absent Claude gets catch-up, Gemini has claim-bound
source report request, missing advisory lanes not credited or blocking builds.
Partition source families to bound context without skipping relevant files.

Each feature row: upstream/local evidence, verdict/gap, native patch/reuse,
permissions/intake, priority/dependencies, owned files, actual tests/liveQA/
visibility, independent review, activation/owner decision. FT4 safety acceptance
is immediate gate while ECC reading proceeds independently. Multiple deliberate
coverage passes are not repeated same-slice planning/review; implementations land,
nonblocking findings go nextslice, real authority/safety failures get bounded fixes.

Complete only when all artifacts/features have explicit dispositions and every
implemented gap has proven acceptance; not all files copied or unsafe tools
enabled merely for exhaustive counts. Redundant temporary bare source retained at
/tmp/aq-ecc-reference.qaXBVs/upstream.git, no checkout/install executed.
