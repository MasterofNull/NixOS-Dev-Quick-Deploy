# C0.2 Preserved Suspended Diff — Mandatory Disposition

**Decision:** `REJECT_AND_REWORK`  
**Recorded:** 2026-07-11  
**Applies to:** uncommitted work preserved after suspension of `auth-c0.2-20260710`

Bound suspended-attempt evidence:

- `C0.2-IMPLEMENTATION-REPORT.md` raw SHA-256
  `e9443fe2fafc2e9e6e21197e4da1a02b9d19ed8d8e9af8184a47f5d96fd17787`
- `C0.2-POST-CHANGE.json` raw SHA-256
  `a406638d0452bef9e036c9f375f27653a0ae94b5b3d3b3b6910b12853418ca6b`
- Reported `aq-report` duration regression: cold `+40.8%`, warm `+45.9%`; both violate the
  authorized `≤10%` budget. The report's assertion that this is acceptable is rejected.

The preserved diff is incident/research evidence, not an implementation baseline and not inherited
authority. A fresh implementer may read it for lessons but must not stage or continue it wholesale.

Mandatory rework:

- reject writer/retention ownership in `scripts/testing/harness_qa/main.py`; use the declared shared
  `scripts/ai/lib/qa_evidence_store.py` boundary;
- reject QA-root ownership in `scripts/ai/lib/agent_run_events.py`; that module remains an event
  producer consumer of the shared boundary only where required;
- reject `AQ_TELEMETRY_ROOT` as an unrestricted `layer: all` production override or repo fallback;
- reject mutable `latest-qa-results.json` fallback writes;
- reject unverified `target_path` consumption in remediation/report readers;
- reject sequential tests labeled concurrent and every test that touches live repo/deployed telemetry;
- retain only independently reviewed concepts/fixtures after reimplementation against the amended
  root and exact inventory.
- meet both the relative `≤10%` and absolute `≤60 s` report budgets; a narrative waiver is not an
  acceptance mechanism.

Before a fresh assignment, an orchestrator must either preserve the diff outside the active worktree
and restore authorized surfaces clean, or bind an exact patch hash as read-only reference evidence.
No file from the suspended attempt is implicitly owned by the next implementer.
