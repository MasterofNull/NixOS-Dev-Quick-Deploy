# C0.2 Frozen Surface Inventory

**Status:** discovery snapshot, 2026-07-10; any additional production consumer requires plan amendment
and re-review before editing.

All producers, the lock, pointer and artifacts use one canonical telemetry-root resolver. A
repo/deployed root mismatch is a fail-closed correctness error, not two independent latest views.

| Surface | Current role | C0.2 disposition |
|---|---|---|
| `scripts/testing/harness_qa/main.py` | writes full `latest-qa-results.json` | replace writer with immutable artifact plus atomic pointer commit |
| `scripts/ai/aq-report` | reads latest QA; canonical effectiveness calculator | migrate verified pointer/artifact reader and evidence algebra |
| `scripts/ai/aq-auto-remediate.py` | reads latest QA for remediation | migrate verified reader; block on invalid evidence |
| `scripts/ai/_aq-qa-bash` | legacy QA reader/checker | additive compatibility reader, then retire full-result assumption |
| `dashboard/backend/api/routes/aistack.py` | projects report scorecard; contains fallback/synthesis behavior | consume canonical scorecard only; add provenance/reason/remediation fields |
| `dashboard.html` | existing effectiveness card | extend existing card; no new view |
| `assets/dashboard.js` | fetches/renders effectiveness endpoint | render canonical dimensions, reasons, run/hash/age and operator action |
| `scripts/ai/aq-system-state` | consumes report scorecard | preserve serialized semantics and blocking reasons |
| `scripts/ai/aq-health-spider` | probes effectiveness endpoint | alert on blocked/invalid provenance with stable reason |
| `scripts/ai/lib/aq_dashboard_client.py` | API client methods | contract-compatible response projection |
| `config/validation-check-registry.json` | focused behavioral test registration | extend existing `effectiveness-scorecard` triggers/tests |
| `scripts/testing/test-aq-report-effectiveness-scorecard.py` | scorecard behavior tests | expand algebra/required-claim fixtures |
| `scripts/testing/test-dashboard-agent-replay.py` | dashboard replay fixture | assert CLI/API semantic parity and provenance |
| `scripts/testing/harness_qa/phases/phase0.py` | integration QA registry | add `0.10.28` immutable QA/effectiveness integration check |
| `scripts/testing/harness_qa/phases/__init__.py` | Phase registration | update only if required by the existing Phase-0 registration contract |
| `scripts/ai/lib/agent_run_events.py` | useful-token/evidence producer contract | reject invalid producer envelopes at boundary |
| `config/schemas/maeah/agent-run-event.schema.json` | event schema | additive versioned evidence fields if required |
| `scripts/testing/test-qa-evidence-store.py` | concurrency, crash, pointer, privacy and retention | new focused test |
| `scripts/testing/test-evidence-algebra.py` | algebra edge cases and required claims | new focused test |

`scripts/data/seed-rag-knowledge.py` contains static issue text matching the search terms but is not a
runtime consumer and is excluded. Any other file discovered by a broader AST/runtime scan is evidence
that this inventory is incomplete; implementation stops and the plan/package root is amended.

## Exact integration visibility

- Phase-0 check `0.10.28`: run two isolated writers, validate pointer/artifact hashes and assert
  required unknown evidence cannot pass.
- Existing dashboard surface: `#section-effectiveness-scorecard` in `dashboard.html`.
- Existing endpoint: `/api/aistack/effectiveness/scorecard`.
- Existing spider probe: `effectiveness-scorecard`.
- Operator action: rerun evidence, inspect immutable artifact, or select a prior verified artifact;
  automation is disabled while provenance is invalid.

`GATE: REQUEST_REVISION — surface inventory and test filenames are frozen for planning, but deployed/dev
telemetry roots and retention owner require owner ratification and the implementation Intent Lock.`
