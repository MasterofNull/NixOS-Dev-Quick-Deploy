# C0.3 Provenance Reconciliation — Stage-2 Recovery Authorization

Authorization ID: `auth-c0.3-provenance-stage2-recovery-20260730`  
Idempotency key: `c0.3:provenance-reconciliation:stage2-recovery:20260730`  
Status: **PREPARED_ONLY — OWNER ACTIVATION REQUIRED**  
Prepared: 2026-07-30 UTC

## 1. Recovery state and purpose

The original reconciliation authorization
`auth-c0.3-provenance-reconciliation-20260727` is consumed and non-replayable.
Its Stage 1 reached the monotonic recovery state defined by the reviewed
authorization: both exact evidence refs exist, while the exact additive
authorization-consumption record remains untracked and uncommitted. The
original activation window expired and HEAD advanced, so its Stage-2 authority
cannot be reused.

This recovery permits only independent acceptance and settlement of the
existing one-file Stage-2 record. It permits no Stage-1 retry or Git-ref
mutation.

## 2. Exact binding

- Pre-execution HEAD:
  `97131faac372e89273f14372edbfa5e52b816d64`
- Governing design SHA-256:
  `5d9811d3fb5f8777283a78be39f6f154b4bfb5aef1cd2abb9188166eb33ea53e`
- Consumed original authorization SHA-256:
  `ac2686b865233b19d55d0a57bf329b7d77bcbceabd6612dd7555f8648a13322c`
- Acceptance candidate SHA-256:
  `7b00447bf9ab0a40808c78da4b002f6c84549c33185cf5ae3f9c23510191708b`
- Independent implementation-acceptance SHA-256:
  `1a76220dc3dbd0920d7f53133d3488214b8767f0f393b21b0379504d062e3644`
- Exact record path:
  `.agents/plans/aqos-refoundation-cycle0/C0.3-AUTHORIZATION-CONSUMPTION.md`
- Record SHA-256:
  `e48ef35574a480c52ef116dbfaf006edb7764fe4ee24a4580541f6814a3cfe09`
- Record Git blob:
  `205d412272bf7de3463f17c8da47577669bcdebb`

Any HEAD, record, artifact, ref, object, overlap, index, or authorization drift
is a hard stop and requires a new reviewed recovery authorization.

## 3. Preserved Stage-1 evidence

The following existing refs are read-only recovery prerequisites:

| Evidence ref | Exact tag object | Exact peeled target | Tag-content SHA-256 |
|---|---|---|---|
| `refs/tags/aqos-evidence/c0.3/integration-c9fe3974` | `7d1e2b709728e96016781098e39fb2b9903e9dc3` | `c9fe3974753395ed343fed9e922c9c2ea695129b` | `5cbf73ba0359217c21159fd6e70971a3551aea7446b2875af24e9011353d8c50` |
| `refs/tags/aqos-evidence/c0.3/reviewed-head-d918a21a` | `73e0226a0c8ce94fd2a702eef747e9e3fadeb075` | `d918a21a24081d1329efb0e85d4b3695627b4617` | `163f4b4732ed670ccb5dc7d27a4f6eecbbbfa13711f91578c9d2579bd9b2a85c` |

Both peeled commits must remain present and non-ancestors of current `main`.
This recovery neither proves complete ancestry beyond the shallow boundary nor
authorizes remote evidence publication.

## 4. Exact one-file ceiling

After activation, the assigned recovery implementer may only:

1. repeat the read-only evidence and drift preflight;
2. stage the exact existing record blob `205d4122…` at the one path in section 2;
3. obtain an independent final `PASS` bound to the staged blob, both exact tag
   objects and targets, and the frozen acceptance artifacts;
4. run the scoped validation below from an exact one-file index projection; and
5. create one ordinary non-merge commit whose sole content change is that exact
   record and whose sole parent is the exact pre-commit `main` tip.

The primary worktree's unrelated changes must remain byte-identical and absent
from the index. The implementer may not edit the record or any other file.

## 5. Required validation

Before staging:

```text
git rev-parse HEAD
git cat-file -t <each-tag-object>
git cat-file tag <each-tag-object> | sha256sum
git rev-parse <each-evidence-ref>^{commit}
git merge-base --is-ancestor <each-peeled-commit> HEAD
sha256sum <candidate> <acceptance> <record>
git hash-object <record>
git diff --cached --name-only
```

The two ancestry commands must each return `1`; every other command must match
the exact binding above. The index must initially contain no path attributable
to this recovery.

After exact staging:

```text
git diff --cached --name-only
git diff --cached --check
scripts/governance/check-doc-links.sh --active
scripts/governance/tier0-validation-gate.sh --pre-commit
```

The staged inventory must be exactly the one record path. Tier-0 evidence must
run inside an exact index projection and return exit `0` with every gate
passing. The ordinary commit hook must also return exit `0`. No differential,
waiver, skip, baseline failure, or static substitute is permitted. If the
current AQ-OS tracker live gate prevents a full PASS, this recovery waits until
that separately owned tracker slice is accepted, deployed, and live-validated;
the C0.3 record must not weaken or bypass it.

After commit, independently verify:

- the prior exact `main` tip is the new commit's sole parent;
- the commit contains exactly the record blob and no other path change;
- both tag refs, objects, peeled targets, and tag-content hashes are unchanged;
- no merge, rebase, cherry-pick, replacement ref, graft, or branch rewrite
  connected the protected historical chains.

## 6. Independent review and activation

The implementer is `codex-subagent-c03-stage2-recovery-implementer`. The
independent reviewer is
`codex-subagent-c03-stage2-recovery-reviewer`. The implementer cannot accept
its own work.

This document requires an independent read-only authorization `PASS`, followed
by owner activation naming this document's exact SHA-256, both assignees, exact
HEAD, and a UTC window no longer than 24 hours. Activation is single-use and is
consumed when the record is first staged.

## 7. Stop conditions and exclusions

Stop without mutation if any exact binding differs, either evidence ref is
missing or changed, either peeled commit becomes an ancestor, the record
requires correction, another lane owns the record or index, any second path
would enter the index, independent review is unavailable/non-PASS, or any
validation reports a new failure.

This recovery never authorizes:

- tag-object creation, ref creation/update/deletion/retargeting, reflog or Git
  configuration mutation;
- force, reset, merge, rebase, cherry-pick, graft, replacement ref, history
  rewrite, branch movement, garbage collection, prune, repack, fetch, push, or
  remote inspection;
- modification of the record or historical C0.3 artifacts;
- deployment, runtime/service/process action, Nix change, network/database
  access, traffic, cutover, authority adjudication, C0.3/Cycle-0 ratification,
  remote publication, or later-slice authority.

The resulting record closes only the single-use implementation-authorization
lifecycle. It does not change the already accepted implementation or grant any
runtime authority.
