# C0.2 Rework Report

Status: implementation and host-lane live endpoint validation complete; independent acceptance pending.

## Files touched

Authorized production/test surfaces only: `scripts/ai/lib/qa_evidence_store.py`, QA producer and frozen consumers, canonical report/API/dashboard projections, Phase-0/Bash registries, event envelope/schema, validation registry, and the three frozen tests. Collaboration checkpoint files were updated as explicitly required. No `.agents/telemetry/**`, deployed telemetry contents, NixOS wiring, mount, bind, tracked-path permission, or deletion operation was performed.

## Fixture results

- Immutable evidence store: 4/4 pass, including real multiprocessing writers.
- Evidence algebra: 6/6 pass.
- Telemetry-root boundary: 4/4 pass.
- aq-report scorecard: 8/8 pass.
- Dashboard API parity: 8/8 passed; live host endpoint confirmed after dashboard API restart.
- Agent-run envelope: pass.
- Syntax/JSON/Bash validation: pass.
- Tier0: 23/23 pass, with the caveat that staged-only gates saw no staged changes because `.git` is read-only.

## Baseline vs post-change

`aq-report --machine` completed 5 cold + 20 warm. Cold mean 21.781s versus 25.362s (-14.12%); warm mean 21.626s versus 22.234s (-2.73%). P95 was 23.481s cold and 22.549s warm. Both the <=10% relative and <=60s absolute budgets pass. The exact endpoint protocol completed 5 cold + 20 warm: cold mean/p95 3.461/3.585 ms and warm mean/p95 3.472/3.854 ms, each 2,805 response bytes and well inside the 250 ms target. Live missing required provenance returns `BLOCKED` with `automation_allowed=false`, as required.

## Mandatory rework disposition

Shared store owns root/lock/CAS/verification/retention; production authority is fixed; repo fallback and unrestricted overrides are removed; mutable latest fallback writes are retired; consumers verify immutable evidence; concurrent tests are real; remediation blocks invalid provenance; dashboard synthesis is retired; C0.2 owns 0.10.28 and capability flush moved to 0.10.35.

## Remaining acceptance work

Stage only authorized files, rerun Tier0, obtain independent non-Codex acceptance, consume the authorization, and commit.
