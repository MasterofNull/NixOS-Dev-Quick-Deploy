# C0.3 Current-Subject Independent Acceptance

**Verdict:** `PASS` — implementation acceptance only
**Reviewed candidate:**
`C0.3-CURRENT-SUBJECT-ACCEPTANCE-CANDIDATE.md`
**Candidate SHA-256:**
`7b00447bf9ab0a40808c78da4b002f6c84549c33185cf5ae3f9c23510191708b`
**Reviewed HEAD:** `d918a21a24081d1329efb0e85d4b3695627b4617`
**Original integration commit:** `c9fe3974753395ed343fed9e922c9c2ea695129b`
**Review date:** 2026-07-17 UTC
**Reviewer:** Codex sub-agent, OpenAI GPT-5 model family
**Role:** independent read-only implementation acceptance reviewer; the reviewer did not implement
the C0.3 subject

## Acceptance boundary

This review accepts the exact current composition of the C0.3 implementation subject. It repairs the
missing post-integration acceptance-evidence boundary without rewriting the historical commit. It does
not consume authorization, select an authority, ratify C0.3 or Cycle 0, authorize Cycle 1, grant a
runtime writer, approve a migration, or adjudicate any of the ten `SPLIT_BRAIN` owner decisions.

Parent authorization `auth-c0.3-20260711`, idempotency key
`301f96f5-8882-4214-9308-f11ce5213a54`, remains `UNCONSUMED`. A separate orchestrator-owned
authorization-consumption addendum is required if the parent key is to be consumed.

## Exact subject identity and inventory

The normalized candidate digest was recomputed and matched
`7b00447bf9ab0a40808c78da4b002f6c84549c33185cf5ae3f9c23510191708b`. Direct comparison against the
staged prior subject `e54bff22bdb45ef7170a6a532fa8c9b74a3d6ebdfbabb44a79de60b434b1d817`
found the same 120 lines and exactly four byte differences: removal of two trailing spaces from lines
3, 4, 6, and 7. Every old/new line pair is equal after `rstrip`; no semantic byte changed. Repository
HEAD remained `d918a21a24081d1329efb0e85d4b3695627b4617`.

`git diff-tree --no-commit-id --name-only -r c9fe3974753395ed343fed9e922c9c2ea695129b`
returned exactly 23 paths. A programmatic set comparison found no missing or extra path, and a fresh
SHA-256 calculation over every path matched all 23 hashes frozen in the candidate. The same full
inventory check was repeated after Tier 0; mismatch count remained zero.

The four shared paths marked `EVOLVED` have the stated, bounded lineage:

- `0c171504`, `499e5a26`, and `fbeffbab` each modify `assets/dashboard.js`,
  `config/validation-check-registry.json`, `scripts/ai/_aq-qa-bash`, and
  `scripts/testing/harness_qa/phases/phase0.py` for the accepted local-inference L1A, L2A, and L2B-A
  shadow slices.
- `66391367` subsequently modifies only `assets/dashboard.js` for accepted L2B-A.1 dashboard parity.
- `git blame` binds the C0.3 `0.10.29` Python registration, Bash registration, focused-CI registry
  entry, atomic publication implementation, and fail-closed dashboard rows to `c9fe3974`; the later
  commits append their own local-inference checks and projections without replacing or weakening
  C0.3 behavior.

The current composition therefore preserves:

- a stdout-only, bounded, read-only checker;
- one Python Phase-0 invocation of `_check_state_authorities(ctx)`;
- the Bash `0.10.29` registration running both focused suites;
- the enabled focused-CI `state-authorities` entry and complete C0.3 trigger inventory;
- fail-closed Phase-0 contract validation and exclusive `O_EXCL`/`O_NOFOLLOW` temporary publication;
- refusal of symlink and non-regular publication targets;
- a one-hour dashboard freshness policy and no missing-value-to-zero coercion.

## Current validation evidence

Commands were run against the stable subject above:

1. `python3 scripts/testing/test-state-authorities.py`
   - exit 0; `PASS: 10 state-authority checks`.
2. `python3 scripts/testing/test-dashboard-governance-projection.py`
   - exit 0; `PASS: 20 dashboard governance projection checks`.
   - Fixtures cover malformed and contradictory counts, invalid identity/time fields, stale evidence,
     symlinked targets, pre-created temporary symlinks, atomic fixed-path publication, and frontend
     no-default-zero behavior.
3. `nix shell nixpkgs#time -c time -v scripts/governance/check-state-authorities.py --machine`
   - exit 0; `registry_valid=true`; 10 authorities; `SPLIT_BRAIN=10`; `blocker_count=10`;
     `error_count=0`; `cycle1_authority=NOT_AUTHORIZED`.
   - 1,979 files scanned of an 8,000-file cap; no truncation; 10 objects of a 128-object cap.
   - Checker-measured duration 1.652 seconds of a 15-second limit; GNU-time wall 1.97 seconds.
   - Maximum RSS 53,272 KiB, below the 256 MiB limit; output 6,026 bytes, below 5 MiB.
   - Zero checker inference calls. Source inspection found no model, embedding, GPU, network,
     lifecycle-store, migration, live-traffic, or runtime-authority path; local subprocesses are
     bounded Git discovery only.
4. `scripts/governance/check-doc-links.sh --active`
   - exit 0; no broken local links.
5. `git diff --check` and `git diff --cached --check`
   - both exit 0.
6. `scripts/governance/tier0-validation-gate.sh --pre-commit`
   - exit 0; 23 passed, 0 failed.
   - Phase 0 passed 168 checks; focused CI, repository structure, roadmap, configuration,
     cross-surface, environment-contract, and extension gates passed.

The standard Phase-0 run refreshed the volatile untracked
`.agents/governance/state-authorities-latest.json` projection. That generated operational snapshot is
not one of the 23 reviewed implementation paths and carries no acceptance or authority weight.

## Immutable recovery evidence

The immutable artifact
`evidence/cas/sha256/491dce6c806b8638f0d97aab379ed4421c78272beb58090e55d178c15def4649.json`
was read as data and independently recomputed:

- byte SHA-256 matches its content-addressed filename;
- size is 198,523 bytes;
- status is complete and `overall_budget_ok=true`;
- exactly 100 samples exist in four 25-sample groups: idle/full, idle/incremental,
  active-local-generation/full, and active-local-generation/incremental;
- every group contains five cold and twenty warm samples;
- every process and GNU-time exit is zero, every sample reports a valid registry,
  `SPLIT_BRAIN=10`, `blocker_count=10`, the exact condition counts, and a passing checker budget;
- active-load samples contain live proof bound to the same registered local task; idle samples contain
  no false active proof;
- independently recomputed p50, p95, maximum duration, maximum RSS, output, file/object counts, and
  cold/warm counts match all four stored aggregates exactly;
- all six subject drift guards report `ok=true`;
- `checker_inference_calls=0`.

The accepted CAS evidence is the immutable V2 recovery subject only. It does not inherit evidence from
either invalid V1 artifact.

## Rejected-artifact exclusion

The following files remain untracked and outside the reviewed 23-file subject:

- `evidence/rejected/c0.3-antigravity-unauthorized-collector.py`
- `evidence/rejected/c0.3-antigravity-unauthorized-collector.py.gz`
- `evidence/rejected/DISPOSITION.md`

They were not executed, imported, staged, or assigned acceptance weight. The two tracked rejected JSON
artifacts were inspected only as incident provenance. The invented-singleton and unauthorized-overwrite
results remain rejected and contribute zero evidence to this verdict.

## Findings and next gate

The implementation truth is internally consistent: all ten registry rows remain `SPLIT_BRAIN`, all
ten selected target authorities remain null, the checker emits ten ratification-blocker findings, and
`meta.cycle1_authority` remains `NOT_AUTHORIZED`. Successful discovery proves the checker and projection;
it does not prove singleton authority.

The next blocking action is owner adjudication of the ten `SPLIT_BRAIN` authority decisions under the
governing Cycle-0 process. Until those decisions and the remaining ratification requirements are
satisfied, C0.3 implementation acceptance must not be represented as C0.3 ratification, Cycle-0 exit,
or Cycle-1 authorization.

VERDICT: PASS — exact-current C0.3 implementation subject satisfies all acceptance criteria; Cycle-0 ratification remains blocked by 10 SPLIT_BRAIN owner decisions
