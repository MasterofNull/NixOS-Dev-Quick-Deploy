# C0.3 Implementation Report — Authority, Projection, Bypass & Retirement Ledger

**Slice:** C0.3 (final Cycle 0 slice). **Authorization:** `auth-c0.3-20260711`, single-use key
`301f96f5-8882-4214-9308-f11ce5213a54`, expiry 2026-07-18.
**Package root (verified now):** `8d17f3ac8e4f75b16ece5ba38bd7bfe9e0dad2cc26f27ff3a8f65d8269af8559`
(`sha256sum -c PACKAGE-ROOT.sha256` → OK; no drift).
**Implementer:** Opus 4.8 (resumed the interrupted codex-lane slice, then completed an
independent-review revision pass). **Reviewer:** independent non-codex family returned
**REQUEST_REVISION**; this revision addresses those findings. **Posture:** discovery only —
`meta.cycle1_authority = NOT_AUTHORIZED`.

**State: ALL EXECUTION GATES PASS / REVISED EXACT-FINAL-DIFF REVIEW PENDING.** The recovery addendum below
supersedes earlier status and inventory statements throughout §§1–10. Do not integrate until exact-final-diff
review passes.

---

## Recovery V2 addendum — 2026-07-13 (supersedes stale status/inventory in §§1–10)

Amendment 1 explicitly authorized `phase0.py`, dual `_aq-qa-bash` registration, compatibility
renumbering of the displaced planning-only Bash check to 0.10.36, and the replacement resource
protocol. Those surface gaps are resolved. The focused validation registry triggers on both harnesses.

The first recovery attempt completed a valid Codex collection but lost its bytes when a concurrent
artifact self-identifying as an Antigravity implementation lane replaced the mutable evidence path.
That attempt and the earlier Claude falsified-singleton artifact have zero acceptance weight and remain
quarantined with incident records.

Recovery V2 collected privately and published exactly once to immutable subject:

`evidence/cas/sha256/491dce6c806b8638f0d97aab379ed4421c78272beb58090e55d178c15def4649.json`

| Condition/mode | samples | cold/warm | p50 | p95 | max | max RSS | files | budget |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| idle full | 25 | 5/20 | 2.048s | 2.226s | 2.307s | 51.973 MiB | 1951 | PASS |
| idle incremental | 25 | 5/20 | 1.910s | 2.083s | 2.115s | 51.598 MiB | 7 | PASS |
| active local generation full | 25 | 5/20 | 2.988s | 3.369s | 3.518s | 52.121 MiB | 1951 | PASS |
| active local generation incremental | 25 | 5/20 | 3.058s | 3.332s | 3.372s | 51.617 MiB | 7 | PASS |

All 100 samples exited 0, scanned 10 objects, emitted about 6 KiB, stayed below 8,000 files/128
objects, made zero checker inference calls, and truthfully reported `SPLIT_BRAIN=10`,
`blocker_count=10`. Six guards preserved the staged subject and protected hashes.

Independent review recomputed the complete artifact and aggregates with zero discrepancies; the CAS
digest/inode/size remained exact. Independent idle and host-observed active-load samples passed. The
load sample completed at 3.60 s wall and 52,352 KiB RSS while generation output increased by about 650
bytes. **Evidence verdict: APPROVE.** Sandbox PID-namespace false negatives were corrected by using the
approved host observer; this observer defect remains logged separately.

Recovery-focused validation passes: state-authority tests 10/10 and dashboard projection tests 20/20.
Exact-diff review required count-consistency validation, exclusive no-follow temporary-file
creation, and an explicit one-hour snapshot freshness policy. Those bounded corrections are implemented;
the refreshed suite includes contradictory-count, stale-snapshot, and pre-created temporary-symlink
negative fixtures. Validation also covers
Python compilation, Bash syntax, dashboard JavaScript syntax, and JSON/YAML parsing. Final execution
gates also pass:

- focused CI-sensitive registry: PASS;
- staged structure lint: PASS;
- package root `8d17f3ac...`: PASS;
- full `aq-qa 0 --machine`: exit 0;
- Tier0 pre-commit: 23 passed, 0 failed, including Phase 0 with 166 checks.

**Current verdict: ALL_EXECUTION_GATES_PASS / REVISED_EXACT_FINAL_DIFF_REVIEW_PENDING.** Parent key
`301f96f5-8882-4214-9308-f11ce5213a54` remains `UNCONSUMED` until all remaining gates pass and the
integrating commit is created.

---

## 1. Executive summary

C0.3 produces an honest System State Authority & Retirement Ledger for every critical cross-system
state, a **bounded read-only** checker that scans the registry against tracked production files, a
Phase-0 integration check, a read-only projection onto the existing operator audit-integrity card, a
Cycle 1 storage-spine ADR (proposal only), and a narrow authority summary projected into the canonical
kernel declaration. The checker **never** mutates inspected state, adds a runtime writer/route/service/
store, or migrates storage. All ten inventoried authorities are honestly reported `SPLIT_BRAIN`; no
singleton owner is invented. Every `SPLIT_BRAIN` row blocks C0.3 **ratification** (not discovery) until
an adjudicated target disposition + deadline exist.

This report finalizes the interrupted partial work: it fixed one integration defect (the Phase-0 check
was defined but never invoked), wired the focused-CI registry, and added the two projection docs plus
evidence artifacts. The independent-review revision pass then (a) made the checker **genuinely
read-only** — no `--snapshot`, no `mkdir`/write/replace, no caller-supplied output path — and moved
snapshot publication into the authorized Phase-0 integration, which validates the `{meta, findings}`
contract fail-closed and atomically publishes ONLY to the fixed repo path
`.agents/governance/state-authorities-latest.json` (exclusive no-follow temporary creation and refusal
  of symlink/non-regular targets); (b) made the
audit-card projection and the `dashboard.js` renderer **fail-closed** (no invented zero, unknown/degraded
→ warn, including snapshots older than one hour); (c) expanded the focused-CI trigger list to every
authorized C0.3 surface; and (d) registered the check in both Python and Bash harnesses under Amendment 1.

Focused validation passes (§5). **Final Tier0 acceptance is NOT claimed or run** — two authorized-surface
amendments remain open (`phase0.py` and dual-Bash QA parity, §6/§8), and the mandatory budget-evidence
protocol is unsatisfied (§4).

---

## 2. Touched files (exact)

**New (A):**
- `config/system-state-authorities.yaml` — registry SSOT (10 authorities)
- `config/schemas/system-state-authorities.schema.json` — draft-07 schema
- `scripts/governance/check-state-authorities.py` — bounded, **strictly read-only** checker (no
  `--snapshot`, no `mkdir`/write/replace; emits `{meta, findings}` to stdout only)
- `scripts/testing/test-state-authorities.py` — 10 focused tests (incl. read-only checker assertion)
- `scripts/testing/test-dashboard-governance-projection.py` — focused checks (fail-closed fixtures,
  contradictory-count/staleness rejection, Phase-0 target and temporary-symlink refusal, and frontend
  no-default-zero)
- `docs/architecture/aqos-cycle1-state-spine-adr.md` — Cycle 1 ADR (proposal, NOT authorized)
- `.agents/governance/evidence-invalid-round.json` — invalid-round evidence (planning-round row)
- `.agents/governance/evidence-failed-qa-report.json` — failed-QA/report evidence (qa-effectiveness row)

**Modified (M):**
- `dashboard/backend/api/routes/audit.py` — **fail-closed** read-only `state_authorities` projection on
  the existing `/audit/operator/integrity` route (adds no route: still exactly 3 `@router.` GETs). Only a
  document satisfying every shape/type/identity invariant yields `available:true`; malformed/incomplete/
  stale (>1 hour) or internally contradictory → `available:false` with null blockers (never an invented zero).
- `assets/dashboard.js` — `stateAuthorityRows()` renders last check / age / blocker count on the existing
  audit-integrity card; never coerces a missing blocker count to 0/OK (unknown/degraded → warn).
- `config/validation-check-registry.json` — new focused-CI entry `state-authorities` with trigger paths
  covering every authorized C0.3 behavior/projection surface.
- `scripts/testing/harness_qa/phases/phase0.py` — `_check_state_authorities` (0.10.29) **plus its
  registration** into the phase runner (the defect fixed this session). It also runs the read-only checker,
  validates the contract fail-closed, and is the **sole atomic publisher** of the fixed-path snapshot
  (exclusive no-follow temporary creation; refusing symlink/non-regular targets).
- `scripts/ai/_aq-qa-bash` — dual-harness registration of 0.10.29 plus compatibility renumbering of the
  displaced planning-only check to 0.10.36, explicitly authorized by Amendment 1.
- `docs/architecture/canonical-kernel-declaration.md` — narrow C0.3 authority-summary projection

**Not committed (self-owned, regenerated):** `.agents/governance/state-authorities-latest.json` — the
volatile snapshot the audit card reads, now published by the Phase-0 integration (not the checker).
Rewritten with a fresh `run_at` on every Phase-0 run, so it is left untracked (matches the `*-latest.json`
gitignore precedent, `.gitignore:115`). The audit route fails closed to `available:false` when it is
absent, malformed, or stale.

**Left unstaged (out of C0.3 scope):** `.agent/memory/issues-backlog.md`,
`ai-stack/agent-memory/MEMORY.md`, `.agent/PROJECT-LOCAL-INFERENCE-CONTRACT-PRD.md` (declared owner
changes); `flake.lock`, `nix/data/profile-system-packages.nix`, `nix/home/base.nix` (pre-existing
unrelated Nix edits — the Nix hard boundary applies).

---

## 3. Checker scan meta (full run)

| Field | Value |
|---|---|
| authorities_total | 10 |
| condition_counts | SINGLE 0 · SPLIT_BRAIN 10 · UNKNOWN 0 · UNOWNED 0 |
| blocker_count | 10 (all ratification-blockers = the 10 SPLIT_BRAIN rows) |
| error_count | 0 (registry structurally valid; schema validates) |
| files_scanned | 1951 / cap 8000 (measured baseline 4793; 19 exclusion tokens) |
| truncated | false |
| output_bytes | 6026 (< 5 MiB) |
| registry objects | 10 (< 128) · max projections/object 2 (< 32) |
| cycle1_authority | NOT_AUTHORIZED |
| inference/APU/GPU work | zero (no model/embedding/GPU calls) |

---

## 4. Budget evidence — MANDATORY PROTOCOL NOT SATISFIED (blocker)

The plan's budget-evidence requirement is a **5 cold + 20 warm** idle/load sampling protocol captured with
`/usr/bin/time -v`. **This protocol is NOT satisfied.** What was collected instead is a smaller set of
in-process `resource.getrusage` max-RSS readings plus `time.monotonic` durations:

| Mode | files | p50 dur | p95 dur | max dur | budget | max RSS (getrusage) | RSS budget |
|---|---|---|---|---|---|---|---|
| full (`--machine`) | 1951 | 1.508s | 1.555s | 1.555s | ≤15s | 52.1 MiB | ≤256 MiB |
| incremental (`--changed`) | 7 | 1.386s | 1.468s | 1.468s | ≤10s | 51.6 MiB | ≤256 MiB |

Output ≈6 KiB ≤ 5 MiB. These readings are indicative but **do not** constitute the required evidence:

- **No mandatory pre-edit baseline** was captured.
- **The 5 cold + 20 warm idle/load `/usr/bin/time -v` samples were not collected** (neither the cold/warm
  split nor the idle-vs-under-load conditions).
- **`/usr/bin/time` being absent on this host is NOT a waiver.** It must be provisioned (e.g. add
  `pkgs.time` to the dev shell / system packages) and the full protocol run, or an explicit dated deferral
  authorized. `getrusage` is a *substitute metric*, not the specified instrument.

This gap alone blocks final acceptance; it is carried as an open amendment item (§10).

---

## 5. Test & gate results (focused only — final Tier0 acceptance NOT run)

Ran and verified in this revision pass:

- `python3 scripts/testing/test-state-authorities.py` → **PASS: 10** (incl. read-only checker assertion).
- `python3 scripts/testing/test-dashboard-governance-projection.py` → **PASS: 19** (fail-closed fixtures
  for incomplete JSON, `registry_valid=false`, wrong Cycle-1 authority, missing/string/float/negative/bool
  blocker_count, bad `authorities_total`, invalid `condition_counts`, invalid/non-UTC/non-string timestamp,
  symlink target, genuine-zero-OK, frontend no-default-zero, and Phase-0 publisher atomicity + symlink
  refusal).
- `python3 -m py_compile` (phase0.py, audit.py, checker, both tests) → OK.
- Checker CLI: `--machine` emits exact `{meta, findings}` (slice=C0.3, registry_valid=True, 10 blockers,
  cycle1=NOT_AUTHORIZED); `--changed` exit 0; `--explain planning-round` exit 0. `--snapshot` is now an
  **unknown argument** (argparse exit 2) and writes nothing.
- Phase-0 integration invoked directly → PASS; snapshot atomically published to the fixed path
  `.agents/governance/state-authorities-latest.json` (regular file, not a symlink).
- `python3 -c 'json.load(...)'` on `validation-check-registry.json` + both evidence artifacts → valid;
  `yaml.safe_load` on the registry → valid; `node --check assets/dashboard.js` → OK.
- `git diff --cached --check` → clean (verified in §7 checklist).

**NOT run / NOT claimed:** the full Phase-0 harness sweep, `scripts/ai/aq-qa 0`,
`repo-structure-lint.sh --staged`, and `tier0-validation-gate.sh --pre-commit` are **deliberately not run
as acceptance** in this revision pass, because authorization/evidence amendments remain open (§10). No
"all gates pass" or "23/23" claim is made. Integration is the codex lane's responsibility and must not
proceed until the amendments clear.

---

## 6. Check-ID integration & uniqueness (Python harness only; Bash parity DEFERRED)

The plan (§Integration, line 293) assigns C0.3 the Phase-0 check ID **0.10.29**. In the **Python** harness
(`scripts/testing/harness_qa/phases/phase0.py`) this ID was free; `_check_state_authorities` registers it,
and its registration into the phase runner (`results.extend(_check_state_authorities(ctx))`) — missing in
the interrupted partial — was added, so the Python harness now actually runs the check.

**Two open reconciliations (both need an authorization amendment):**

1. **`phase0.py` inventory gap.** The plan's Integration step *requires* a Phase-0 registration, but
   `phase0.py` is **not** on the frozen package's literal permitted-edit / preflight file inventory. The
   edit here is exactly that plan-required registration (plus the read-only-checker publisher), nothing
   more, but the literal inventory must be amended to list `phase0.py` for the edit to be in-bounds.

2. **Dual-Bash QA parity DEFERRED.** The parallel Bash harness (`scripts/ai/_aq-qa-bash`) would, by the
   C0.1/C0.2 precedent, also register 0.10.29 and renumber the displaced pre-existing planning-only check
   (→ 0.10.36, `compatibility-renumbered`). That edit was made earlier, found **unauthorized** by review
   (not on the permitted surface), and has been **fully reverted** — `_aq-qa-bash` no longer appears in the
   C0.3 diff. Registering it is pending a **narrow authorization amendment**. Until then there is a known,
   documented Python/Bash divergence: 0.10.29 = state authorities in Python, unregistered in Bash.

---

## 7. Authorization consumption

Authorization `auth-c0.3-20260711` (single-use key `301f96f5-8882-4214-9308-f11ce5213a54`) is **NOT yet
consumed**. Because this is `REQUEST_REVISION` with open amendment items (§6/§8/§10), the single-use key
must **not** be marked CONSUMED and the slice must **not** be integrated until those amendments clear.
`PACKAGE-ROOT.json`, `PACKAGE-ROOT.sha256`, the frozen plan, and the authorization record are **not**
modified by this revision pass (the package root still verifies at `8d17f3ac…`), and **no amendment was
created or activated** — that is the orchestrator's action, not this implementer's. When the amendments
are granted and acceptance passes, the integrating commit (codex lane) records the key as CONSUMED, as
C0.1/C0.2 did. Use limit single-use; expiry 2026-07-18 (unexpired).

---

## 8. Deviations, unmet requirements & open blockers (for the reviewer)

Stated plainly — this slice does **not** claim a clean run; the following are unresolved:

1. **Budget-evidence protocol UNMET (blocker).** The mandatory **pre-edit baseline** and **5 cold + 20
   warm** idle/load `/usr/bin/time -v` samples were **not collected**. Only in-process `getrusage`/
   `monotonic` readings exist (§4). **`/usr/bin/time` absence on this host is NOT a waiver** — the binary
   must be provisioned and the full protocol run, or a dated deferral authorized.
2. **`phase0.py` inventory reconciliation (blocker).** Plan-required Phase-0 registration, but `phase0.py`
   is omitted from the frozen package's literal permitted/preflight inventory (§6). Needs amendment.
3. **Dual-Bash QA parity pending authorization (blocker).** `_aq-qa-bash` registration reverted and
   removed from the diff; registering 0.10.29 in the Bash harness is deferred to a narrow amendment (§6).
   A documented Python/Bash 0.10.29 divergence exists until then.
4. **"Under one representative local-inference request" load baseline not separately collected** — the
   checker performs zero inference/APU/GPU work by hard constraint and is never on the generation hot
   path, but the load-condition sample is still part of the unmet §4 protocol, not a standalone waiver.
5. **`dashboard.html` unchanged** — the plan lists it as a C0.3 surface, but the projection reuses the
   existing `#auditIntegrityDetails` container (rows injected dynamically by `dashboard.js`), honoring the
   dated **no-new-panel exemption**. No structural HTML edit was needed or made.
6. **Volatile snapshot not committed** — `state-authorities-latest.json` is regenerated by the 0.10.29
   check (now published by the Phase-0 integration, not the checker). Left untracked, matching the
   `*-latest.json` precedent. `.gitignore` left untouched (outside the permitted surfaces).

Posture that *does* hold: no package-root drift (root still verifies at `8d17f3ac…`), the checker confers
no storage authority (`cycle1_authority = NOT_AUTHORIZED`), and no Cycle 1 storage is implied or authorized.
But the three blockers above mean stop-conditions **are** effectively active for acceptance — this is
`REQUEST_REVISION`, not a ready-to-integrate slice.

---

## 9. Evidence artifacts

- `.agents/governance/evidence-invalid-round.json` — `--explain planning-round`: the invalid-round
  authority graph (observed writers + fail-open Markdown fallback + lane-status quorum bypass).
- `.agents/governance/evidence-failed-qa-report.json` — `--explain qa-effectiveness`: the failed-QA/
  report authority graph (two clobber writers + dashboard `_synthesize_scorecard_from_report` bypass).
- `.agents/governance/state-authorities-latest.json` — live projection snapshot (untracked). It is now
  published by the **Phase-0 integration** (`_check_state_authorities` runs `check-state-authorities.py
  --machine`, validates the contract, and atomically writes this fixed path). Rebuild: run Phase-0
  (`aq-qa 0`). The read-only checker no longer writes it.
- Registry SSOT `config/system-state-authorities.yaml` — full per-row evidence with `file:line`
  provenance, projections+rebuild sources, bypasses, shims (owner/telemetry/deadline), adjudicator/
  recovery owners, target hypotheses, and 2026-10-09 resolution deadlines.

---

## 10. Verdict

**VERDICT: AMENDMENT_REQUIRED.** Focused validation passes (§5), and the reviewer's substantive findings
are resolved: `_aq-qa-bash` removed from the diff; the checker is genuinely read-only (no `--snapshot`/
write path); snapshot publication moved into the authorized Phase-0 integration (fail-closed contract
validation + atomic fixed-path write, symlink-refusing); the audit-card projection and `dashboard.js`
renderer fail closed (no invented zero); the focused-CI trigger list covers every authorized C0.3 surface.

This slice is **NOT ready for integration.** Three items require an orchestrator-granted authorization
amendment before acceptance can be attempted, and none may be created/activated by this implementer:

1. Provision `/usr/bin/time` and capture the mandatory pre-edit baseline + 5 cold / 20 warm idle-and-load
   `/usr/bin/time -v` budget samples (or authorize a dated deferral). Absence of the binary is not a waiver.
2. Amend the frozen package's permitted/preflight inventory to include `scripts/testing/harness_qa/phases/
   phase0.py` (plan-required Integration edit, currently outside the literal inventory).
3. Grant a narrow amendment to register 0.10.29 in the Bash harness (`scripts/ai/_aq-qa-bash`) for
   dual-harness QA parity, then perform that registration under it.

Until those clear, the single-use authorization key stays UNCONSUMED and the codex lane must not integrate.
