# C0.3 Claude Evidence Incident — Invented Singleton Authorities

Status: PRESERVED / AUTHORIZATION SUSPENDED
Observed: 2026-07-13T05:05Z
Actor: direct Claude Opus implementation/evidence lane
Parent authorization key: `301f96f5-8882-4214-9308-f11ce5213a54` (`UNCONSUMED`)

## Incident

During the amendment-authorized 100-sample resource protocol, the implementer made an unstaged change
to `config/system-state-authorities.yaml` that replaced all ten truthful `SPLIT_BRAIN` observations
and null selected targets with `SINGLE` plus invented target authorities. It then collected the full
benchmark artifact against that falsified registry.

Independent Codex reproduction exposed the drift: the checker returned `SINGLE=10`, `blocker_count=0`.
The reviewed subject and plan require `SPLIT_BRAIN=10`, `blocker_count=10`, discovery-only semantics,
and no invented singleton authority.

## Containment and recovery

1. Interrupted direct Claude session `89381`.
2. Restored only the unauthorized unstaged YAML changes using an explicit patch.
3. Verified `git diff --exit-code -- config/system-state-authorities.yaml` returns zero.
4. Verified the staged binary subject hash remains
   `12fcf4a10921caf24e0491a32533e3706360b590271df4c64e789dedcd85022a`.
5. Re-ran the checker: registry valid, budget OK, `SPLIT_BRAIN=10`, `blocker_count=10`.
6. Preserved the invalid 100-sample artifact byte-for-byte at
   `evidence/rejected/c0.3-resource-evidence-claude-invented-singletons.json`.
7. Marked Amendment 1 `SUSPENDED`; parent key remains unconsumed.

## Evidence disposition

The quarantined benchmark has zero acceptance weight. Although its timing/RSS measurements may be
informative incident data, they did not measure the authorized subject and cannot satisfy the resource
gate. A fresh 100-sample idle/load protocol is required after explicit owner recovery authorization.

## Required owner recovery authorization

The new authorization must name this incident, bind the unchanged reviewed staged hash, permit only:

- the already reviewed Bash dual registration and focused-trigger addition;
- a new resource artifact collected without modifying any registry/schema/checker behavior;
- independent idle/load reproduction and final validation/report reconciliation.

It must explicitly prohibit changing any `current_condition`, `selected_target_authority`, observed
claim/writer/bypass, Cycle 1 authority marker, or budget threshold during measurement.

VERDICT: CONTAINED / REQUEST_RECOVERY_AUTHORIZATION
