# C0.3 Provenance Reconciliation Authorization

Authorization ID: `auth-c0.3-provenance-reconciliation-20260727`  
Idempotency key: `c0.3:provenance-reconciliation:20260727`  
Status: **PREPARED_ONLY — NO REF, WORKTREE, INDEX, OR COMMIT ACTION AUTHORIZED**

This is a single-use, hash-bound authorization template for the exact two-stage
reconciliation designed in
`.agents/plans/aqos-refoundation-cycle0/C0.3-PROVENANCE-RECONCILIATION-DESIGN.md`.
It is not active. It permits no action until an independent reviewer passes this
exact authorization and the owner later activates its exact SHA-256 for one
named implementer and a window no longer than 24 hours.

## 1. Exact subject and repository binding

- Exact pre-execution HEAD:
  `107f7e8ab2452b4d89ff737b28966e35bf4f9e24`
- Reviewed design SHA-256:
  `5d9811d3fb5f8777283a78be39f6f154b4bfb5aef1cd2abb9188166eb33ea53e`
- Repository object format: `sha1`
- Repository shallow state: `true`
- Current `main` root:
  `a0bd5520e9b80e3850b23bb7d17dfea0a9e1c639`
- Parent authorization ID: `auth-c0.3-20260711`
- Parent idempotency key: `301f96f5-8882-4214-9308-f11ce5213a54`
- Parent lifecycle state before reconciliation: `UNCONSUMED`
- Accepted candidate SHA-256:
  `7b00447bf9ab0a40808c78da4b002f6c84549c33185cf5ae3f9c23510191708b`
- Acceptance artifact SHA-256:
  `1a76220dc3dbd0920d7f53133d3488214b8767f0f393b21b0379504d062e3644`
- Acceptance verdict: `PASS` — implementation acceptance only

Any HEAD, topology, object, artifact, design, ref, overlap, or lifecycle drift is
a hard stop and requires a new reviewed authorization. This authorization does
not reinterpret implementation acceptance as C0.3 ratification, Cycle-0 exit,
authority-row convergence, or later-slice authority.

## 2. Frozen commit, tree, parent, and content metadata

| Subject | Exact value |
|---|---|
| Accepted integration commit | `c9fe3974753395ed343fed9e922c9c2ea695129b` |
| Integration parent | `b62dde1027ab8cb61c714dcec8d8cfdc6e333df1` |
| Integration tree | `17a36cf37ae651925bfae770094b9483185e2790` |
| Integration object type / size | `commit` / `447` bytes |
| Integration commit-content SHA-256 | `7a0882a45f68b02be749b2fe24688f95ad5c179c2b4f98f91c7b7df99774985d` |
| Integration tree-content SHA-256 | `895b5bd96223ef236d651e6c525cc7384faa553feaae37df683494a0aa7dc8f3` |
| Reviewed subject commit | `d918a21a24081d1329efb0e85d4b3695627b4617` |
| Reviewed subject parent | `08203901ac264d7039412064473880755576f2de` |
| Reviewed subject tree | `6ff31b8bf49a05da9852bc77097afb7d6d77a353` |
| Reviewed object type / size | `commit` / `2300` bytes |
| Reviewed commit-content SHA-256 | `4a1103dd7469a77994122776add171858cdd0bf8b06cee7f5ffcac700dcb6844` |
| Reviewed tree-content SHA-256 | `031b416c4eb758e17cf8c61afea7d1035b8f9ff91fd378142cde098244485e52` |
| Frozen shallow boundary | `a0f79cf95b88a82035c29ed3df58d6e701f6a566` |
| Raw hidden parent recorded by boundary | `d247f7f011cc2df085d70f0d86e5803c74f1c847` |

Both protected commits currently exist and are not ancestors of the exact
pre-execution HEAD. The reconciliation preserves only each exact target and its
currently available local ancestry segment. It makes no complete-ancestry or
remote-durability claim.

## 3. Frozen evidence refs and deterministic tag metadata

Exactly these two previously absent refs and no others are in scope:

| Ref | Exact peeled target |
|---|---|
| `refs/tags/aqos-evidence/c0.3/integration-c9fe3974` | `c9fe3974753395ed343fed9e922c9c2ea695129b^{commit}` |
| `refs/tags/aqos-evidence/c0.3/reviewed-head-d918a21a` | `d918a21a24081d1329efb0e85d4b3695627b4617^{commit}` |

Both annotated tag objects must use:

- tagger identity: `AQ-OS Evidence Reconciler <aqos-evidence@localhost>`;
- tagger timestamp: `1785133886 +0000`
  (`2026-07-27T06:31:26Z`);
- UTF-8, LF line endings, one blank line between tag header and message, and one
  terminal LF;
- no signature, optional header, comment, or additional field.

The exact integration tag object is:

```text
object c9fe3974753395ed343fed9e922c9c2ea695129b
type commit
tag aqos-evidence/c0.3/integration-c9fe3974
tagger AQ-OS Evidence Reconciler <aqos-evidence@localhost> 1785133886 +0000

AQ-OS C0.3 accepted integration evidence
evidence_schema: aqos.provenance-evidence-tag.v1
authorization_id: auth-c0.3-20260711
idempotency_key: 301f96f5-8882-4214-9308-f11ce5213a54
target_commit: c9fe3974753395ed343fed9e922c9c2ea695129b
target_parent: b62dde1027ab8cb61c714dcec8d8cfdc6e333df1
target_tree: 17a36cf37ae651925bfae770094b9483185e2790
acceptance_candidate_sha256: 7b00447bf9ab0a40808c78da4b002f6c84549c33185cf5ae3f9c23510191708b
acceptance_artifact_sha256: 1a76220dc3dbd0920d7f53133d3488214b8767f0f393b21b0379504d062e3644
evidence_only: true
grants_authority: false
remote_publication: NOT_AUTHORIZED
```

The exact reviewed-head tag object is:

```text
object d918a21a24081d1329efb0e85d4b3695627b4617
type commit
tag aqos-evidence/c0.3/reviewed-head-d918a21a
tagger AQ-OS Evidence Reconciler <aqos-evidence@localhost> 1785133886 +0000

AQ-OS C0.3 independently reviewed subject evidence
evidence_schema: aqos.provenance-evidence-tag.v1
authorization_id: auth-c0.3-20260711
idempotency_key: 301f96f5-8882-4214-9308-f11ce5213a54
target_commit: d918a21a24081d1329efb0e85d4b3695627b4617
target_parent: 08203901ac264d7039412064473880755576f2de
target_tree: 6ff31b8bf49a05da9852bc77097afb7d6d77a353
acceptance_candidate_sha256: 7b00447bf9ab0a40808c78da4b002f6c84549c33185cf5ae3f9c23510191708b
acceptance_artifact_sha256: 1a76220dc3dbd0920d7f53133d3488214b8767f0f393b21b0379504d062e3644
evidence_only: true
grants_authority: false
remote_publication: NOT_AUTHORIZED
```

The implementer must create these two deterministic tag objects without
installing refs, parse and validate every field, and then install both absent
refs together in one atomic `git update-ref --stdin` transaction. Each update
must use the all-zero old object ID appropriate to the repository's `sha1`
format. No single-ref installation is permitted in the normal path.

## 4. Exact two-stage ceiling

### Stage 1 — atomic evidence preservation

After the full read-only preflight passes, Stage 1 may only:

1. create the two exact annotated tag objects frozen in section 3;
2. validate both tag objects, commit objects, parents, trees, messages, types,
   sizes, and peeled targets;
3. atomically create the two exact absent refs with a two-update transaction and
   zero-old-value compare-and-swap; and
4. independently re-read the complete ref inventory and verify both tags.

Stage 1 may perform no other object, ref, reflog, configuration, index,
worktree, branch, remote, or history mutation.

### Stage 2 — additive lifecycle consumption

Stage 2 begins only after Stage 1 is exact and durable. It may only:

1. create the one tracked file
   `.agents/plans/aqos-refoundation-cycle0/C0.3-AUTHORIZATION-CONSUMPTION.md`
   with the exact closed fields required by the reviewed design;
2. stage only that path and bind independent acceptance to its exact staged-blob
   SHA-256 plus both already verified evidence refs;
3. obtain an explicit final `PASS` from a reviewer different from the
   implementer; and
4. create one atomic, non-merge commit whose sole content change is the accepted
   consumption record and whose sole parent is the exact main tip immediately
   preceding that commit.

The consumption record must set `prior_state: UNCONSUMED`,
`new_state: CONSUMED`, `consumed_by_commit:
c9fe3974753395ed343fed9e922c9c2ea695129b`, and `replay_allowed: false`;
identify both exact evidence refs and peeled targets; bind both acceptance
digests and the recorded independent `PASS`; record implementation and review
identities, dates, subject hashes, and validation evidence; and explicitly deny
C0.3/Cycle-0 ratification, authority adjudication, runtime authority, migration,
cutover, remote publication, or later-slice authorization.

Historical authorization, incident, recovery, report, candidate, and acceptance
artifacts remain byte-identical. The new additive record supersedes their
publication-time `UNCONSUMED` state only for current lifecycle queries.

## 5. Read-only preflight gate

Before creating a tag object, changing a ref, writing the consumption record, or
staging anything, the activated implementer must capture and validate every
read-only preflight command and expected result in the reviewed design section
6.1. At minimum it must prove:

- exact HEAD, object format, shallow state, root, and shallow boundary;
- both protected commits remain present, exact, and non-ancestors of HEAD;
- exact commit/tree types, sizes, parents, trees, and content SHA-256 values;
- both evidence refs are absent;
- `refs/replace/*`, grafts, object alternates, and relevant Git override
  environment variables are absent;
- acceptance candidate and artifact bytes still match;
- no other active lane owns either ref or the consumption-record path; and
- no index or worktree mutation is attributable to this slice before Stage 2.

Any mismatch stops before mutation.

## 6. Activation, identity, and review

Activation is valid only when the owner states, in one unambiguous instruction:

- this document's exact SHA-256;
- one named implementer;
- an activation start and expiry no more than 24 hours apart;
- unchanged exact HEAD, design hash, object/tag/ref metadata, two-stage ceiling,
  tracked-file inventory, stop conditions, and exclusions.

The activation is single-use and is consumed by the first Stage-1 object write,
whether the attempt succeeds, fails, is interrupted, or is abandoned. It may
not be replayed, transferred, self-amended, or inferred from any prior C0.3
authorization.

The implementer cannot accept its own work. A different named independent
reviewer must remain read-only, repeat the post-ref and staged-record checks, and
bind its explicit final `PASS` to the exact tag objects, refs, targets, staged
blob, and validation evidence. Unavailable, recused, timed-out, abstaining, or
non-PASS review cannot authorize Stage-2 commit.

## 7. Monotonic recovery

Recovery may only move toward preserving more exact evidence and publishing the
accepted additive record:

- deterministic tag objects with no refs: preserve and reverify exact bytes;
  the initial activation is already consumed, so a newly reviewed recovery
  authorization is required before retrying the atomic two-ref zero-old
  transaction;
- one exact authorized ref from an interrupted/non-atomic external state:
  preserve it; stop normal execution; a newly reviewed recovery authorization
  may create only the absent exact ref after proving both tag objects;
- both exact refs with no committed record: preserve both; a newly reviewed
  recovery authorization may resume only record acceptance and commit;
- committed exact consumption record: terminal `CONSUMED`; never reset, delete,
  replace, rewrite, or replay;
- any conflicting object, ref, tag message, peeled target, staged blob, or
  record: `CONFLICT`; stop without cleanup mutation.

Correct preservation refs are never deleted or retargeted as rollback. Factual
corrections use a new additive superseding record under new authority.

## 8. Mandatory stop conditions

Stop without mutation, or at the nearest monotonic recovery boundary, if:

1. HEAD, root, topology, object format, shallow boundary, design hash, accepted
   digests, commit metadata, tree metadata, or content digest differs;
2. either protected object is absent, corrupt, or unexpectedly becomes an
   ancestor of `main`;
3. either evidence ref is pre-existing except as an exact independently
   reviewed recovery state;
4. a tag object, message, tagger field, type, or peeled target differs;
5. replacement refs, grafts, alternates, Git override environment, or object
   ambiguity exists;
6. another lane owns or changes either ref or the consumption-record path;
7. any action requires force, deletion, retargeting, history rewrite, branch
   movement, a second tracked path, or a non-atomic ref update;
8. any required validation, exact inventory check, or independent review fails;
9. activation identity/window is absent, expired, ambiguous, exceeds 24 hours,
   or names another implementer; or
10. the record would imply ratification, authority convergence, runtime
    permission, migration, cutover, remote publication, or downstream authority.

The implementer must report the exact observed state and make no unauthorised
cleanup or compensating mutation.

## 9. Explicit prohibitions

This PREPARED_ONLY document authorizes no action now. Even after exact owner
activation it never authorizes:

- force, ref deletion, retargeting, branch movement, reset, merge, rebase,
  cherry-pick, graft, replace ref, commit-tree rewrite, or history rewrite;
- fetch, push, remote inspection, network access, or external publication;
- garbage collection, prune, repack, reflog expiry, or deletion of unreachable
  objects;
- modification of Git configuration, remotes, hooks, alternates, shallow
  metadata, or object format;
- modification of any historical C0.3 artifact or accepted implementation path;
- more than the two exact tag objects, two exact refs, one exact tracked record,
  and one exact atomic consumption-record commit;
- deployment, Nix/systemd/service/process action, database access, runtime hook,
  traffic, cutover, authority-row adjudication, C0.3/Cycle-0 ratification, or
  Foundation/later-slice implementation.

No Stage-2 staging may occur before Stage-1 verification. No Stage-2 commit may
occur before independent exact-byte acceptance of the sole staged record.

## 10. Next gate

An independent reviewer must issue a read-only exact-subject authorization
review of this document and the reviewed design. Only that final `PASS` permits
the owner to activate the resulting exact authorization hash for one named
implementer and a bounded window of no more than 24 hours.
