# Unified Program Round Aggregate

**Round:** `unified-program`  
**Subject commit:** `aa2e4452`  
**Aggregated:** 2026-07-15 by Codex in orchestrator role  
**Evidence state:** four lane files collected; legacy contribution envelopes are not producer-bound  
**Authority:** narrative review projection only; this file does not authorize implementation

## Verdict

`REQUEST_REVISION`. The Codex–Fable synthesis is accepted as the parent architecture. The Unified
Program Plan, Verified Factory PRD, and Check Kernel PRD require bounded contract corrections before
Track V or Check Kernel activation.

| Subject | Codex | Antigravity | Claude/Fable | Local | Aggregate |
|---|---|---|---|---|---|
| Unified Program Plan | REQUEST_REVISION | APPROVE WITH REVISIONS | RECUSED (author) | ABSTAIN/incomplete | REQUEST_REVISION |
| Verified Factory | REQUEST_REVISION | REQUEST_REVISION | RECUSED (author) | ABSTAIN/incomplete | REQUEST_REVISION |
| Check Kernel | REQUEST_REVISION | REQUEST_REVISION | RECUSED (author) | ABSTAIN/incomplete | REQUEST_REVISION |
| Codex–Fable synthesis | APPROVE | APPROVE | APPROVE | ABSTAIN/incomplete | APPROVE |

The local lane produced an incomplete reasoning fragment and is retained as evidence, not counted as
a substantive vote. Claude/Fable's recusal from its own subjects is valid reviewer separation.

## Required Revisions

### Unified Program Plan

1. Correct the top-level Track V index from `VF-1..VF-8` to `VF-1..VF-9`.
2. Resolve the B2/Q2 sequence: ratify the authority hypothesis and exact first shadow vertical before
   evidence collection; evidence decides expansion or replacement, not retroactive writer authority.
3. Define whether “L2B close” means L2B-A or L2B-A plus L2B-B. VF-1 must wait for L2B-B unless an
   exact hash-bound, surface-disjoint, warn-only authorization proves non-overlap.
4. Distinguish warn-only learning/reporting slices from authoritative acceptance enforcement.
5. Bind Check Kernel migration to the post-L2B registry snapshot rather than a floating check count.

### Verified Factory

1. Define the existing state-spine authority, versioned acceptance-record schema, legal transitions,
   verifier identity, CAS/replay rules, recovery owner, and rollback before VF-3 can be authoritative.
2. Specify VF-1 sealed execution as argv, working directory, environment allowlist, path containment,
   network profile, output cap, timeout/kill semantics, and metacharacter policy.
3. Add frozen warn/dry-run fixtures proving deterministic classification, zero side effects, refusal
   reasons, waiver handling, cleanup, and dashboard telemetry before promotion.
4. Split readable task/oracle material from sealed answers and canaries.
5. Promote VF-3 immediately after VF-1/VF-7 only if its authority contract is ratified; otherwise keep
   it report-only. Phasing urgency cannot manufacture writer authority.
6. Any interim VF-5 routing mutation requires a named authority, owner gate, evidence record,
   rollback, expiry, and an explicit ban on model self-promotion.

### Check Kernel

1. Define closed, versioned `ck.finding.v1` and run-result envelope schemas.
2. Land a minimal `aq check` compatibility runner/normalizer before registry migration. It may delegate
   legacy execution to focused-CI while preserving exact selection, argv, timeouts, and diagnostics.
3. Preserve legacy fields until parity is proven; do not overload legacy `tier`. Introduce separate
   `check_class` and `risk_tier` concepts.
4. Migrate the registry in bounded batches against a frozen post-L2B snapshot, with legacy/canonical
   selection and result parity vectors for every batch.
5. Define generated Python/Bash Phase-0 projection ownership, stable-ID rules, regeneration, and drift
   checks before claiming generated registrations.
6. Treat the registry/runner/tier0 mutation as T2. Use module-level ratchets where stable ownership
   exists, directory fallback otherwise. Use Biome first for the current vanilla dashboard.

## Owner Decisions

| Decision | Aggregate disposition |
|---|---|
| Q1 | APPROVE synthesis as parent architecture; corrected Unified Plan remains its projection. |
| Q2 | REVISE wording: select and ratify the first shadow authority vertical before evidence writes. |
| Q3 | APPROVE zero-trust direction; detailed network profiles require their own threat pass. |
| Q4 | APPROVE model-neutral versioned canon behavior policy. |
| Q5 | APPROVE measured, scoped, expiring lane eligibility with independent promotion. |
| Q6 | APPROVE retaining `local-orchestrator` until a named kernel revision. |
| Q7 | APPROVE eval-gated promotion with scorer/dataset lineage and rollback. |
| Q8 | REQUIRED owner session: adjudicate all ten Cycle-0 authority rows before B2. |
| Q9 | REVISE: Track V activation follows the contract amendments and exact slice authorizations. |

Owner endorsements remain direction, not implementation authorization. Q8 cannot be delegated.

## Amendments A1–A6

- **A1 — REVISE:** move stacking work into Products D/E, but measure the 27 GB RAM/VRAM/latency
  envelope before Q10 assumes one quantization step funds both a resident model and speculation.
- **A2 — APPROVE:** re-verify F2.5 live wiring and pull forward only the proven missing scheduler path.
- **A3 — REVISE:** add routing taxonomy and the owner-gated, expiring manual mutation contract.
- **A4 — APPROVE:** add atomizer/context-curator capabilities and a deterministic evidence clerk only
  with typed, measurable handoffs; do not create elevated permanent roles by naming them.
- **A5 — REVISE:** define pass independence, typed outputs, deduplication, stop conditions, and an
  overhead budget before requiring six T2 passes.
- **A6 — APPROVE:** name the Product-E consumer, privacy/provenance controls, dedupe, and promotion
  criterion for captured failures.

## Sequence From This Aggregate

1. Finish independent L2B-A acceptance and atomic integration; do not mix these planning artifacts
   into that commit.
2. Amend the three rejected source documents and the synthesis's non-blocking Postgres hypothesis
   wording; update Q10 and rebind subject hashes.
3. Run a bounded revision-review round over changed sections, using evidence-bound contribution
   envelopes where available. Legacy prose cannot create typed authorization.
4. Hold the Q8 owner adjudication session.
5. Prepare exact, PREPARED_ONLY authorizations. No VF, CK, M1–M3, or inference R1 work is authorized by
   this aggregate.

`VERDICT overall: REQUEST_REVISION — parent architecture approved; execution documents require the
listed contract amendments and fresh authorization.`
