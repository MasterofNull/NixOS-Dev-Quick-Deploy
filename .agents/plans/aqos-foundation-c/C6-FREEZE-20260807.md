---
title: "Foundation C — C6 FREEZE (epoch authority + scheduler revocation gate)"
slice: "C6 (main)"
kind: "FREEZE (hash-bound; build gated on owner activation)"
date: "2026-08-07"
frozen_design_sha256: "6c719a615f66758fdb35ffc72fad414cde27215578565b4be9c411caf44a977e"
build_head: "1a555b384828e51d469751d89a25b1acd7d21b52"
---

# C6 — FREEZE

Frozen at design **rev3** (SHA-256 `6c719a61…977e`), binding re-review PASS. Build gated on a single-use
OWNER build-activation grant (below). Drift of the frozen design bytes or any anchored baseline hash ⇒
re-freeze.

## Review chain (independent; Rule 18)
- rev1 codex depth-review (R1–R6, binding history) → rev2 (answers R1–R6) → **rev3** (re-anchored after
  C2-SCI + C6-P0 landed; both blockers resolved).
- **rev3 binding re-review = PASS** (fresh Claude flagship, Codex-substitute; `C6-REV3-BINDING-REVIEW-20260807.md`):
  all 12 §1 hashes match the working tree exactly (zero churn); Q-C6-1 (built C2-SCI issuer + dispatch
  ingress) and Q-C6-2 (built C6-P0 anchors) genuinely resolved against the code; core epoch-authority +
  scheduler-gate design sound; no fail-open / auto-reissue / byte-parity-break.

## Anchored baseline (verify at build; drift ⇒ re-freeze) — the rev3 §1 table
UNCHANGED (C6 edits/consumes): `slot_queue.py` `e4e7e9b1…` (EDIT target — the fence), `slot_scheduler.py`
`ea3b5b9a…`, `phase0.py` `5e6f2208…`, `aq-event` `5deba81b…`, `delegate-to-local` `b5d2c5cb…`.
RE-ANCHORED (verify current): `capability_lease.py` `611f1af8…`, `capability_lease_gate.py` `970d10f7…`,
`dispatch.py` `77ba0c25…`, `env-contract.yaml` `74c10eb2…`, `aistack.py` `aa855d61…`, `dashboard.js`
`9c892841…`, `default.nix` `f772a1eb…`.
VERIFY-ONLY (built by C6-P0 / C2-SCI — do NOT re-create): `config/aqos/c6-owner-public-keys.json`,
`config/schemas/revocation-epoch-bump.schema.json`, `config/schemas/scheduler-lease-context.schema.json`,
`config/schemas/scheduler-lease-gate-decision.schema.json`, `scripts/ai/lib/scheduler_context_issuer.py`
(+ transport + service), `dispatch.py::verify_ingress_scheduler_context`.

## C6-main NEW files + the one EDIT (still-absent at freeze; the build creates them, default-OFF)
- `scripts/ai/lib/revocation_epoch.py` — durable authenticated epoch read/bump primitive (fail-closed;
  no bootstrap-to-zero / env fallback / direct file write / auto-reissue).
- `scripts/ai/aq-epoch-bump` — the owner-facing bump CLI (presents a signed `aq.revocation-epoch-bump/1`).
- `nix/modules/services/revocation-epoch-authority.nix` — default-OFF confined `aq-revocation-epoch-authority`
  (dedicated user, SOPS-free owner-verify allowlist, UDS 0660 + SO_PEERCRED defense-in-depth, StateDirectory
  epoch + replay ledger + audit receipts). Mirror the ALA/C2-SCI confinement + client-access pattern.
- EDIT `scripts/ai/lib/slot_queue.py` — before `acquire` mutates queue state, verify the immutable signed
  `aq.scheduler-lease-context/1` (via the built `verify_ingress_scheduler_context`), fence on a durable
  reservation digest, drop queued/held reservations on an epoch bump. Flag `CAPABILITY_SCHEDULER_LEASE_GATE`
  (default 0, byte-parity off — INDEPENDENT of `CAPABILITY_SCHEDULER_CONTEXT_ISSUER`).
- Tests: `test-revocation-epoch.py`, `test-scheduler-lease-gate.py`, `test-c6-service-coverage.py` +
  `fixtures/revocation-epoch-golden.json`; phase0 registration; dashboard C6 card.

## Watch items from the re-review (fold into the build + its code review)
- **A2 (HIGH for the build's code review):** the scheduler-gate/ingress wiring MUST feed the CURRENT epoch
  from `resolve_current_epoch()` into `verify_ingress_scheduler_context` — never rely on its `current_epoch=0`
  default (0 would be a fail-open revocation surface). The build's independent code review MUST confirm this.
- **A1 (activation):** `config/aqos/c6-owner-public-keys.json` is a placeholder all-zeros key (fails closed).
  Provision the real owner public key at activation before any epoch bump.

## Build decomposition (recommended; each default-OFF, orchestrator-verified + independently reviewed)
- **C6-B1**: `revocation_epoch.py` + `aq-epoch-bump` + `test-revocation-epoch.py` (the epoch primitive + CLI).
- **C6-B2**: `revocation-epoch-authority.nix` confined service + client-access (ALA lesson).
- **C6-B3**: `slot_queue.py` fence EDIT + wire `verify_ingress_scheduler_context` (A2!) + `test-scheduler-lease-gate.py`;
  flag-gated, byte-parity-OFF (prove the F2.5 slot_queue suite unchanged when off).
- **C6-B4**: Service Coverage + dashboard + phase0.

## Owner build-activation grant (single-use; OWNER emits — orchestrator MUST NOT self-emit)
```
scripts/ai/aq-event emit --agent owner --type activation.grant \
  --subject c6-epoch-authority-scheduler-gate-build \
  --payload '{"idempotency_key":"c6-epoch-authority-scheduler-gate-build-20260807","subject_design_sha256":"6c719a615f66758fdb35ffc72fad414cde27215578565b4be9c411caf44a977e","build_head":"1a555b384828e51d469751d89a25b1acd7d21b52","predecessors":["c2-scheduler-context-issuer-build","c6-p0-trust-anchors","ala-rev4-minter-activated"],"implementer":"sonnet","window_hours":48}'
```
Hash-bound single-use owner act, NOT standing authority. After build → owner flag/enable + A1 key
provisioning → C6 activation → **C4** (network confinement, the last Foundation-C cycle).

`RECORD: FROZEN. No implementation, service enablement, epoch bump, flag flip, or activation until the owner grant is emitted.`
