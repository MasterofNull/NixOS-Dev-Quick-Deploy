# R7 Provisioning — FREEZE record (rev2)

status: FROZEN 2026-08-03 — design bytes locked; build NOT yet authorized
tier: enforcement

## Frozen subject
- Design: `.agents/plans/aqos-foundation-c/R7-PROVISIONING-DESIGN-20260803.md`
  sha256 `a009e66f92880b41f4eb740372d37f514da288c669cb1fe07bc6df24d8e6b8b5`

## Predecessor anchors (chain of custody)
- runner-hardening rev4 accepted + integrated (`0cf1192e`); R6 deploy-exercise PASS (bugs #2/#5/#6/#7/#8, validated live)
- R2 clone primitive (`execution_cell_clone.py`, R2_REVIEWED_PASS); R1 grant; R5 adapter
- base HEAD at design: `a439527f`

## Ceiling (locked — the build may do EXACTLY this)
1. EDIT `nix/modules/services/execution-cell-runner.nix` — `mirrorPath` option, the bare-mirror
   provisioning oneshot/timer + tmpfiles, and `AQ_EXECUTION_CELL_RUNNER_TRUSTED_REPO_MIRRORS` as
   **JSON** (`builtins.toJSON { primary = mirrorPath; }`). switchboard.nix UNTOUCHED (anchor).
2. EDIT `ai-stack/switchboard/execution_cell_runner.py` `build_config_from_env` — swap BOTH
   `reservation_set` + `cell_reservation_set` for durable constructors (separate files). No change to
   verify / clone / cell / validator / SO_PEERCRED logic.
3. NEW durable reservation store (built on R2 `_fsync_write_json` + `threading.Lock`) + offline test
   incl. concurrent-`try_reserve`.
4. NEW live GREEN deploy-exercise acceptance (post-rebuild, adapter ON): fresh unique effect →
   mint→sign→UDS→accept→verify→reserve→repo-trust PASS→bwrap cell→validator→typed GREEN receipt; real
   tool-calling unchanged; C2/C5 unaffected; replay survives restart; stale mirror → `base-oid-unreachable`.

## Review
- **Binding-substitute (fresh Claude flagship, codex offline) — PASS** on rev2: all 7 rev1 findings
  resolved, verified line-by-line + code-grounded, no new issues. Rule 18 permits a fresh flagship as
  binding acceptance. Local-Qwen FAIL was an ungrounded false-negative (content is OID-bound); Antigravity
  did not complete (remote_key_endpoint_mismatch dead-end).
- **Codex confirmatory** re-review queued for its return — advisory catch-up (Rule 18), NON-blocking.

## Build gate (required, ordered)
1. **single-use owner activation** hash-bound to subject `a009e66f92880b41…` (the owner's act — freeze authorizes nothing).
2. build within the ceiling → independent acceptance → orchestrator integration (small-batched commits).
3. owner R6-style adapter re-activation (CAPABILITY_CELL_ADAPTER=1) + the live GREEN deploy-exercise.

The freeze locks the bytes; it authorizes no build, activation, or commit of implementation.
