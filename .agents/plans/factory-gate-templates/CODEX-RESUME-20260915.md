# Claude factory-template operations resumed by Codex

Owner requested resumption and use of available agent strengths.

- FT-1 existing implementer remains owner of `templates/factory-gate-bundle/`.
  Full task ID: `codex-20260915-095542-xi4avpxxxxxx`, PID 193387. Claude's
  abbreviated ID omitted the suffix; CLI works with the full ID. No duplicate
  writer was started and root has not edited this bundle or the shared index.
- Independent bounded acceptance routed to a fresh non-author reviewer.
- Local FT-2 command-profile prep queued read-only/direct:
  `local-20260915-102237-47afub` (512 output-token budget, 900s timeout).
  DONE, partial at token bound; its output is proposal evidence, not verified execution.
- Antigravity advisory queued via IDE inbox; quota/old undrained claims are
  unavailable-lane evidence, not acceptance or a blocking roster requirement.
- FT-3/4 inventory found clobber-prone legacy `install_git_secret_hooks` and
  scaffold writers in `scripts/ai/aqd`; future installer must compose safely,
  not blindly call those during brownfield retrofit.
- Previous crash SR-3 candidate was stashed by Claude (`stash@{0}` at handoff
  history; verify exact stash identity before applying). Do not pop/reintroduce
  it into the FT-1 slice. F2 remains committed `7547e8c4`.
  Verified stash object: `4ad339993fc8ce52f67b70afd617aaf69151fd9a`.
- Preserve unrelated dirty files and existing ahead-of-origin commits.

Next: collect terminal FT-1 evidence + one exact-subject acceptance, run Tier-0
on the isolated staged slice, record truthful disposition and commit if eligible.
Then FT-2 is the next implementation slice. HIGH/CORE live enforcement activation
still requires its explicitly scoped owner-confirmation contract; a general
gate-lift is not permission to bypass security controls.
