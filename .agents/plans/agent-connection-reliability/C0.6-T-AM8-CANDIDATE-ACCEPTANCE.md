# C0.6-T AM8 Candidate Acceptance

**Reviewer:** `codex-subagent-track-v-corrections` — independent flagship lane  
**Verdict:** REQUEST_REVISION  
**Review scope:** exact nine-file AM8 candidate; offline focused evidence only

## Verified

- All nine final hashes matched the submitted candidate.
- Both frozen schema paths remained byte-identical.
- Projection tests passed 122/122; web/backend tests passed 21/21.
- Registry shape, frontend composition, terminal chronology, Phase-0 port
  resolution, privacy/cardinality, and Service Coverage closures were present.
- Reliability remained truthfully 13/16 solely because external
  `task_registry.py` manifest drift affects checks 02/15/16; no repin occurred.

## Blocking findings

1. Age tests do not exercise the required 10/10+/60/60+ matrix across
   refresh-success, refresh-failure fallback, shared-waiter, and
   cancellation-fallback return paths.
2. Lifecycle tests do not exercise immediately-before/at/after each
   spawn/read/cap/terminate/kill/reap boundary or one end-to-end total-elapsed
   vector proving a single absolute deadline across phases.

The correction may remain test-only if the direct vectors prove current code.
Any code defect or need outside the two existing test paths requires a new
bounded revision. AM8 is consumed and rejected; no staging, commit, Tier0,
aq-qa, live, provider, network, service, Nix, or deployment action is accepted.

