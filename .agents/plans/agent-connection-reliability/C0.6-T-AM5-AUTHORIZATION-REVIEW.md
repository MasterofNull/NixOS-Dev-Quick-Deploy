# Independent Authorization Review — C0.6-T Telemetry AM5

Review date: 2026-07-22  
Reviewer: `codex-subagent-c06-t-am5-authorization-reviewer`  
Role: independent architecture, privacy/security, SRE, dashboard, and authorization reviewer  
Review mode: exact-subject read-only governance review; no candidate or live authority

## Exact subjects and frozen pre-state

- Correction packet: `C0.6-T-AM5-CANDIDATE-REVISION.md`
  - SHA-256: `92d4e33ff199537feaa7a278b4bad388f976917b36395c44c6ab9f4364c09312`
- PREPARED_ONLY authorization: `C0.6-T-IMPLEMENTATION-AUTHORIZATION-AM5.md`
  - SHA-256: `42dd0e1a646f3c7416cc01bf1fedcc4ecfb1189807a24056d545c16595124b09`
- Exact HEAD: `2ddc2671da3ce15db8f2368901cd226dd12279ef`
- Design: `C0.6-T-TELEMETRY-DESIGN-PACKET.md`
  - SHA-256: `50ea6fcb7584e26b2f5774571692db3acd5403275b11000f2f1d8bf5e5130aff`
- Binding AM3 rejection: `C0.6-T-AM3-CANDIDATE-ACCEPTANCE.md`
  - SHA-256: `3d9e6f638efadacd7725b5dd6b908b5b3d7b520f5b3cc85708e1c6f235f1daf7`
- Binding AM4 review: `C0.6-T-AM4-AUTHORIZATION-REVIEW.md`
  - SHA-256: `ad0780c10ab92acd2bb62bece085b8467949f92384d3fa795432660c132fbd90`

All bindings and HEAD match. The nine rejected-candidate paths are regular, non-symlink, unstaged,
and match the required all-MODIFY predecessor hashes exactly:

`4bf21532bfb6fcf2fd64249e8948c8c0f083bcad8f800b543d39097461e6f9d8`,
`7fd3c38118aabce13dcaa5f3b4f04fa96b07b3a3e857e045ade3ba5899511cf0`,
`9685542c3686d2f9980cbfdaf12256971bab10ffe7b0333847a0bed5bf88ebad`,
`52fb75c41f1c32e6d5b933dac58f7769bfcc901b9c1fe7da44be7d0721ec17ad`,
`e1c3cd2d0f5ae5a2281954ee1f6556b87d9a168c160b35701ecdf9926f9b9250`,
`a7886699ed75ff0b265830b14bab8f291bb321b461121fef6ffc2137ea66c544`,
`f39021c080a2742e8841972cf9d88e0333cb200ee4b828c472ce0f8e2244447b`,
`9e1be4a685aacea459c9973b9da3e9e05f6dc158517a499ccfb41c2f0fcecb7c`, and
`d7bd095f68aeab2945ccb06160191f0a4b836b0dfb42a62333ce5fcb33f78979`.

The owner activation and candidate-complete events prove AM3 crossed first write; its binding
`REQUEST_REVISION` makes AM3 consumed and non-replayable. AM4 remained PREPARED_ONLY, reached no
first write, and its exact-subject review requested revision. AM5 correctly supersedes AM4 and makes
it non-activatable without misdescribing it as consumed.

## Evidence-grounded producer-trust adjudication

The AM5 conclusion is supported by the current repository bytes:

- The closed `$id=aq.delegation-task-record.v1` initial-record schema contains no `local_direct`,
  lifecycle-provenance envelope, `trusted`, or observation-producer field. Its
  `admission_producer="dispatcher"` constant attests only dispatcher admission of that record.
- `lane=local` selects a route; it does not authenticate the producer of later telemetry.
  `record_revision` supplies CAS ordering; it does not authenticate an observation producer.
- `TaskRegistry._m2a_read_records()` bounds inode type, total/record size, framing, JSON decoding,
  and object shape, but does not validate arbitrary record keys against a persisted-record schema.
  Nested `trusted`/`producer`, a copied admission string, an unkeyed digest, or a self-described
  writer identity therefore cannot acquire authority merely by appearing in that file.
- `begin()`, `attach_process()`, and `transition_m2a()` own admission/process/state mutation, but no
  current named writer method writes an authenticated local-agent, switchboard, or trusted-observer
  lifecycle fact. There is consequently no honest positive trusted-producer mapping in this slice.

The frozen current behavior follows from those facts and is deterministic. A valid v1 admission or
writer-transition record without attempted lifecycle telemetry contributes no lifecycle fact and
projects typed `not_assessed` / `source_not_instrumented` with null lifecycle numbers. A current row
that attempts `local_direct` or another extra provenance/telemetry extension is outside the closed
authority contract; the entire local-direct source is rejected as `not_assessed`, `blocked`, and
unavailable, with null lifecycle numbers. Mixed valid rows cannot launder an unbound telemetry row.
Legacy data without attempted telemetry remains uninstrumented rather than falsely blocked.

This is the only evidence-grounded AM5 policy: all current local-direct evidence remains untrusted,
and no mapping to local-agent, switchboard, or trusted-observer exists until a separately authorized
closed writer/schema amendment lands. The AM5 TUI adapter must therefore construct no trusted
runtime local-direct facts from current registry rows. This precisely closes AM4's first blocker
without expanding the implementation inventory or simulating future authority.

## Exact QA evidence and incorporated corrections

AM5 freezes the two previously missing immutable literals exactly:

- `0.10.42`: `local-direct lifecycle route contract`
- `0.10.43`: `local-direct Agent Ops card visibility`

Each result is the exact four-key ordinary layer-5 CheckResult, with a closed status and an empty
reason so serialization cannot append variable text. The descriptions are invariant across status,
host availability, error, and reason. Exact-literal negative vectors are mandatory. This closes
AM4's second blocker.

The correction packet also carries forward every AM4 requirement that closed the binding AM3
findings: schema-valid 10/60-second cache fallback; standalone/embedded/projector/TUI/backend/final
response parity; combined max-plus-one child I/O and bounded cleanup/reaping; exact immutable QA
shape and inclusive evidence age; offline suites before host-only Phase-0 skip; full semantic GET
validation; coordinator-independent local rendering and worst severity; deadline equality, terminal
window/order/dedup and no false timeout; full adversarial/privacy vectors; typed no-data; aggregate-
only output; and sole-reader/no-mutation monitoring. Nothing reopens a passed criterion.

## Scope, activation, and stop controls

AM5 remains an exact nine-file all-MODIFY correction. It adds no writer/schema amendment, tenth
implementation path, live route, service, timer, HTML sink, provider/model action, network request,
database/store, Nix/systemd/AppArmor, deployment, traffic, or lifecycle authority.

The authorization is PREPARED_ONLY and single-use after an explicit hash-bound owner activation.
Activation must name the exact packet and authorization hashes, one eligible implementer, a window
of no more than 24 hours, exact HEAD, all nine predecessor hashes, and prove every path unstaged,
regular/non-symlink, and free of an active or ambiguous overlapping writer. First candidate write
consumes AM5. Drift, mismatch, overlap, interruption, failed precondition, or further correction
makes it non-replayable and requires a fresh authorization.

Implementation validation is limited to the listed injected/offline commands. `aq-qa`, Tier0, live
registry/dashboard reads, provider/network/local inference, acceptance, staging, commit, push,
rollback, deletion, runtime activation, and deployment remain prohibited. Independent candidate
acceptance is still required and cannot be self-issued by the implementer.

No candidate edit, implementation validation, live action, staging, or commit occurred in this
review.

VERDICT: PASS — AM5 evidence-groundedly keeps all current local-direct telemetry untrusted and not instrumented, rejects attempted unbound telemetry source-wide, freezes both exact CheckResult descriptions, preserves every AM4 correction, and is ready only for explicit hash-bound owner activation
