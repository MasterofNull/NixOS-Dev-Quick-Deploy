# C0.6-T — Telemetry Implementation Authorization AM5

Authorization ID: `auth-agent-connection-reliability-c0.6-t-am5-20260722`  
Idempotency key: `agent-connection-reliability:c0.6-t:telemetry:am5:20260722`  
Status: **PREPARED_ONLY — IMPLEMENTATION NOT AUTHORIZED**  
Exact base HEAD: `2ddc2671da3ce15db8f2368901cd226dd12279ef`  
Design SHA-256: `50ea6fcb7584e26b2f5774571692db3acd5403275b11000f2f1d8bf5e5130aff`  
AM5 correction SHA-256: `92d4e33ff199537feaa7a278b4bad388f976917b36395c44c6ab9f4364c09312`  
AM4 review SHA-256: `ad0780c10ab92acd2bb62bece085b8467949f92384d3fa795432660c132fbd90`  
Binding AM3 rejection SHA-256: `3d9e6f638efadacd7725b5dd6b908b5b3d7b520f5b3cc85708e1c6f235f1daf7`

## 1. Lineage and activation

AM3 is consumed and non-replayable. AM4 was never activated or consumed, but its exact-subject
review requested revision; AM4 is superseded and non-activatable. AM5 incorporates all unchanged AM4
requirements and adds only the exact current-v1 provenance disposition and two frozen description
literals in `C0.6-T-AM5-CANDIDATE-REVISION.md`.

This record grants no action while PREPARED_ONLY. Activation requires a fresh independent exact-
subject `PASS`, followed by an explicit owner/orchestrator action naming this authorization's final
SHA-256, the AM5 correction hash, one eligible implementer, a window no longer than 24 hours, exact
HEAD, all nine predecessor hashes, and no staged/symlink/overlapping-writer condition. A content
change invalidates review. Standing direction and all prior activations grant nothing to AM5.

## 2. Exact nine-file all-MODIFY ceiling

| # | Path | Required predecessor SHA-256 |
|---:|---|---|
| 1 | `scripts/ai/lib/agent_ops_projection.py` | `4bf21532bfb6fcf2fd64249e8948c8c0f083bcad8f800b543d39097461e6f9d8` |
| 2 | `config/schemas/agent-ops-projection.schema.json` | `7fd3c38118aabce13dcaa5f3b4f04fa96b07b3a3e857e045ade3ba5899511cf0` |
| 3 | `scripts/ai/aq-tui-dashboard` | `9685542c3686d2f9980cbfdaf12256971bab10ffe7b0333847a0bed5bf88ebad` |
| 4 | `dashboard/backend/api/routes/aistack.py` | `52fb75c41f1c32e6d5b933dac58f7769bfcc901b9c1fe7da44be7d0721ec17ad` |
| 5 | `assets/dashboard.js` | `e1c3cd2d0f5ae5a2281954ee1f6556b87d9a168c160b35701ecdf9926f9b9250` |
| 6 | `scripts/testing/test-agent-ops-projection.py` | `a7886699ed75ff0b265830b14bab8f291bb321b461121fef6ffc2137ea66c544` |
| 7 | `scripts/testing/harness_qa/phases/phase0.py` | `f39021c080a2742e8841972cf9d88e0333cb200ee4b828c472ce0f8e2244447b` |
| 8 | `config/schemas/agent-ops-local-direct-health.schema.json` | `9e1be4a685aacea459c9973b9da3e9e05f6dc158517a499ccfb41c2f0fcecb7c` |
| 9 | `scripts/testing/test-agent-ops-local-direct-health-web.py` | `d7bd095f68aeab2945ccb06160191f0a4b836b0dfb42a62333ce5fcb33f78979` |

Before first write the implementer must verify exact HEAD, every hash, regular/non-symlink state,
none staged, and no active or ambiguous writer. A tenth implementation path, drift, mismatch,
overlap, or missing candidate is a hard stop.

## 3. Exact authorized correction

The activated implementer may apply only AM4's unchanged cache, schema parity, bounded child I/O and
reaping, exact QA evidence, Phase-0, dashboard severity, projector-boundary, adversarial-test, privacy,
and no-authority corrections, with these AM5 bindings:

1. Current `aq.delegation-task-record.v1` cannot carry authority-bound local-direct evidence. Exact
   valid v1 rows without telemetry yield typed not-assessed/not-instrumented. Any current row that
   attempts a `local_direct` or provenance extension is untrusted and yields the typed rejected-source
   projection for the whole local-direct source. No nested trust/producer claim, lane, revision,
   admission-producer string, or unkeyed digest may improve health.
2. There is presently no authority-bound mapping to local-agent, switchboard, or trusted-observer.
   Trusted telemetry remains gated on a later separately authorized TaskRegistry writer/schema
   amendment outside this ceiling. AM5 must not edit or simulate that writer.
3. Immutable results require exact layer-5 four-key CheckResults and literal descriptions:
   `0.10.42 -> local-direct lifecycle route contract` and
   `0.10.43 -> local-direct Agent Ops card visibility`. Description/reason text cannot vary by status.

All deterministic absent/extra/malformed/version/provenance/description/freshness rules and tests in
the AM5 correction specification are mandatory.

## 4. Authorized offline validation only

```text
python3 scripts/testing/test-agent-ops-projection.py
python3 scripts/testing/test-agent-ops-local-direct-health-web.py
python3 -m py_compile scripts/ai/lib/agent_ops_projection.py scripts/ai/aq-tui-dashboard \
  dashboard/backend/api/routes/aistack.py scripts/testing/test-agent-ops-projection.py \
  scripts/testing/test-agent-ops-local-direct-health-web.py \
  scripts/testing/harness_qa/phases/phase0.py
node --check assets/dashboard.js
python3 -m json.tool config/schemas/agent-ops-projection.schema.json
python3 -m json.tool config/schemas/agent-ops-local-direct-health.schema.json
git diff --check -- <exact nine paths>
sha256sum <exact nine paths>
```

Tests use only injected clocks, temporary registries/QA stores, fake subprocesses/HTTP, bounded local
process fixtures, and privacy canaries. No live registry, dashboard, network, provider, local model,
`aq-qa`, Tier0, service, or readiness action is permitted. Report exact hashes, test counts, semantic
parity vectors, child cleanup evidence, v1 provenance rejection vectors, literal-description vectors,
scope, and skipped live work. Do not stage, commit, push, self-review, or delegate.

## 5. Independent acceptance and exclusions

A different eligible reviewer must recompute all nine hashes and verify every incorporated AM4
criterion plus exact v1 schema/writer evidence, no present producer mapping, typed whole-source
rejection of attempted telemetry, no partial laundering, both exact descriptions across all statuses,
and drift-negative tests. A PASS only permits a separately authorized Tier0/live read-only acceptance
step; it grants no commit or runtime activation.

AM5 excludes every Tier0 file, wrapper, dispatcher, TaskRegistry writer/schema, registry data, slot
queue, transport, prompt/model/profile, environment/port contract, dashboard HTML/app wiring, Nix,
service, systemd, AppArmor, database/store/broker, migration, live read, provider/network/local
inference, canary, traffic, deployment, staging, commit, push, rollback, and deletion.

AM5 is single-use after activation. First candidate write consumes it. Stop, interruption, drift,
overlap, failed precondition, or any further correction makes it non-replayable and requires a fresh
authorization. Unavailable or timed-out reviewers are not approvals.

`RECORD: PREPARED_ONLY. No implementation, validation execution, acceptance, staging, commit,
provider, network, runtime, service, Nix, deployment, canary, traffic, registry-writer, schema, or
lifecycle authority is granted before independent PASS and explicit hash-bound activation.`
