# C0.6-T AM8 Candidate Revision — Flagship Closure

Status: **PREPARED_ONLY / GOVERNANCE_ONLY — NO IMPLEMENTATION AUTHORITY**

AM8 is a fresh, strictly bounded recovery packet for the rejected AM7 candidate. It authorizes no candidate edit, validation execution, staging, commit, provider/network action, service/process action, Nix, or deployment.

## 1. Binding lineage and non-replayability

- Exact base HEAD: `107f7e8ab2452b4d89ff737b28966e35bf4f9e24`
- Parent design SHA-256: `50ea6fcb7584e26b2f5774571692db3acd5403275b11000f2f1d8bf5e5130aff`
- Parent AM7 revision SHA-256: `1a22fad17c9d10f8ec375e456369a972b4cd76c742bf50631551815de3d2244b`
- Parent AM7 authorization SHA-256: `80f0010f11c79abea68f1306046da2f3d3d5599613a6287398f237d310ea06d4`
- Parent AM7 REQUEST_REVISION artifact SHA-256: `e7c75fe9e35fea203b92a3231f5be2ced54235755dddb3047f6ef23d3aa8d1e8`

AM7 is rejected and consumed; AM3, AM5, and AM6 are non-replayable, and AM4 is superseded/non-activatable. AM8 inherits neither an implementer nor an activation window.

## 2. Exact ceiling and rejected-candidate freeze

The ceiling is the same nine existing paths: no creation, deletion, rename, archive, symlink, mode change, generated repository output, or tenth path. Every hash is the rejected candidate's actual current byte hash. `FROZEN_UNCHANGED` paths are in scope for verification but must be byte-identical after an AM8 attempt.

| Path | AM8 disposition | Rejected candidate SHA-256 |
|---|---|---|
| `scripts/ai/lib/agent_ops_projection.py` | MODIFY only for AM8 corrections | `50a683dc362180cafa56b85b46ba54cc49869f48c6a3f8d74383752cbceda6c3` |
| `config/schemas/agent-ops-projection.schema.json` | FROZEN_UNCHANGED | `816661919e8fdc8b5cd5e6599930e9ad615cf7fc9622190ca1c974fabe44cbc4` |
| `scripts/ai/aq-tui-dashboard` | MODIFY only for AM8 corrections | `14b37fa3d8a220565a500cdc18870715578262f20e1f8471422f4832fb356efc` |
| `dashboard/backend/api/routes/aistack.py` | MODIFY only for AM8 corrections | `8108a4c8dd5984bf895c4dd4cdddf833ae3b3e41cbe47de50d95cd7d33e6eba4` |
| `assets/dashboard.js` | MODIFY only for AM8 corrections | `2f6c8882c646b3392bc249df38f93c7e8dffefa1cf27fd6e46d6009173d608a5` |
| `scripts/testing/test-agent-ops-projection.py` | MODIFY only for AM8 regressions | `631805ee0c2b7bf6786f6dc936398dd43cecfb12297e7212736cdef98af8e235` |
| `scripts/testing/harness_qa/phases/phase0.py` | MODIFY only to replace the two literal dashboard-port fallbacks with the approved port-SSOT resolver | `91979b59d7049f6cf1e5bc12a02115e315c33c89a1c3e09139303da30e6deee9` |
| `config/schemas/agent-ops-local-direct-health.schema.json` | FROZEN_UNCHANGED | `0b03f0a5456a8c1501feb469dbeaf8a26de69f0590b06abc4cc729234de80ef9` |
| `scripts/testing/test-agent-ops-local-direct-health-web.py` | MODIFY only for AM8 regressions | `c9bd0c19e88372b277549db4ce9fa1b27096ca9cb0366b8565d10d32705a0a74` |

HEAD/hash/file identity/writer overlap/operation/mode drift is a hard stop.

## 3. Mandatory flagship closures and direct regressions

1. **Frontend composition:** coordinator is available only for a non-array object whose explicit `available` is not `false`. Missing, null, malformed, arrays, and the exact backend fallback `available:false` are unavailable. Local-direct retrieval/rendering remains independent. Test the complete coordinator-fallback × local-direct-state and severity cross-products; worst severity is deterministic and never improves either source.
2. **Effective age:** all fast-path, refresh-success, refresh-failure fallback, shared-waiter, and cancellation-fallback returns use one pure copied effective-age projection followed by schema/semantic validation. Store accepted bytes, `source_age_s`, and injected monotonic acceptance time. Deterministically test 10, 10+, 60, and 60+ seconds on normal and fallback returns: <=10 preserves; >10 through 60 is at least stale/degraded with `source_stale`; >60 is typed unavailable with assessed values null. Existing worse reason/coverage/classification never improves.
3. **Closed registry:** validate each supplied row against only the closed outer `aq.delegation-task-record.v1` `new_record` keys: `record_version`, `task_id`, `lane`, `role`, `access`, `task_class`, `artifact_expectation`, `created_epoch`, `record_revision`, `admission_producer`, `status`. Enforce frozen constants/enums/bounds/task-id pattern and boolean-as-integer rejection, including unhashable-input protection. Extra keys, nested payloads, operation objects, malformed values, and mixed valid/invalid input reject whole-source as `blocked/source-invalid`; do not salvage. An exact valid v1 row remains `not-assessed/source-not-instrumented`.
4. **One lifecycle deadline:** one injected monotonic budget <=2 seconds includes spawn, stdout/stderr read, output cap, terminate, kill, and confirmed reap. Every await/blocking boundary gets remaining time only; cleanup opens no new window. Test immediately-before/at/after each boundary, cancellation/shared waiters/one refresh owner, total elapsed vectors/terminal state, and non-returning terminate/kill/wait-reap doubles. No normal or cached fallback return precedes confirmed terminal reap; unconfirmed reap propagates fixed cleanup-integrity failure.
5. **Terminal ordering:** `terminal_at_ms < phase_entered_at_ms` rejects whole-source through a reachable validator with a direct negative regression. If the dead helper cannot be made reachable without scope growth, remove it and test the surviving direct validator, recording that disposition. Preserve the future-skew upper bound.
6. **Phase-0 port:** web evidence resolves the dashboard port from the port SSOT (`nix/modules/core/options.nix` or approved resolver), never literal `8889`. Preserve exact IDs/layers/descriptions/four-key details, including `0.10.42` and `0.10.43`; only the two reviewed port-resolution call sites may change.

## 4. Preserved contracts, external drift, and next gate

Preserve recursive privacy/cardinality: no prompt, response, argv, cwd, environment, task ID, provider/exception text, registry payload, or other sensitive/high-cardinality value reaches projection/API/TUI/dashboard/QA/metrics. Preserve passive projection, closed schemas, typed vocabulary, age boundaries, monitoring, and Service Coverage: integration-path Phase-0 evidence plus visible TUI/web health surfaces are mandatory and cannot be deferred, stubbed, hidden, or weakened.

Reliability tests 02/15/16 remain external drift because their manifest does not recognize `scripts/ai/lib/task_registry.py` SHA-256 `9285658eef68eb07a29e39db9f7fd9b43c51c2c827522d168d3ec5a86aa7b284`. The registry/test/fixtures/manifest are out of ceiling: report exactly; never repin, suppress, waive, or reinterpret.

Only later offline focused tests, syntax/schema checks, hashes, inventory, and `git diff --check` can be activated. Tier0, aq-qa, live route/browser/registry/provider/network/database/service/process work, Nix, deploy, stage, and commit remain prohibited. Independent review must PASS this revision and separate AM8 authorization before one exact owner activation for one implementer with unchanged HEAD/hashes and expiry <=24 hours.

`RECORD: PREPARED_ONLY. No implementation or candidate mutation authority.`
