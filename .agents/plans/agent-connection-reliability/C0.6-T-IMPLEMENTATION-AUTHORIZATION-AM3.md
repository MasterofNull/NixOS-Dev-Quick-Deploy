# C0.6-T — Telemetry Implementation Authorization AM3

Authorization ID: `auth-agent-connection-reliability-c0.6-t-am3-20260722`
Idempotency key: `agent-connection-reliability:c0.6-t:telemetry:am3:20260722`
Status: **PREPARED_ONLY — IMPLEMENTATION NOT AUTHORIZED**
Revision: **AM3 — HEAD-only refreeze**
Base commit: `2ddc2671da3ce15db8f2368901cd226dd12279ef`
Design subject: `C0.6-T-TELEMETRY-DESIGN-PACKET.md`
Design SHA-256: `50ea6fcb7584e26b2f5774571692db3acd5403275b11000f2f1d8bf5e5130aff`
Normative predecessor authorization: `C0.6-T-IMPLEMENTATION-AUTHORIZATION.md`
Normative predecessor SHA-256: `416623c8547d52a5798444d8da03ff4a2623a77c93c44d5eecbdb9c482aa4c46`

## 1. Refreeze scope and normative incorporation

This is a governance-only HEAD refreeze. The exact AM2 authorization identified above is
incorporated by reference, byte-semantically and in full, including its activation rule, exact
nine-file ceiling, grant, aggregation semantics, privacy and authority invariants, explicit
exclusions, permitted offline evidence, independent acceptance requirements, consumption rule,
single-use rule, stop conditions, and prohibition on Tier0, staging, commit, provider, network,
runtime, service, Nix, deployment, canary, traffic, and lifecycle actions.

Only these AM2 identity fields change:

1. revision and authorization identity advance from AM2 to AM3;
2. the exact base HEAD advances from
   `c286169a90c7a22e6bd163d1bbcf3b065fed79cd` to
   `2ddc2671da3ce15db8f2368901cd226dd12279ef`; and
3. AM2 is superseded because of HEAD drift before activation, not because it was activated,
   consumed, rejected, or semantically revised.

In any ambiguity, the exact AM2 bytes control every implementation and acceptance semantic except
the three identity substitutions above. No standing direction or prior AM2 review activates AM3.
A fresh independent exact-subject review of this AM3 document is mandatory before owner activation.

## 2. Disjoint intervening commit

The only commit between the AM2 base and this AM3 base is
`2ddc2671da3ce15db8f2368901cd226dd12279ef` (`fix(governance): isolate tier0 staged validation`).
The exact diff from the AM2 base contains only:

- `.agents/plans/agent-connection-reliability/TIER0-STAGED-ISOLATION-AM3-BINDING-ACCEPTANCE.md`;
- `scripts/governance/tier0-validation-gate.sh`; and
- `scripts/testing/test-tier0-staged-isolation.sh`.

Those paths are outside the C0.6-T nine-file implementation inventory. The two Tier0 paths remain
explicitly excluded by the incorporated AM2 authorization. The commit therefore changes no C0.6-T
design subject, predecessor, absence guard, implementation path, or runtime authority.

## 3. Exact nine-file ceiling and reverified pre-state

After a future exact activation, one bounded implementer may modify or create only:

| # | Operation | Path | Required pre-state at AM3 refreeze |
|---:|---|---|---|
| 1 | MODIFY | `scripts/ai/lib/agent_ops_projection.py` | SHA-256 `3edf0cea2811176493fa0eb60885071c4064aa6442792f9e67e62a57878afcd3` |
| 2 | MODIFY | `config/schemas/agent-ops-projection.schema.json` | SHA-256 `a58680b29eb3893e4446d113af8dee5c3a15d0aa69d623355de6252eb643a466` |
| 3 | MODIFY | `scripts/ai/aq-tui-dashboard` | SHA-256 `8bab94bb7f7f1d2eb5757494be0bee2d5807a9a94e52578c3e7bd4790b10051f` |
| 4 | MODIFY | `dashboard/backend/api/routes/aistack.py` | SHA-256 `a5831eecf7b36cc178f72565a998836133e512e66c17d291e92fbc80cb28d8b6` |
| 5 | MODIFY | `assets/dashboard.js` | SHA-256 `1e0287cd7b4c153bc57d832fda7cac434fc19ec799eaadb7b98cd3ea7808ea49` |
| 6 | MODIFY | `scripts/testing/test-agent-ops-projection.py` | SHA-256 `cab49dd463d19ece31ce8341bdcad3b6352a244024f7ffe77e2f9ba6854f95af` |
| 7 | MODIFY | `scripts/testing/harness_qa/phases/phase0.py` | SHA-256 `b01edf1308c6433c6fae1316a75b7d4251b77df9afa5d8e4fb41b99c5ca63999` |
| 8 | NEW | `config/schemas/agent-ops-local-direct-health.schema.json` | absent and not a symlink |
| 9 | NEW | `scripts/testing/test-agent-ops-local-direct-health-web.py` | absent and not a symlink |

All seven hashes and both absence/non-symlink guards were recomputed at AM3 preparation against the
exact base HEAD and matched AM2. None of the nine paths was changed by the intervening commit.

The activated implementer must reverify exact HEAD, all seven hashes, both absences, no staged copy
of any of the nine paths, and no active or ambiguous writer immediately before the first write. A
tenth implementation file, predecessor mismatch, HEAD drift, staged overlap, concurrent writer, or
candidate change is a mandatory fail-stop and requires another fresh authorization.

## 4. Activation, evidence, and acceptance remain unchanged

This record grants no action in PREPARED_ONLY state. A fresh independent reviewer who authored
neither the AM2 nor AM3 authorization must issue an exact-subject `PASS` covering the incorporated
AM2 architecture, privacy/security, SRE, dashboard truthfulness, Service Coverage, inventory
closure, exact design binding, disjoint-commit evidence, and active-writer controls.

Only after that PASS may the owner or authorized orchestrator emit an activation naming:

- this AM3 document's final SHA-256;
- one eligible implementer identity;
- a window no longer than 24 hours;
- exact HEAD `2ddc2671da3ce15db8f2368901cd226dd12279ef`; and
- all seven predecessor hashes and both absence guards.

Implementation validation remains limited to the exact offline candidate-local commands in AM2.
Independent acceptance remains separate and must recompute all nine final hashes and challenge every
AM2 schema, bounds, privacy, freshness, aggregation, cache, Phase-0, frontend, Service Coverage,
authority, and exclusion criterion. Repository Tier0 and live read-only dashboard checks require a
later separately activated acceptance grant. Commit authority remains orchestrator-only.

AM2 was never activated and its first-write consumption point was never reached. It is nevertheless
superseded and non-activatable after this AM3 refreeze because its exact-HEAD guard no longer names
the repository base. AM3 is itself single-use after activation: first candidate write consumes it;
any stop, drift, overlap, reviewer correction, or byte change makes it non-replayable.

`RECORD: PREPARED_ONLY. This document authorizes no implementation, validation execution, staging,
commit, provider, network, runtime, service, Nix, deployment, canary, traffic, or lifecycle action.`
