# C0.6-T AM7 candidate acceptance

**Verdict:** REQUEST_REVISION  
**Review date:** 2026-07-27  
**Independent reviewer:** `codex-subagent-c06-am7-acceptance`
(`gpt-5.6-sol`, flagship; distinct from implementer)  
**Projection author:** `codex-orchestrator` (transcribed from the review receipt;
not the reviewer)

## Exact subject

- authorization:
  `80f0010f11c79abea68f1306046da2f3d3d5599613a6287398f237d310ea06d4`;
- revision:
  `1a22fad17c9d10f8ec375e456369a972b4cd76c742bf50631551815de3d2244b`;
- reviewed HEAD:
  `107f7e8ab2452b4d89ff737b28966e35bf4f9e24`;
- nine candidate hashes: recorded exactly in the AM8 revision packet.

Focused evidence passed: 121 projection checks, 17 web/backend checks, Python
compile, both JSON parses, JavaScript syntax, and diff hygiene. Reliability
checks 02/15/16 fail only on the separately documented `task_registry.py`
manifest drift and do not count against AM7.

## Blocking revisions

1. reject boolean and unhashable closed-registry values deterministically;
2. route every cached return through effective-age projection and test
   `10/10+/60/60+` plus worse-state preservation;
3. enforce one absolute spawn/read/cap/terminate/kill/reap lifecycle deadline,
   including non-returning doubles and elapsed-time assertions;
4. reject frontend arrays and execute the coordinator-fallback × local-state ×
   severity cross-product;
5. add the surviving-path `terminal_at_ms < phase_entered_at_ms` negative vector
   and disposition dead aggregation helpers; and
6. resolve the dashboard port from the port SSOT instead of hardcoding `8889`.

No selective integration, staging, commit, runtime adoption, or deployment is
authorized.
