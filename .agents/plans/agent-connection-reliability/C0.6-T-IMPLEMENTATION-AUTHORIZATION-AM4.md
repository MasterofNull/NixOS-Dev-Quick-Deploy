# C0.6-T — Telemetry Implementation Authorization AM4

Authorization ID: `auth-agent-connection-reliability-c0.6-t-am4-20260722`  
Idempotency key: `agent-connection-reliability:c0.6-t:telemetry:am4:20260722`  
Status: **PREPARED_ONLY — IMPLEMENTATION NOT AUTHORIZED**  
Revision: **AM4 — rejected-candidate correction only**  
Exact base HEAD: `2ddc2671da3ce15db8f2368901cd226dd12279ef`  
Design SHA-256: `50ea6fcb7584e26b2f5774571692db3acd5403275b11000f2f1d8bf5e5130aff`  
Correction specification: `C0.6-T-AM4-CANDIDATE-REVISION.md`  
Correction specification SHA-256: `e2525bcf39e2838a6cb127995942a1b0d11d7548a6cec321fdf8f6b9cb2eb73f`  
Consumed AM3 authorization SHA-256: `409003716bf444e68f0e30d0cf4edf5fa2d61b8ee139b0708e0b244853ab08b7`  
Binding AM3 rejection SHA-256: `3d9e6f638efadacd7725b5dd6b908b5b3d7b520f5b3cc85708e1c6f235f1daf7`

## 1. Status, lineage, and activation rule

AM3 was activated, produced the exact nine-file candidate, and was consumed at first write. Its
binding acceptance verdict is `REQUEST_REVISION`; AM3 and every earlier C0.6-T authorization are
non-replayable and non-activatable. AM4 preserves the rejected bytes as its exact predecessor and
authorizes only the corrections in the bound specification after a future activation.

This record grants no action while PREPARED_ONLY. Before activation, an independent reviewer who
authored neither AM4 document must issue an exact-subject `PASS` covering lineage, correction
completeness, architecture, privacy/security, SRE, dashboard truthfulness, authority-bound producer
trust, Service Coverage, nine-file closure, tests, and all exclusions. Any byte change invalidates
that review.

Only after that PASS may the owner or authorized orchestrator emit an explicit activation naming:

- this document's final SHA-256 and the correction-specification SHA-256;
- exactly one eligible implementer identity;
- a window no longer than 24 hours;
- exact HEAD `2ddc2671da3ce15db8f2368901cd226dd12279ef`;
- all nine predecessor hashes below; and
- confirmation that none of the nine paths is staged, symlink-substituted, or owned by another
  active/ambiguous writer.

Standing program direction, the AM3 activation, the AM3 test passes, and this document's existence do
not activate AM4.

## 2. Exact nine-file MODIFY ceiling

After activation, the named implementer may modify exactly these existing candidate paths and no
others:

| # | Path | Required AM4 pre-state SHA-256 |
|---:|---|---|
| 1 | `scripts/ai/lib/agent_ops_projection.py` | `4bf21532bfb6fcf2fd64249e8948c8c0f083bcad8f800b543d39097461e6f9d8` |
| 2 | `config/schemas/agent-ops-projection.schema.json` | `7fd3c38118aabce13dcaa5f3b4f04fa96b07b3a3e857e045ade3ba5899511cf0` |
| 3 | `scripts/ai/aq-tui-dashboard` | `9685542c3686d2f9980cbfdaf12256971bab10ffe7b0333847a0bed5bf88ebad` |
| 4 | `dashboard/backend/api/routes/aistack.py` | `52fb75c41f1c32e6d5b933dac58f7769bfcc901b9c1fe7da44be7d0721ec17ad` |
| 5 | `assets/dashboard.js` | `e1c3cd2d0f5ae5a2281954ee1f6556b87d9a168c160b35701ecdf9926f9b9250` |
| 6 | `scripts/testing/test-agent-ops-projection.py` | `a7886699ed75ff0b265830b14bab8f291bb321b461121fef6ffc2137ea66c544` |
| 7 | `scripts/testing/harness_qa/phases/phase0.py` | `f39021c080a2742e8841972cf9d88e0333cb200ee4b828c472ce0f8e2244447b` |
| 8 | `config/schemas/agent-ops-local-direct-health.schema.json` | `9e1be4a685aacea459c9973b9da3e9e05f6dc158517a499ccfb41c2f0fcecb7c` |
| 9 | `scripts/testing/test-agent-ops-local-direct-health-web.py` | `d7bd095f68aeab2945ccb06160191f0a4b836b0dfb42a62333ce5fcb33f78979` |

All nine are `MODIFY` operations for AM4. The two untracked AM3-created files must remain regular,
non-symlink files at their exact hashes. A tenth implementation file, HEAD drift, hash mismatch,
staged overlap, symlink, concurrent/ambiguous writer, missing rejected candidate, or changed design/
specification is a hard stop before writing.

## 3. Exact authorized correction

The activated implementer may perform only the mechanical and test changes required by
`C0.6-T-AM4-CANDIDATE-REVISION.md`:

1. make every cache/fallback response schema-valid, preserve typed unavailable for no-data, permit
   assessed stale values only through the effective 60-second bound, and erase lifecycle values after
   that bound;
2. enforce semantic equality among standalone and embedded Draft-2020-12 schemas and the projector,
   TUI, backend, final-response validators using one golden parity corpus;
3. replace unbounded `communicate()` accumulation with combined max-plus-one stdout/stderr reads and
   bounded terminate/kill/reap cleanup across overflow, timeout, route cancellation, and shared
   refresh cancellation;
4. require exact ordinary four-field layer-5 CheckResults for IDs `0.10.42` and `0.10.43`, inclusive
   300-second immutable-evidence freshness, fixed descriptions, unique IDs, and closed status mapping;
5. run both offline contract/privacy suites before `dashboard_safe` or host-unavailable skip, and
   validate a successful GET against the full semantic schema;
6. keep local lifecycle rendering active when coordinator status is unavailable and include both in
   deterministic worst-severity badge selection;
7. correct deadline equality, false `timeout_observed`, terminal timestamp/window/dedup ordering, and
   add every specified boundary, cancellation, oversize, schema, privacy, and ordering vector; and
8. derive trust only from existing strict TaskRegistry sole-writer provenance. Nested registry
   `trusted`/`producer` claims never authorize themselves; insufficient authority-bound provenance
   rejects and remains unavailable/blocked. No registry writer edit is allowed.

AM4 must not weaken the design's typed no-data, aggregate-only payload, privacy canaries, one-refresh
anti-self-attestation, sole-reader boundary, no-mutation monitoring rule, fixed vocabulary, or
one-timer frontend contract.

## 4. Authorized offline validation only

The implementer may run only these candidate-local, non-provider, non-network validations:

```text
python3 scripts/testing/test-agent-ops-projection.py
python3 scripts/testing/test-agent-ops-local-direct-health-web.py
python3 -m py_compile scripts/ai/lib/agent_ops_projection.py scripts/ai/aq-tui-dashboard \
  dashboard/backend/api/routes/aistack.py scripts/testing/test-agent-ops-projection.py \
  scripts/testing/test-agent-ops-local-direct-health-web.py \
  scripts/testing/harness_qa/phases/phase0.py
node --check assets/dashboard.js
python3 -m json.tool config/schemas/agent-ops-projection.schema.json
python3 -m json.tool config/schemas/agent-ops-local-direct-health.schema.json
git diff --check -- <the exact nine paths>
sha256sum <the exact nine paths>
```

All child/cache/Phase-0 tests must use injected clocks, temporary synthetic registries and immutable
QA stores, fake subprocesses, fake HTTP results, and process fixtures that never invoke an agent,
provider, local inference, deployed dashboard, or network. Process fixtures must be bounded and
reaped. Do not run `aq-qa`, repository Tier0, a live GET, a service, or a provider readiness probe.

The implementation report must state exact final hashes, test names and counts, the semantic parity
vector count, child-reap evidence, authority/provenance negative vectors, scoped diff inventory,
explicit skipped live/Tier0 work, and any deviation. The implementer must not stage, commit, push,
self-review, dispatch another agent, or change collaboration authority.

## 5. Mandatory independent acceptance

A different eligible reviewer/session must recompute all nine final hashes and independently verify:

- cache values at 10, 10+epsilon, 60, and 60+epsilon seconds, final semantic validation, and no
  assessed values surviving beyond 60 seconds;
- exact accepted/rejected parity across both schemas and every runtime/API validator;
- combined stdout/stderr max, max+1, continuous-output, timeout, waiter cancellation,
  shared-refresh cancellation, terminate/kill/reap, and subsequent single-refresh recovery;
- exact CheckResult fields/types/layer/ID/description/status, duplicate/missing/wrong-ID behavior,
  inclusive 300-second freshness, and one-refresh anti-self-attestation;
- offline suites executing in safe mode before host skip, and complete closed-schema GET validation;
- coordinator-unavailable plus local healthy/degraded/blocked/unavailable rendering and worst badge;
- deadline equality, terminal window edges/order/dedup, timeout reason correctness, every phase/reason/
  severity row, all numeric boundaries, generation/deadline/cache cases, and recursive privacy;
- authority-bound registry producer provenance and rejection of self-asserted, legacy, ambiguous, or
  mismatched facts; and
- exact scope, no staged paths, no provider/network/runtime action, and all exclusions.

A PASS permits only a later separately authorized repository Tier0/live read-only acceptance step.
It does not authorize Track A runtime, canary traffic, provider work, deployment, staging, or commit.

## 6. Exclusions, consumption, and stop record

AM4 authorizes no edit to Tier0 scripts, delegate wrappers, dispatcher, TaskRegistry writer, registry
data, slot queue, local-inference transport, prompt/model/profile policy, environment/port contract,
dashboard HTML, application wiring, Nix, service, systemd, AppArmor, database, lifecycle store, or
broker. It authorizes no live registry/dashboard/provider/network/local inference, service action,
deployment, canary, traffic, migration, staging, commit, push, rollback, or deletion.

AM4 is single-use after activation. The first candidate write consumes it. Any stop, interruption,
drift, overlap, correction beyond the bound findings, failed precondition, or reviewer-requested byte
change makes it non-replayable and requires a fresh exact authorization. An unavailable or timed-out
reviewer is never credited as approval.

`RECORD: PREPARED_ONLY. No implementation, validation execution, acceptance, staging, commit,
provider, network, runtime, service, Nix, deployment, canary, traffic, or lifecycle authority is
granted until a fresh independent exact-subject PASS and explicit hash-bound activation.`
