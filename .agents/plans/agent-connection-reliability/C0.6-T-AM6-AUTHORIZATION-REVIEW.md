# Independent Authorization Review — C0.6-T Telemetry AM6

Review date: 2026-07-22  
Reviewer: `codex-subagent-c06-t-am6-authorization-reviewer`  
Role: independent architecture, privacy/security, SRE, dashboard, and authorization reviewer  
Review mode: exact-subject read-only governance review; no candidate or live authority

## Exact subjects and frozen pre-state

- AM6 correction packet: `C0.6-T-AM6-CANDIDATE-REVISION.md`
  - SHA-256: `7c4bf48d88bf3a11e583c5233d1a168c4d5eb56be032abc177779b8b897ba11d`
- PREPARED_ONLY authorization: `C0.6-T-IMPLEMENTATION-AUTHORIZATION-AM6.md`
  - SHA-256: `4d2d2e8dfa5582c659ad6e75ba19bca45052e22cb639da3352b13513d684be2c`
- Exact HEAD: `2ddc2671da3ce15db8f2368901cd226dd12279ef`
- Parent design: `50ea6fcb7584e26b2f5774571692db3acd5403275b11000f2f1d8bf5e5130aff`
- Binding AM5 acceptance: `f8b3a164f1943f2b50f4770f90579de780735c1d543d8acc384b4d774e8f2076`

All bindings and HEAD match. The nine current AM5 candidate paths are regular, non-symlink,
unstaged, and match the required all-MODIFY predecessor hashes exactly:

`7534af1a0b605c8b82dee1e44c16ba86694c07cac2d36fef17147f7cffc77ff3`,
`816661919e8fdc8b5cd5e6599930e9ad615cf7fc9622190ca1c974fabe44cbc4`,
`09134cd8d1712f8bf7fdcdd725a25ba92ccd255f37aed358e65fbff17998c0d8`,
`5695ac633cab146f06895887172d725f6b0c6e96decb8785b397fb14e15aa3e6`,
`63236e756f19672e69daf1798eb0a6390c22e76383fa61f22b37eb4d0ddc365b`,
`58a78f4dd46ceaef25c887bfaa2c7e7bd91d9bcb372bd7314e5150e4220eef83`,
`91979b59d7049f6cf1e5bc12a02115e315c33c89a1c3e09139303da30e6deee9`,
`0b03f0a5456a8c1501feb469dbeaf8a26de69f0590b06abc4cc729234de80ef9`, and
`5f78d63ceafb6cab27a01617a5aabea15861f79d255f302eedce5a9347e6062f`.

The collaboration record proves AM3 and AM5 crossed first write and are consumed/non-replayable.
AM4 was prepared but never activated, then superseded after its exact-subject revision verdict; it
is non-activatable. AM6 inherits no prior implementer or window and does not revive any grant.

## Correction completeness and implementability

### Frontend availability truth

AM6 directly closes the first AM5 blocker. Coordinator availability is now a closed predicate:
object-shaped response with no explicit `available=false`; missing, null, malformed, and the exact
backend fallback object are unavailable. Local retrieval/rendering remains independent and the badge
must select the worst frozen severity. The required cross-product tests prevent a truthy fallback
object or local state from laundering coordinator unavailability.

### Effective age on every cache return

AM6 replaces branch-specific cache behavior with one pure effective-age projection used by the
ordinary TTL hit, refresh success, failure fallback, shared-waiter return, and cancellation fallback.
It freezes the accepted payload, accepted source-age metadata, injected monotonic acceptance time,
copy-before-projection, non-improvement rule, final validation, and inclusive `<=10`, `>10..<=60`,
and `>60` transitions. The current conservative category-bound source-age metadata remains usable;
AM6 does not require a schema field or a tenth path. Direct route tests at exact and immediately-over
boundaries close the test hole identified in AM5.

### Closed current-v1 outer authority

AM6 freezes an implementable exact outer allowlist and every type, enum, constant, and scalar bound
from the `new_record` branch of `aq.delegation-task-record.v1`. Booleans are explicitly excluded from
integer positions. Process-attachment and transition-request operation objects reject as source rows.
Exact valid rows have no lifecycle telemetry and remain typed not-instrumented; any unknown/extra
key, nested payload, malformed value, named or arbitrarily named attempted provenance/trust field,
or mixed invalid row rejects the whole source as blocked/source-invalid. Requiring exact closed-
schema validation rather than a forbidden-name list closes the AM5 selective-detection defect and
prevents partial laundering.

### One absolute child lifecycle budget

AM6 freezes one injected absolute monotonic deadline of at most two seconds across spawn, both pipe
reads, combined output-cap enforcement, terminate, kill, and confirmed reap. Every await consumes
only remaining budget; cleanup cannot create a second window. Cached fallback or normal response is
forbidden until terminal/reaped state is confirmed. An unconfirmed reap propagates a fixed integrity
failure rather than being suppressed. Cancellation, concurrent waiters, output overflow, and non-
returning test doubles are all explicitly covered, including total elapsed-time evidence. This is a
complete, testable correction to AM5's cleanup-outside-deadline defect.

### Terminal ordering and regression closure

AM6 requires `terminal_at_ms >= phase_entered_at_ms` wherever terminal data survives, with whole-
source rejection on failure. It permits removal of unreachable future-version aggregation only if a
test proves the surviving validator. This avoids preserving misleading dead validation while still
preventing inverted timestamps from becoming an accepted future path.

Together these five groups close every binding AM5 finding without weakening prior AM4/AM5 cache,
schema-parity, output-cap, QA-evidence, Phase-0, dashboard, deadline, privacy, or monitoring
requirements. Regression coverage is mandatory rather than deferred.

## Scope, activation, and stop controls

AM6 preserves the exact nine-file all-MODIFY ceiling and offline-only implementation boundary. It
adds no writer/schema authority, live source, service, provider/model call, network/database access,
Nix/systemd/AppArmor change, deployment, traffic, M2B/M3/R1-R4 action, or later slice.

The authorization remains PREPARED_ONLY. A valid activation must name its final hash, one eligible
implementer, bounded start/expiry, exact HEAD, all nine pre-edit hashes, and unchanged stops. Before
first write the implementer must prove no drift, staging, symlink, operation change, tenth path, or
active/ambiguous overlapping writer. First candidate write consumes AM6; failure, interruption,
drift, overlap, or abandonment makes it non-replayable.

Only the packet's offline validations are permitted. Tier0, live route/browser/registry work,
provider/network/local inference, service/process control, staging, commit, push, deployment,
rollback, and deletion remain excluded. The implementer cannot self-accept; a fresh exact-byte
candidate review is mandatory.

No candidate edit, implementation validation, live action, staging, or commit occurred in this
review.

VERDICT: PASS — AM6 completely and implementably closes the five binding AM5 defects within the frozen nine-file offline correction boundary and is ready only for explicit hash-bound owner activation
