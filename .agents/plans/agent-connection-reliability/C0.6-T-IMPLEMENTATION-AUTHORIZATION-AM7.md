# C0.6-T Implementation Authorization AM7

Authorization ID: `auth-agent-connection-reliability-c0.6-t-am7-20260727`  
Idempotency key: `agent-connection-reliability:c0.6-t:telemetry:am7:20260727`  
Status: **PREPARED_ONLY — IMPLEMENTATION NOT AUTHORIZED**

This is a single-use, hash-bound authorization template. It is not active and
permits no implementation until an independent reviewer passes this exact
subject and the owner later activates its exact SHA-256, names one implementer,
and supplies a bounded activation window.

## 1. Authorized subject

- Exact base HEAD: `107f7e8ab2452b4d89ff737b28966e35bf4f9e24`
- AM7 candidate revision:
  `.agents/plans/agent-connection-reliability/C0.6-T-AM7-CANDIDATE-REVISION.md`
- AM7 candidate revision SHA-256:
  `1a22fad17c9d10f8ec375e456369a972b4cd76c742bf50631551815de3d2244b`
- Parent design packet SHA-256:
  `50ea6fcb7584e26b2f5774571692db3acd5403275b11000f2f1d8bf5e5130aff`
- AM6 authorization review SHA-256:
  `415dfa2c905940deb3e614e616ea3d330673d49e427efbeab7a4771c480a27a5`
- AM6 authorization at owner activation SHA-256:
  `4d2d2e8dfa5582c659ad6e75ba19bca45052e22cb639da3352b13513d684be2c`
- Current AM6 authorization SHA-256 after its additive activation-record appendix:
  `5d5d5811d05a4ecfeeb24c49e2811bb7df58626707a829bd3b69da8f793386bc`
- AM6 activation event: `af07ebdcd2724f14b02924be3ba5ed1b`
  (binds the historical activated hash; the appendix does not rewrite it).
- AM5 acceptance SHA-256:
  `f8b3a164f1943f2b50f4770f90579de780735c1d543d8acc384b4d774e8f2076`

AM3, AM5, and AM6 are consumed and non-replayable. AM4 was never activated and
remains superseded and non-activatable. This authorization inherits no earlier
implementer identity or activation window and does not revive any prior grant.

## 2. Exact file ceiling and pre-edit hashes

If later activated, the assigned implementer may modify exactly these nine
candidate paths and no others:

| Path | Operation | Required pre-edit SHA-256 |
|---|---|---|
| `scripts/ai/lib/agent_ops_projection.py` | MODIFY | `7534af1a0b605c8b82dee1e44c16ba86694c07cac2d36fef17147f7cffc77ff3` |
| `config/schemas/agent-ops-projection.schema.json` | MODIFY | `816661919e8fdc8b5cd5e6599930e9ad615cf7fc9622190ca1c974fabe44cbc4` |
| `scripts/ai/aq-tui-dashboard` | MODIFY | `09134cd8d1712f8bf7fdcdd725a25ba92ccd255f37aed358e65fbff17998c0d8` |
| `dashboard/backend/api/routes/aistack.py` | MODIFY | `5695ac633cab146f06895887172d725f6b0c6e96decb8785b397fb14e15aa3e6` |
| `assets/dashboard.js` | MODIFY | `63236e756f19672e69daf1798eb0a6390c22e76383fa61f22b37eb4d0ddc365b` |
| `scripts/testing/test-agent-ops-projection.py` | MODIFY | `58a78f4dd46ceaef25c887bfaa2c7e7bd91d9bcb372bd7314e5150e4220eef83` |
| `scripts/testing/harness_qa/phases/phase0.py` | MODIFY | `91979b59d7049f6cf1e5bc12a02115e315c33c89a1c3e09139303da30e6deee9` |
| `config/schemas/agent-ops-local-direct-health.schema.json` | MODIFY | `0b03f0a5456a8c1501feb469dbeaf8a26de69f0590b06abc4cc729234de80ef9` |
| `scripts/testing/test-agent-ops-local-direct-health-web.py` | MODIFY | `5f78d63ceafb6cab27a01617a5aabea15861f79d255f302eedce5a9347e6062f` |

No creation, deletion, rename, archive, symlink, mode change, generated file, or
out-of-inventory repository write is permitted. The sole generated-output
exception is Python bytecode under `/tmp/aq-c06t-am7-pyc` for explicit syntax
validation; test commands must set `PYTHONDONTWRITEBYTECODE=1`.

## 3. Authorized objective

If activated, the implementer may make only the five corrections and regression
closures specified by the exact AM7 revision:

1. honor explicit coordinator `available: false`, preserve independent
   local-direct rendering, and select deterministic worst combined severity;
2. apply elapsed effective source age and inclusive 10/60-second relabeling on
   every cached return without mutating stored evidence or improving health;
3. enforce the exact closed outer `aq.delegation-task-record.v1` `new_record`
   branch, reject whole-source extra/malformed/operation attempts, and keep an
   exact valid v1 row typed not-instrumented;
4. place spawn, pipe reads, output cap, terminate, kill, and confirmed reap
   inside one absolute monotonic budget of at most two seconds, including
   cancellation and shared waiters, with explicit unconfirmed-reap failure;
5. reject `terminal_at_ms < phase_entered_at_ms` in a reachable validator or
   remove dead validation and directly test the surviving path.

Every regression vector enumerated in AM7 section 3 is mandatory. Privacy,
passive projection, Phase-0 evidence, visible TUI/web dashboard health, and the
full Service Coverage contract are acceptance requirements, not follow-ups.

## 4. Activation requirements

Activation is valid only after an independent reviewer issues `PASS` for this
exact AM7 revision and authorization. The owner must then state all of the
following in one unambiguous instruction:

- this authorization document's exact SHA-256;
- one implementer identity;
- activation start and expiry timestamps;
- confirmation that exact HEAD, all nine pre-edit hashes, inventory, privacy,
  Service Coverage, offline-only bounds, and stop conditions remain unchanged.

The implementer must verify HEAD, all nine hashes, regular-file identity, and no
overlapping active writer before the first write. Any mismatch is a hard stop.
Activation is consumed by the first candidate write whether the attempt passes,
fails, is interrupted, or is abandoned. It cannot be replayed or transferred.

## 5. Mandatory stop conditions

Stop without editing if any of these is true:

- HEAD or any frozen pre-edit hash differs;
- another active task overlaps any of the nine paths;
- the correction requires a tenth file, changed operation, mode change, or
  generated artifact;
- satisfying a test would weaken a schema, status/reason vocabulary, age
  boundary, privacy rule, Phase-0 check, dashboard surface, Service Coverage
  gate, recusal rule, monitoring contract, or fail-closed behavior;
- network, live provider, database, registry read/write, service/process, Nix,
  deployment, traffic, Tier-0, staging, or commit access appears necessary;
- child termination and confirmed reap cannot remain within one absolute
  lifecycle budget without suppressing an integrity failure;
- the activation window is absent, expired, ambiguous, or names a different
  implementer.

On stop, leave all candidate paths untouched and report the exact blocker. The
implementer may not self-amend this authorization.

## 6. Independent reliability-manifest drift

The current broader reliability suite fails tests 02, 15, and 16 because its
frozen manifest does not recognize the current
`scripts/ai/lib/task_registry.py` SHA-256
`9285658eef68eb07a29e39db9f7fd9b43c51c2c827522d168d3ec5a86aa7b284`.
That path, the reliability test, its fixtures, and its manifest owner are
outside this exact nine-file grant.

Focused implementation and acceptance may run
`python3 scripts/testing/test-local-delegation-reliability.py`, but the failure
must be reported exactly. It may not be silently re-pinned, suppressed, waived,
or used to expand AM7. Any correction to that baseline requires an independent
bounded slice.

## 7. Validation and evidence

Only offline validation named in the exact AM7 revision is authorized. The
implementer must provide:

- exact command, result, and elapsed-time evidence for every required command;
- explicit evidence for all five correction groups and every regression vector;
- total elapsed-time and terminal-state evidence for lifecycle boundary tests;
- recursive privacy and complete Service Coverage evidence;
- `git status --short`, `git diff --check`, changed-file inventory, and final
  SHA-256 values for all nine candidate paths;
- exact reporting of the known broader reliability-manifest failure;
- a statement that no other path was written, staged, committed, deployed, or
  contacted.

Python files, including `scripts/ai/aq-tui-dashboard`, must be validated with
`PYTHONPYCACHEPREFIX=/tmp/aq-c06t-am7-pyc python3 -m py_compile`; `bash -n` is
not a valid syntax check for the dashboard CLI. Python test commands must use
`PYTHONDONTWRITEBYTECODE=1`. `assets/dashboard.js` requires `node --check`; both
JSON schemas require `python3 -m json.tool`.

Tier-0, `aq-qa`, live route/browser/registry access, provider/network/local
inference, database, service/process control, Nix, deployment, staging, and
commit are prohibited.

## 8. Acceptance and commit gate

The implementer cannot review, accept, stage, or commit the candidate. A fresh
independent reviewer must verify the activated authorization, exact revision
hash, candidate hashes, inventory, stop compliance, all mandatory vectors,
privacy, Service Coverage, and validation evidence and issue an explicit
`PASS`. A revision verdict permits no selective integration.

No staging, commit, live adoption, or later slice is authorized even after
implementation or candidate review. Each requires its own applicable authority.

## 9. Explicit exclusions

This document authorizes no edit while its status is `PREPARED_ONLY`. It never
authorizes staging, commit, deployment, live traffic, providers, network,
database, registry access, service/process control, Nix changes, broad
validation, Tier-0, M2B, M3, R1-R4, or any later AQ-OS work.

## 10. Next gate

An independent reviewer must issue an exact-subject authorization review.
Only after that `PASS` may the owner explicitly activate the resulting exact
authorization hash for one named implementer and bounded window.
