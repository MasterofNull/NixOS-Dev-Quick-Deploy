# C0.6-T Implementation Authorization AM6

Authorization ID: `auth-agent-connection-reliability-c0.6-t-am6-20260722`  
Idempotency key: `agent-connection-reliability:c0.6-t:telemetry:am6:20260722`  
Status: **PREPARED_ONLY — IMPLEMENTATION NOT AUTHORIZED**

This is a single-use, hash-bound authorization template. It is not active and
does not permit implementation until the owner explicitly activates its exact
SHA-256, names one implementer, and supplies a bounded activation window.

## 1. Authorized subject

- Exact base HEAD: `2ddc2671da3ce15db8f2368901cd226dd12279ef`
- AM6 candidate revision:
  `.agents/plans/agent-connection-reliability/C0.6-T-AM6-CANDIDATE-REVISION.md`
- AM6 candidate revision SHA-256:
  `7c4bf48d88bf3a11e583c5233d1a168c4d5eb56be032abc177779b8b897ba11d`
- Parent design packet SHA-256:
  `50ea6fcb7584e26b2f5774571692db3acd5403275b11000f2f1d8bf5e5130aff`
- AM5 acceptance SHA-256:
  `f8b3a164f1943f2b50f4770f90579de780735c1d543d8acc384b4d774e8f2076`

AM3 and AM5 are consumed and non-replayable. AM4 was unactivated and is
superseded and non-activatable. This authorization does not inherit an
implementer identity or activation window from any prior authorization.

## 2. Exact file ceiling and pre-edit hashes

If activated, the assigned implementer may modify exactly these nine files and
no others:

| Path | Operation | Required pre-edit SHA-256 |
|---|---|---|
| `scripts/ai/lib/agent_ops_projection.py` | MODIFY | `7534af1a0b605c8b82dee1e44c16ba86694c07cac2d36fef17147f7cffc77ff3` |
| `config/schemas/agent-ops-projection.schema.json` | MODIFY | `816661919e8fdc8b5cd5e6599930e9ad615cf7fc9622190ca1c974fabe44cbc4` |
| `scripts/ai/aq-tui-dashboard` | MODIFY | `09134cd8d1712f8bf7fdcdd725a25ba92ccd255f37aed358e65fbff17998c0d8` |
| `dashboard/backend/api/routes/aistack.py` | MODIFY | `5695ac633cab146f06895887172d725f6b0c6e96decb8785b397fb14e15aa3e6` |
| `assets/dashboard.js` | MODIFY | `63236e756f19672e69daf1798eb0a6390c22e76383fa61f22b37eb4d0ddc365b` |
| `scripts/testing/test-agent-ops-projection.py` | MODIFY | `58a78f4dd46ceaef25c887bfaa2c7e7bd91d9bcb372bd7314e5150e4220eef83` |
| `scripts/testing/harness_qa/phases/phase0.py` | MODIFY | `91979b59d7049f6cf1e5bc12a02115e315c33c89a1c3e09139303da30e6deee9` |
| `config/schemas/agent-ops-local-direct-health.schema.json` | MODIFY | `0b03f0a5456a8c1501feb469dbeaf8a26de69f0590b06abc4cc729234de80ef9` |
| `scripts/testing/test-agent-ops-local-direct-health-web.py` | MODIFY | `5f78d63ceafb6cab27a01617a5aabea15861f79d255f302eedce5a9347e6062f` |

Every path is `MODIFY`; no creation, deletion, rename, archive, symlink, mode
change, generated file, or out-of-inventory write is permitted.

## 3. Authorized objective

If activated, the implementer may make only the corrections specified in the
exact AM6 revision:

1. make frontend coordinator availability honor explicit `available: false`,
   render local-direct state independently, and choose the worst combined
   severity;
2. apply elapsed effective source age and deterministic inclusive 10/60-second
   relabeling on every cache return without improving or extending health;
3. enforce the exact closed outer `aq.delegation-task-record.v1` schema at the
   adapter, reject the whole source on any extra/malformed attempted evidence,
   and classify an exact valid v1 record as not instrumented;
4. place spawn, read, output cap, terminate, kill, and confirmed reap inside one
   monotonic budget of at most two seconds, including cancellation and shared
   waiters, with no suppressed unconfirmed child;
5. reject `terminal_at_ms < phase_entered_at_ms` in a reachable validator, or
   remove the dormant helper and directly test the surviving validation path.

Regression coverage for every correction is part of the authorized objective,
not an optional follow-up.

## 4. Activation requirements

Activation is valid only when the owner states all of the following in one
unambiguous instruction:

- this authorization document's exact SHA-256;
- one implementer identity;
- an activation start and expiry timestamp;
- confirmation that exact HEAD, pre-edit hashes, file inventory, and all stop
  conditions remain unchanged.

The implementer must verify HEAD and all nine pre-edit hashes before the first
write. A mismatch is a hard stop. Activation is single-use and is consumed by
the first candidate write, whether the candidate passes, fails, is interrupted,
or is abandoned. It may not be replayed or transferred to another implementer.

## 5. Mandatory stop conditions

Stop without editing if any of these is true:

- HEAD or any frozen pre-edit hash differs;
- another active task overlaps any of the nine paths;
- the requested correction requires a tenth file or different operation;
- satisfying a test would weaken a frozen schema, reason, age, coverage,
  Phase-0, recusal, monitoring, or fail-closed contract;
- network, live provider, database, registry write, service, Nix, deployment,
  traffic, or Tier-0 access appears necessary;
- child termination and confirmed reap cannot be kept within the one absolute
  lifecycle budget without suppressing an integrity failure;
- the activation window is absent, expired, ambiguous, or names a different
  implementer.

On stop, the implementer must leave candidate files untouched and report the
exact blocker. The implementer may not self-amend this authorization.

## 6. Validation and evidence

Only offline validation named in the exact AM6 revision is authorized. Tier-0,
live route calls, live browser use, deployment, service restart, and provider or
network access are prohibited.

The implementer must provide:

- exact command/result evidence for every required validation;
- explicit regression evidence for all five AM6 correction groups;
- total elapsed-time evidence for lifecycle boundary tests;
- `git status --short` and `git diff --check` output;
- final SHA-256 values for all nine candidate files;
- a statement that no other path was written, staged, committed, deployed, or
  contacted.

## 7. Acceptance and commit gate

The implementer cannot accept or commit the candidate. An independent reviewer
must verify the activated authorization, exact revision hash, candidate hashes,
inventory, stop-condition compliance, and regression evidence and issue an
explicit `PASS`. A revision verdict does not permit selective integration.

No staging, commit, live adoption, or later slice is authorized by this
document, even after implementation or review. Those actions require their own
applicable authority.

## 8. Explicit exclusions

This document does not authorize any edit while its status is `PREPARED_ONLY`.
It never authorizes staging, commit, deployment, live traffic, live providers,
network access, database access, registry writes, service/process control, Nix
changes, broad validation, Tier-0, M2B, M3, R1-R4, or any later AQ-OS work.


## Owner Activation Record (reconciled 2026-07-23)
**Activation state: ACTIVATED** (record reconciled from the authoritative event ledger).
Owner activation recorded as a `pulse.append` in `.agents/events/*.jsonl` — subject `C0.6-T-AM6`, event_id `af07ebdcd2724f14b02924be3ba5ed1b`, ts `2026-07-22T21:54:17Z`. Any `PREPARED_ONLY / NOT ACTIVATED` status earlier in this record is a **stale header** predating the activation; the owner activation and any independently-accepted, committed candidate stand. Reconciled by fable-5 (no scope, ceiling, or hash change — header hygiene only).
