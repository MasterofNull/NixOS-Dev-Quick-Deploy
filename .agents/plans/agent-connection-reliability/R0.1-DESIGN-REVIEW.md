# Independent Design Review — R0.1 Legacy Registry Lookup Compatibility

Reviewer: independent Codex sub-agent, recorded by the orchestrator
Reviewed: 2026-07-16
Subject SHA-256: `41079079cce71ce83e834a7e24a2373813fc3b8311d09d0e9c91a6f591c67633`

The amended design closes every binding finding from the first review:

- byte-level LF/CRLF/final-record framing, cumulative source bounds, and inode mutation outcomes are
  exact;
- duplicate JSON keys and ambiguous or invalid identities fail closed;
- duplicate-target detection remains scan-to-EOF and memory bounded;
- strict M2A writers and `show_m2a()` remain unchanged;
- the CLI record contract and injected projection-health contract are separate, closed, and private;
- the projector remains pure and performs no registry I/O;
- access-time behavior is not represented as an immutability guarantee;
- C0.5B sequencing, adversarial tests, Phase-0/TUI visibility, and the mandatory separate web
  visibility prerequisite are explicit.

Current code confirms the packet premises: the strict record limit remains 4,096 bytes, strict show
still uses the strict reader, the identity grammar matches the design, and projection v2 carries
`coverage_health.web_dashboard`.

This PASS approves the design only. Implementation requires a separate hash-bound authorization and
cannot activate while the required source-backed web visibility amendment remains absent.

VERDICT: PASS
