diff --git a/.gitignore b/.gitignore
index 3cfa7011e27fa75246a22e5973e9045ddf03bb83..34126fc75dea208a228ed3e57e2c3aa101c80b27 100644
--- a/.gitignore
+++ b/.gitignore
@@ -203,6 +203,10 @@ nix/hosts/*/.sops.yaml
 # Agent delegation task output logs and local registry are runtime state.
 .agents/delegation/outputs/
 .agents/delegation/fanout/
+# CS-1: per-dispatch isolated git worktrees (delegate-to-codex/-local) — torn
+# down after handback; the delegate/<task-id> branch + outputs/*.patch are
+# the preserved evidence, this directory itself is disposable scratch.
+.agents/delegation/worktrees/
 # Phase 83 — DAG session traces (runtime; large JSONL, not for version control)
 .agents/dag-sessions/
 # Phase 153 — Candidate proposals (runtime, auto-generated from candidates.json)
diff --git a/scripts/ai/delegate-to-codex b/scripts/ai/delegate-to-codex
index 6f7d5c229bf79f0e4111ef98ca36cc392c681aad..d7a70f071c5a88905e76c520869373df3d2018a5 100755
--- a/scripts/ai/delegate-to-codex
+++ b/scripts/ai/delegate-to-codex
@@ -19,6 +19,16 @@
 #   --model MODEL         Model override (e.g. o3, o4-mini)
 #   --wait                Block until task completes
 #   --force-quota-retry   Bypass an active quota cooldown for one call
+#   --shared, --no-worktree
+#                         Run in the SHARED checkout (REPO_ROOT) instead of an
+#                         isolated git worktree. Default is ISOLATED: every
+#                         dispatch runs in its own git worktree on branch
+#                         delegate/<task-id> and hands the result back as a
+#                         patch (.agents/delegation/outputs/<id>.patch) plus
+#                         a commit on that branch — never staging/committing
+#                         into the shared REPO_ROOT index. Use --shared only
+#                         for read-only/analysis tasks that need the live
+#                         shared tree.
 #   --check ID            Print output of a completed task
 #   --status ID           Show task status
 #   --list                List all registered tasks
@@ -46,6 +56,8 @@ COOLDOWN_FILE="$DELEGATION_DIR/.codex-quota-cooldown"
 # shellcheck disable=SC1091
 source "${SCRIPT_DIR}/lib/audit-write.sh" 2>/dev/null || true
 source "${SCRIPT_DIR}/lib/harness-grounding.sh" 2>/dev/null || true
+# CS-1 — worktree isolation (wt_create/wt_handback/wt_teardown/wt_tag_registry)
+source "${SCRIPT_DIR}/lib/worktree-isolation.sh" 2>/dev/null || true
 
 # ── helpers ─────────────────────────────────────────────────────────────────
 
@@ -174,6 +186,21 @@ check_pid_alive() {
     [[ -n "$pid" ]] && [[ "$pid" != "null" ]] && kill -0 "$pid" 2>/dev/null
 }
 
+# cmd_worktree_handback <task_id> <worktree_path> <branch>
+# CS-1: called once the codex run has fully exited (wait-mode: inline;
+# background-mode: re-invoked via --internal-worktree-handback, mirroring the
+# --internal-quota-capture re-entry pattern below). Captures the isolated
+# worktree's changes as a patch + a commit on its own delegate/<task-id>
+# branch, tags the registry, then tears the worktree down. No-op if no
+# worktree was created for this dispatch (shared-checkout mode).
+cmd_worktree_handback() {
+    local task_id="$1" wt_path="$2" branch="$3"
+    [[ -n "$wt_path" ]] || return 0
+    wt_handback "$REPO_ROOT" "$wt_path" "$task_id" "$OUTPUTS_DIR"
+    wt_tag_registry "$REGISTRY" "$task_id" "$wt_path" "$branch" "$OUTPUTS_DIR/${task_id}.patch"
+    wt_teardown "$REPO_ROOT" "$wt_path"
+}
+
 # ── quota-aware pre-check (delegate-codex-quota-precheck slice) ─────────────
 # See .agents/plans/delegate-codex-quota-precheck/SLICE-DESIGN-AND-AUTHORIZATION.md
 
@@ -333,7 +360,7 @@ PYEOF
 }
 
 cmd_delegate() {
-    local prompt="$1" mode="${2:-safe}" model_flag="${3:-}" wait_mode="${4:-false}" force_quota_retry="${5:-false}"
+    local prompt="$1" mode="${2:-safe}" model_flag="${3:-}" wait_mode="${4:-false}" force_quota_retry="${5:-false}" shared_mode="${6:-false}"
 
     require_codex
     ensure_dirs
@@ -398,6 +425,22 @@ cmd_delegate() {
 
 ${prompt}"
 
+    # CS-1: isolate this dispatch in its own git worktree by default so codex
+    # stages/commits into a private index on branch delegate/<id> — never the
+    # shared REPO_ROOT index. --shared opts out for read-only/analysis tasks.
+    # Fails OPEN to the shared checkout (old behavior) if worktree creation
+    # itself fails, so a git/disk hiccup never blocks a delegation.
+    local wt_path="" wt_branch=""
+    if [[ "$shared_mode" != "true" ]]; then
+        if wt_path="$(wt_create "$REPO_ROOT" "$id")" && [[ -n "$wt_path" ]]; then
+            wt_branch="delegate/${id}"
+            info "Worktree isolation: $wt_path (branch $wt_branch)"
+        else
+            wt_path=""
+            info "WARNING: worktree isolation unavailable — falling back to shared checkout for this dispatch"
+        fi
+    fi
+
     # Build command — codex exec is the correct headless subcommand
     local cmd=("$CODEX_BIN" "exec")
     [[ -n "$model_flag" ]] && cmd+=("-m" "$model_flag")
@@ -422,8 +465,16 @@ ${prompt}"
         # failure path (status update + quota capture) reachable AND the real exit
         # code intact, so the success/failure branches below are accurate.
         local exit_code=0
-        "${cmd[@]}" < /dev/null 2>&1 | tee "$output_file" || exit_code="${PIPESTATUS[0]}"
+        if [[ -n "$wt_path" ]]; then
+            ( cd "$wt_path" && "${cmd[@]}" < /dev/null ) 2>&1 | tee "$output_file" || exit_code="${PIPESTATUS[0]}"
+        else
+            "${cmd[@]}" < /dev/null 2>&1 | tee "$output_file" || exit_code="${PIPESTATUS[0]}"
+        fi
         quota_capture_from_output "$output_file" || true
+        # CS-1: hand back the isolated worktree's changes (patch + commit on
+        # its own delegate/<id> branch) and tear it down — regardless of
+        # exit_code, so a failed/partial run's edits are still captured.
+        cmd_worktree_handback "$id" "$wt_path" "$wt_branch"
         if [[ "$exit_code" -eq 0 ]]; then
             update_registry "$REGISTRY" "$id" "status" "done"
             audit_event_end "codex" "$id" "success" "0" "completed: ${prompt:0:100}" "$mode"
@@ -438,14 +489,19 @@ ${prompt}"
         local _coord_url="${HYBRID_COORDINATOR_URL:-http://127.0.0.1:8003}"
         local _prompt_brief; _prompt_brief="${prompt:0:100}"
         local _audit_helper; _audit_helper="$SCRIPT_DIR/lib/audit-post.sh"
+        # CS-1: cd into the isolated worktree (if any) before running codex,
+        # so it never sees the shared REPO_ROOT as its CWD.
+        local _cd_prefix=""
+        [[ -n "$wt_path" ]] && _cd_prefix="cd $(printf '%q' "$wt_path") && "
         # Background — write output to persistent file, not /tmp
         nohup bash -c "
             _start_ms=\$(date +%s%3N 2>/dev/null || echo 0)
-            $(printf '%q ' "${cmd[@]}") > \"$output_file\" 2>&1
+            ${_cd_prefix}$(printf '%q ' "${cmd[@]}") > \"$output_file\" 2>&1
             exit_code=\$?
             _end_ms=\$(date +%s%3N 2>/dev/null || echo 0)
             _lat=\$(( _end_ms - _start_ms ))
             \"$SCRIPT_PATH\" --internal-quota-capture \"$output_file\" >/dev/null 2>&1 || true
+            \"$SCRIPT_PATH\" --internal-worktree-handback $(printf '%q' "$id") $(printf '%q' "$wt_path") $(printf '%q' "$wt_branch") >/dev/null 2>&1 || true
             if [[ \$exit_code -eq 0 ]]; then
                 bash \"$_audit_helper\" \"$_coord_url\" task_completed codex success \"\$_lat\" \"$id\" \"completed: $_prompt_brief\" \"$mode\" \"$prompt\" \"$output_file\" || true
             else
@@ -486,8 +542,11 @@ MODE="safe"
 MODEL=""
 WAIT=false
 FORCE_QUOTA_RETRY=false
+SHARED_MODE=false
 SUBCMD=""
 SUBCMD_ARG=""
+WT_HB_PATH=""
+WT_HB_BRANCH=""
 
 while [[ $# -gt 0 ]]; do
     case "$1" in
@@ -497,10 +556,13 @@ while [[ $# -gt 0 ]]; do
         --model)       MODEL="$2";        shift 2 ;;
         --wait)        WAIT=true;         shift   ;;
         --force-quota-retry) FORCE_QUOTA_RETRY=true; shift ;;
+        --shared|--no-worktree) SHARED_MODE=true; shift ;;
         --check)       SUBCMD="check";   SUBCMD_ARG="$2"; shift 2 ;;
         --status)      SUBCMD="status";  SUBCMD_ARG="$2"; shift 2 ;;
         --list)        SUBCMD="list";    shift   ;;
         --internal-quota-capture) SUBCMD="quota-capture"; SUBCMD_ARG="$2"; shift 2 ;;
+        --internal-worktree-handback)
+            SUBCMD="worktree-handback"; SUBCMD_ARG="$2"; WT_HB_PATH="$3"; WT_HB_BRANCH="$4"; shift 4 ;;
         --help|-h)     grep "^#" "$0" | sed 's/^# \?//'; exit 0 ;;
         *) die "Unknown option: $1 (run with --help for usage)" ;;
     esac
@@ -520,12 +582,17 @@ case "${SUBCMD:-}" in
     # the script itself to run quota_capture_from_output after the run
     # completes. Not part of the public CLI surface.
     quota-capture) ensure_dirs; quota_capture_from_output "$SUBCMD_ARG" ;;
+    # Internal re-entry point (CS-1): background codex runs finish inside a
+    # detached nohup bash -c that cannot see this script's functions, so it
+    # re-invokes the script itself to hand back the isolated worktree once
+    # codex has fully exited. Not part of the public CLI surface.
+    worktree-handback) ensure_dirs; cmd_worktree_handback "$SUBCMD_ARG" "$WT_HB_PATH" "$WT_HB_BRANCH" ;;
     "")
         if [[ -n "$PROMPT_FILE" ]]; then
             [[ -f "$PROMPT_FILE" ]] || die "Prompt file not found: $PROMPT_FILE"
             PROMPT="$(cat "$PROMPT_FILE")"
         fi
         [[ -n "$PROMPT" ]] || die "Provide --prompt TEXT or --prompt-file FILE (or --help)"
-        cmd_delegate "$PROMPT" "$MODE" "$MODEL" "$WAIT" "$FORCE_QUOTA_RETRY"
+        cmd_delegate "$PROMPT" "$MODE" "$MODEL" "$WAIT" "$FORCE_QUOTA_RETRY" "$SHARED_MODE"
         ;;
 esac
diff --git a/scripts/ai/delegate-to-local b/scripts/ai/delegate-to-local
index da8690013d18d59592adef023ca353155cbda942..d72b7da5fb7259c393a6f291adcecf0f6bfec857 100755
--- a/scripts/ai/delegate-to-local
+++ b/scripts/ai/delegate-to-local
@@ -37,11 +37,23 @@
 #   --no-consult       Skip the router/claim consult gate before real dispatch
 #                       (also: AQ_NO_CONSULT=1). Read-only subcommands above
 #                       never consult regardless of this flag.
+#   --shared, --no-worktree
+#                       Run agent-mode dispatches in the SHARED checkout
+#                       (REPO_ROOT) instead of an isolated git worktree.
+#                       Default is ISOLATED: a dispatch that resolves to
+#                       --mode agent (explicit, or "auto" classified as agent)
+#                       runs aq-agent-loop in its own git worktree on branch
+#                       delegate/<task-id> and hands the result back as a
+#                       patch (.agents/delegation/outputs/<id>.patch) plus a
+#                       commit on that branch — never staging/committing into
+#                       the shared REPO_ROOT index. direct/hybrid/ralph modes
+#                       never touch files, so isolation is a no-op for them.
 #   --help             Show this help
 
 set -euo pipefail
 
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
+SCRIPT_PATH="$SCRIPT_DIR/$(basename "${BASH_SOURCE[0]}")"
 REPO_ROOT="$(cd "$SCRIPT_DIR/../.." && pwd)"
 DELEGATION_DIR="$REPO_ROOT/.agents/delegation"
 LIB_DIR="$SCRIPT_DIR/lib"
@@ -54,6 +66,8 @@ source "$LIB_DIR/trace-seed.sh" 2>/dev/null || true
 # Phase 56.1/56.7 — delegation audit bridge
 # shellcheck disable=SC1091
 source "${LIB_DIR}/audit-write.sh" 2>/dev/null || true
+# CS-1 — worktree isolation (wt_create/wt_handback/wt_teardown/wt_tag_registry)
+source "${LIB_DIR}/worktree-isolation.sh" 2>/dev/null || true
 
 # Ports — read from env, fall back to harness defaults (source of truth: options.nix)
 LLAMA_PORT="${LLAMA_PORT:-8080}"
@@ -105,6 +119,10 @@ TASK_TYPE=""  # Phase 173: "research"|"analysis"|"prd" raises stagnation read li
 SUBCMD=""
 SUBCMD_ARG=""
 REPAIR_STALE_APPLY=false
+# CS-1: default ON — isolate agent-mode dispatches in a private git worktree.
+SHARED_MODE=false
+WT_HB_PATH=""
+WT_HB_BRANCH=""
 # Resolution B (DISPATCH-INTEGRATION-DESIGN.md amendment): dispatch.py stays
 # frozen (L2B golden-manifest pin) — the router/claim consult seam lives here
 # in the shim instead. Escape hatch for callers that must bypass it (e.g. the
@@ -135,6 +153,11 @@ while [[ $# -gt 0 ]]; do
         --cancel)        SUBCMD="cancel";        SUBCMD_ARG="${2:-}"; shift; [[ $# -gt 0 ]] && shift ;;
         --kill-all)      SUBCMD="kill-all";      shift   ;;
         --no-consult)    NO_CONSULT=true;        shift   ;;
+        --shared|--no-worktree) SHARED_MODE=true; shift  ;;
+        --internal-worktree-handback)
+            SUBCMD="worktree-handback"
+            SUBCMD_ARG="${2:-}"; WT_HB_PATH="${3:-}"; WT_HB_BRANCH="${4:-}"
+            shift 4 ;;
         --help|-h)     grep "^#" "$0" | sed 's/^# \?//'; exit 0 ;;
         *) die "Unknown option: $1 (run with --help for usage)" ;;
     esac
@@ -195,6 +218,18 @@ case "${SUBCMD:-}" in
     kill-all)
         python3 "$DISPATCH" kill-all --delegation-dir "$DELEGATION_DIR"
         ;;
+    # Internal re-entry point (CS-1): a background agent-mode dispatch is
+    # detached via setsid and this shim's own process exits right after
+    # launch, so the worktree handback+teardown for that case is wrapped
+    # inside the backgrounded command itself, which re-invokes this script
+    # to run after dispatch.py has fully exited. Not part of the public CLI.
+    worktree-handback)
+        ensure_dirs
+        wt_handback "$REPO_ROOT" "$WT_HB_PATH" "$SUBCMD_ARG" "$DELEGATION_DIR/outputs"
+        wt_tag_registry "$DELEGATION_DIR/registry.jsonl" "$SUBCMD_ARG" "$WT_HB_PATH" "$WT_HB_BRANCH" \
+            "$DELEGATION_DIR/outputs/${SUBCMD_ARG}.patch"
+        wt_teardown "$REPO_ROOT" "$WT_HB_PATH"
+        ;;
     "")
         # Resolve prompt from file if needed
         if [[ -n "$PROMPT_FILE" ]]; then
@@ -252,6 +287,47 @@ sys.exit(1)
 
         audit_event_start "local" "$ID" "${PROMPT:0:200}"
 
+        # ── CS-1: worktree isolation for agent-mode dispatches ──────────────
+        # aq-agent-loop is the only mode that reads/writes repo files and can
+        # shell out to git (file_operations.py / shell_tools.py / git_tools.py
+        # all resolve relative paths and repo-root git ops from the process
+        # CWD / the running script's own on-disk location — see DESIGN.md).
+        # direct/hybrid/ralph never touch the working tree, so isolation is
+        # skipped for them. "auto" is resolved here with dispatch.py's own
+        # classify_mode() (imported, not reimplemented) so the common
+        # (default) auto-routed agent dispatch still gets isolated — the
+        # decision has to be made before invoking dispatch.py because the
+        # worktree-local --script-dir override must be in the argv below.
+        EFFECTIVE_SCRIPT_DIR="$SCRIPT_DIR"
+        WT_PATH=""
+        WT_BRANCH=""
+        if [[ "$SHARED_MODE" != "true" ]]; then
+            _EFFECTIVE_MODE="$MODE"
+            if [[ "$_EFFECTIVE_MODE" == "auto" ]]; then
+                _EFFECTIVE_MODE="$(PYTHONPATH="$LIB_DIR" python3 -c '
+import sys
+from dispatch import classify_mode
+print(classify_mode(sys.argv[1]))
+' "$PROMPT" 2>/dev/null || echo "auto")"
+            fi
+            if [[ "$_EFFECTIVE_MODE" == "agent" ]]; then
+                if WT_PATH="$(wt_create "$REPO_ROOT" "$ID")" && [[ -n "$WT_PATH" ]]; then
+                    WT_BRANCH="delegate/${ID}"
+                    # Point AgentRunner at the WORKTREE'S OWN copy of
+                    # scripts/ai/aq-agent-loop: its __file__-derived
+                    # _REPO_ROOT (and therefore the local-agents tool
+                    # modules it imports, including git_tools.py's
+                    # find-.git-from-__file__ repo-root resolution) then
+                    # resolves to the worktree, not the shared REPO_ROOT.
+                    EFFECTIVE_SCRIPT_DIR="$WT_PATH/scripts/ai"
+                    info "Worktree isolation: $WT_PATH (branch $WT_BRANCH)"
+                else
+                    WT_PATH=""
+                    info "WARNING: worktree isolation unavailable — falling back to shared checkout for this dispatch"
+                fi
+            fi
+        fi
+
         # Build dispatch.py argument list (bash array preserves quoting for prompts
         # containing spaces/special chars — never interpolate via printf '%q').
         _DISPATCH_ARGS=(
@@ -267,13 +343,29 @@ sys.exit(1)
             --llama-url      "http://127.0.0.1:${LLAMA_PORT}"
             --hybrid-url     "http://127.0.0.1:${HYBRID_PORT}"
             --ralph-url      "http://127.0.0.1:${RALPH_PORT}"
-            --script-dir     "$SCRIPT_DIR"
+            --script-dir     "$EFFECTIVE_SCRIPT_DIR"
         )
         [[ -n "$TASK_TYPE" ]] && _DISPATCH_ARGS+=(--task-type "$TASK_TYPE")
 
         if [[ "$WAIT" == "true" ]]; then
             info "Running in blocking mode (timeout: ${TIMEOUT}s)..."
-            if "${_DISPATCH_ARGS[@]}"; then
+            DISPATCH_OK=true
+            if [[ -n "$WT_PATH" ]]; then
+                ( cd "$WT_PATH" && "${_DISPATCH_ARGS[@]}" ) || DISPATCH_OK=false
+            else
+                "${_DISPATCH_ARGS[@]}" || DISPATCH_OK=false
+            fi
+            # CS-1: hand back the isolated worktree (patch + commit on its own
+            # delegate/<id> branch) and tear it down — regardless of outcome,
+            # so a failed/partial run's edits are still captured. No-op when
+            # no worktree was created (shared-checkout / non-agent dispatch).
+            if [[ -n "$WT_PATH" ]]; then
+                wt_handback "$REPO_ROOT" "$WT_PATH" "$ID" "$DELEGATION_DIR/outputs"
+                wt_tag_registry "$DELEGATION_DIR/registry.jsonl" "$ID" "$WT_PATH" "$WT_BRANCH" \
+                    "$DELEGATION_DIR/outputs/${ID}.patch"
+                wt_teardown "$REPO_ROOT" "$WT_PATH"
+            fi
+            if [[ "$DISPATCH_OK" == "true" ]]; then
                 audit_event_end "local" "$ID" "success" "0" \
                     "completed mode=${MODE}: ${PROMPT:0:100}" "$MODE"
                 audit_save_session "local-${MODE}" "$ID" "$PROMPT" "$OUTPUT_FILE"
@@ -303,10 +395,24 @@ sys.exit(1)
             # died, and its aq-agent-loop child then orphaned (ppid=1) → the self-watchdog
             # killed a task that had barely started. setsid detaches it (own session leader,
             # no controlling terminal) — the ROOT fix for the orphaned/self-terminated dispatches.
-            setsid "${_DISPATCH_ARGS[@]}" > /dev/null 2>&1 &
+            #
+            # CS-1: when isolated, wrap the same setsid dispatch inside a
+            # bash -c string that (a) cd's into the worktree first so
+            # aq-agent-loop's CWD-relative file ops land there, and (b) once
+            # dispatch.py has fully exited, re-invokes this script's own
+            # --internal-worktree-handback re-entry point (this shim's own
+            # process already returned by then — that re-entry point is what
+            # performs the patch/branch handback + teardown for the
+            # background-mode case).
+            if [[ -n "$WT_PATH" ]]; then
+                setsid bash -c "cd $(printf '%q' "$WT_PATH") && $(printf '%q ' "${_DISPATCH_ARGS[@]}"); $(printf '%q' "$SCRIPT_PATH") --internal-worktree-handback $(printf '%q' "$ID") $(printf '%q' "$WT_PATH") $(printf '%q' "$WT_BRANCH") >/dev/null 2>&1 || true" > /dev/null 2>&1 &
+            else
+                setsid "${_DISPATCH_ARGS[@]}" > /dev/null 2>&1 &
+            fi
             BG_PID=$!
             disown "$BG_PID"
             info "Task $ID started in background (pid $BG_PID)"
+            [[ -n "$WT_PATH" ]] && info "Isolated worktree: $WT_PATH (branch $WT_BRANCH)"
             info "Check output:  delegate-to-local --check $ID"
             info "Check status:  delegate-to-local --status $ID"
             info "NOTE: Local model tasks take 90-300s. Check back in a few minutes."
diff --git a/scripts/ai/lib/worktree-isolation.sh b/scripts/ai/lib/worktree-isolation.sh
new file mode 100644
index 0000000000000000000000000000000000000000..3f5c27d36197440cc97a598439aade795661a4b9
--- /dev/null
+++ b/scripts/ai/lib/worktree-isolation.sh
@@ -0,0 +1,128 @@
+#!/usr/bin/env bash
+# lib/worktree-isolation.sh — CS-1 coordination-safety: per-dispatch git
+# worktree isolation for delegate-to-codex / delegate-to-local.
+#
+# Root cause fixed (.agents/plans/coordination-safety-worktree-isolation/DESIGN.md):
+# both delegate scripts used to run the implementer agent directly in the
+# SHARED checkout (REPO_ROOT), so it staged/committed into the same git index
+# as the orchestrator and every other concurrent lane -> recurring
+# concurrent-index contention (Rule 19 root-cause, observed 3x).
+#
+# Fix: every implementer dispatch runs in its own throwaway `git worktree` on
+# a private branch (delegate/<task-id>), NEVER in the shared REPO_ROOT index.
+# On completion the caller hands the result back as a patch file
+# (.agents/delegation/outputs/<task-id>.patch) and a commit on the
+# delegate/<task-id> branch, then tears the worktree down (Rule 12: the
+# worktree directory is disposable scratch — the patch + branch are the
+# preserved evidence, so `git worktree remove` here is the designed teardown
+# primitive, not a content deletion). Only the orchestrator ever commits to
+# main.
+#
+# Source this file from delegate-to-* scripts:
+#   source "$(dirname "${BASH_SOURCE[0]}")/lib/worktree-isolation.sh"
+#
+# All functions are `set -e` safe (no bare failing command can kill the
+# calling script) and degrade to "no worktree info" rather than aborting a
+# delegation — worktree isolation is a safety improvement, not a new single
+# point of failure.
+#
+# Functions:
+#   wt_create       <repo_root> <task_id>
+#       Creates .agents/delegation/worktrees/<task_id> on branch
+#       delegate/<task_id> from HEAD. Echoes the worktree path and returns 0
+#       on success; returns 1 (nothing echoed) on failure so the caller can
+#       fall back to the shared checkout.
+#   wt_handback     <repo_root> <worktree_path> <task_id> <outputs_dir>
+#       Stages all changes in the worktree, writes the staged diff to
+#       <outputs_dir>/<task_id>.patch (empty file if no changes), and commits
+#       them to the worktree's own delegate/<task_id> branch (never main).
+#   wt_teardown     <repo_root> <worktree_path>
+#       Removes the worktree working directory via `git worktree remove`.
+#       The delegate/<task_id> branch and the handed-back patch file are
+#       preserved.
+#   wt_tag_registry <registry_file> <task_id> <worktree_path> <branch> <patch_file>
+#       Records worktree/branch/patch location on the matching registry.jsonl
+#       entry (flock-guarded read-modify-write) so --status/--check reflect
+#       where the isolated work landed. No-op if the entry isn't found yet.
+
+# wt_create <repo_root> <task_id>
+wt_create() {
+    local repo_root="$1" task_id="$2"
+    local wt_root="$repo_root/.agents/delegation/worktrees"
+    local wt_path="$wt_root/$task_id"
+    local branch="delegate/$task_id"
+    mkdir -p "$wt_root" 2>/dev/null || true
+    if git -C "$repo_root" worktree add -q -b "$branch" "$wt_path" HEAD >/dev/null 2>&1; then
+        echo "$wt_path"
+        return 0
+    fi
+    return 1
+}
+
+# wt_handback <repo_root> <worktree_path> <task_id> <outputs_dir>
+wt_handback() {
+    local repo_root="$1" wt_path="$2" task_id="$3" outputs_dir="$4"
+    [[ -n "$wt_path" && -d "$wt_path" ]] || return 0
+    mkdir -p "$outputs_dir" 2>/dev/null || true
+    local patch_file="$outputs_dir/${task_id}.patch"
+    git -C "$wt_path" add -A >/dev/null 2>&1 || true
+    if git -C "$wt_path" diff --cached --quiet 2>/dev/null; then
+        # No changes — still write an (empty) patch as evidence the dispatch
+        # ran cleanly in isolation with nothing to hand back.
+        : > "$patch_file" 2>/dev/null || true
+        return 0
+    fi
+    git -C "$wt_path" diff --cached > "$patch_file" 2>/dev/null || true
+    # Commit to the worktree's OWN delegate/<task-id> branch only — this is
+    # never `main` and never the shared REPO_ROOT index (a separate git
+    # worktree has its own index). The orchestrator reviews the patch/branch
+    # and performs the single main commit.
+    git -C "$wt_path" \
+        -c user.email="delegate-bot@harness.local" \
+        -c user.name="delegate-bot" \
+        commit -q -m "delegate: ${task_id}" >/dev/null 2>&1 || true
+    return 0
+}
+
+# wt_teardown <repo_root> <worktree_path>
+wt_teardown() {
+    local repo_root="$1" wt_path="$2"
+    [[ -n "$wt_path" ]] || return 0
+    git -C "$repo_root" worktree remove --force "$wt_path" >/dev/null 2>&1 || true
+    git -C "$repo_root" worktree prune >/dev/null 2>&1 || true
+}
+
+# wt_tag_registry <registry_file> <task_id> <worktree_path> <branch> <patch_file>
+wt_tag_registry() {
+    local registry="$1" tid="$2" wt="$3" branch="$4" patch="$5"
+    [[ -f "$registry" ]] || return 0
+    python3 - "$registry" "$tid" "$wt" "$branch" "$patch" <<'PYEOF' 2>/dev/null || true
+import fcntl, json, sys
+rf, tid, wt, branch, patch = sys.argv[1:6]
+with open(rf, "r+") as fh:
+    fcntl.flock(fh, fcntl.LOCK_EX)
+    try:
+        fh.seek(0)
+        raw = fh.read()
+        lines_out = []
+        for line in raw.splitlines():
+            line = line.strip()
+            if not line:
+                continue
+            try:
+                e = json.loads(line)
+                if e.get("id") == tid:
+                    e["worktree"] = wt
+                    e["worktree_branch"] = branch
+                    if patch:
+                        e["patch_file"] = patch
+                lines_out.append(json.dumps(e))
+            except Exception:
+                lines_out.append(line)
+        fh.seek(0)
+        fh.truncate()
+        fh.write("\n".join(lines_out) + ("\n" if lines_out else ""))
+    finally:
+        fcntl.flock(fh, fcntl.LOCK_UN)
+PYEOF
+}
