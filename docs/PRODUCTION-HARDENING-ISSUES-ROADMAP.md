# Production Hardening Issues Roadmap (K3s-only)

The stack now runs on K3s. Hardening tasks should target:
- Kubernetes manifests in `ai-stack/kubernetes/`
- Kustomize overlays in `ai-stack/kustomize/`
- Cluster policies and secrets

Use `DEPLOYMENT.md` for current operational guidance.

