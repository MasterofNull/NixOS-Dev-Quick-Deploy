---
# Default deny all ingress and egress traffic in ai-stack namespace.
# All traffic must be explicitly allowed by other NetworkPolicy resources.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: ai-stack
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
