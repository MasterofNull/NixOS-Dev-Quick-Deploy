apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: ai-stack-allow-internal
  namespace: ai-stack
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector: {}
  egress:
    - to:
        - podSelector: {}
