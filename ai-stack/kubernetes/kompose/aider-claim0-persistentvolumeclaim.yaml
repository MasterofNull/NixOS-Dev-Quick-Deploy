apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: aider-claim0
  name: aider-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
