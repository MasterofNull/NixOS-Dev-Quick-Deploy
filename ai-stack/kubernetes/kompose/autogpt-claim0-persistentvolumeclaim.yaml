apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: autogpt-claim0
  name: autogpt-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
