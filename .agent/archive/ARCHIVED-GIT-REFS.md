# archived git refs

- **archive/backup-pre-recommit-20260807** → 749a28bf (tagged 2026-08-07). Pre-48d92962-recommit safety backup; the re-commit attempt was reverted, so this is the known-good state before it. Preserved per Rule 12 (archive, never delete). Restore: `git checkout archive/backup-pre-recommit-20260807`.
