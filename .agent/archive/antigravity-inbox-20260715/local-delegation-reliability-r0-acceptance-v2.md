# Antigravity Acceptance Task: Local Delegation Reliability R0 v2

Role: independent flagship architecture, security, SRE, and eval acceptance reviewer. **Read only.**

This is a post-remediation implementation acceptance. Review the latest bytes of the exact seven-file
R0 inventory:

1. `.agent/PROJECT-LOCAL-DELEGATION-RELIABILITY-PRD.md`
2. `.agent/PROJECT-LOCAL-DELEGATION-RELIABILITY-PLAN.md`
3. `config/schemas/local-delegation-runtime-policy.schema.json`
4. `config/local-delegation-runtime-policy.json`
5. `scripts/ai/lib/local_delegation_reliability.py`
6. `scripts/testing/fixtures/local-delegation-reliability-golden.json`
7. `scripts/testing/test-local-delegation-reliability.py`

The first implementation acceptance found: incomplete metrics, duplicate TERM, sequential rather
than concurrent CAS evidence, incomplete closed-schema enforcement, caller-asserted contract health,
weak substring characterizations, absent stable digests, and incomplete subprocess/state hermeticity.
Verify each is substantively repaired rather than hidden by self-consistent assertions.

Also verify:

- exact seven-file R0 boundary and no live adoption/L2B-A overlap;
- full D1–D11 live-source manifest, regex/count/block evidence, and stable vector/manifest/fixed-result
  digests generated by the pure module;
- idempotent TERM/KILL and exactly one terminal publication;
- adversarial 1,000-mutation CAS, fencing, old/new inode, PID reuse, OOM, and telemetry tamper cases;
- closed Draft-2020-12 policy validation and undeclared-env rejection;
- hermetic subprocess caps and before/after source/state evidence;
- low-cardinality monitoring vocabulary required by the PRD;
- the new R0M monitoring prerequisite prevents live R1 work from using untraceable agent routes.

Run `python3 scripts/testing/test-local-delegation-reliability.py`. Return `PASS` or
`REQUEST_REVISION` with exact file/line findings. Explicitly state whether R0 is accepted, whether R0M
must precede R1, and whether R1–R4 remain unauthorized.

Write `.agents/plans/local-delegation-reliability-r0/antigravity-acceptance-v2.md`, then complete this
inbox item with `scripts/ai/aq-antigravity-inbox complete`. Do not edit, stage, commit, deploy, or
invoke local inference.
