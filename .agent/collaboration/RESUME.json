{
  "current_objective": "Phase 107.3 complete — stream_options.include_usage + all SSE parse sites fixed",
  "phase": "active",
  "todo_snapshot": [
    "DONE: Phase 106.1 — checkpoint tmpfiles + async qdrant upsert (0a099081) — LIVE",
    "DONE: Phase 107.1 — token_usage event emission per delegation (31428fd2) — LIVE",
    "DONE: Phase 107.2 — SSE body reconstruction initial_body+body sites (61040dfb) — LIVE",
    "DONE: Phase 107.3 — stream_options.include_usage + local fallback SSE (d7919601)",
    "PENDING-REBUILD: d7919601 — stream_options + local fallback fix needs restart"
  ],
  "uncommitted_changes": [],
  "resume_hint": "Phase 107.3 complete. Root cause chain fully addressed: (1) switchboard forces stream=True for local, (2) SSE reconstruction now applied at all 3 parse sites, (3) stream_options.include_usage ensures llama.cpp includes usage in final SSE chunk. Needs nixos-rebuild switch. After rebuild: smoke delegation + aq-report should show useful_token_ratio populated."
}
