# Owner-directed orchestrator activation

The owner explicitly appointed Codex orchestrator after the CS-1 integration
quiet window. This supersedes earlier documents assigning that seat to Claude;
it does not terminate Claude's session or invalidate authored work.

## Execution and integration ownership

Codex sequences slices, assigns bounded owners, records terminal dispositions and
coordinates the main integration window. Claude contributes bounded plans,
implementation and independent review. Gemini/Antigravity contributes through the
existing claim-bound watched inbox. Local contributes measured dogfood and eligible
tasks, not assumed review credit. Independent acceptance is an eligible non-author
lane, not permanently a particular provider.

Until the enforced CS-3 integration fence lands, only the designated integrator
may stage/commit the shared main checkout. Implementers preserve private branch/
patch evidence; no shared HEAD switching or foreign staging. Local Agent tool
subagents in this session share the checkout: do not assume automatic isolation.
Separate file claims continue protecting overlapping edits. This is a cooperative
temporary boundary, not a claim that machine enforcement already exists.

## Bounded queue

1. Confirmatory audit of landed CS-1 e8a905e8; fix critical isolation/handback
   defects in a distinct slice, not a rewrite of its history.
2. Finish preserved local-latency visibility F1: narrow receipt-parser ValueError
   handling, fixture, exact non-author review, stable staged gate and atomic commit.
3. CS-2: all editing-lane isolation parity and instruction projection through the
   existing canonical authority; do not clobber unreconciled provider worktree edits.
4. CS-3: machine-enforced trunk integration with baseline/subject-bound lease,
   bounded acquisition, safe interruption and no blind stale-lease stealing.
5. CS-4: concurrent-dispatch/failure/recovery evidence proving main/index preservation
   and handback retention, with QA and operator visibility.
6. Local inference: obtain required protected-producer timing authority, distinguish
   queue/prefill/first-content time, make one measured fix, then bounded coding dogfood.
7. Native ECC: resolve P0-B ownership; reuse source-grounded P0-C event seam;
   converge P0-E with existing FT templates. Action pinning alone is not P0-E done.

Claude planning and Gemini advisory use the same systems/security/implementation/QA
baseline. Local's corresponding input is deferred explicitly: the unchanged-profile
READY repeat failed after402.2s with zero output. No blind retry or fictional review.
Absent-lane input follows the existing catch-up mechanism and is nonblocking unless
it reveals a concrete critical defect. Planning freezes after one batch/synthesis;
implementation produces a terminal disposition and distinct bounded follow-ups.

## Boundaries

This role change grants orchestration of the agreed dev path, not deletion,
forced worktree teardown, secret access, remote account changes, firmware/boot
changes, push or system activation. Preserve unrelated dirty work and the existing
sleep/hibernate behavior. All source acceptance still requires truthful evidence,
independent review where mandated, QA/dashboard delivery and atomic commit gates.
