# Independent confirmatory re-review — evidence scope v3

Reviewed subject: `a3dbee29..review/evidence-scope-fix-v3` (`8aefee7dcec5fd35f2878c0886d3f176b51216c6`)

Diff identity: `git diff de84c9e3..review/evidence-scope-fix-v3 | sha256sum` → `636dc1818223dc83994b88028f43eaee0490638da4ea86646e7299f998b2e820  -`

1. **PASS — CRITICAL closure confirmed** (`templates/factory-gate-bundle/gate-runner:339-365,418-440,481-507`). `_read_once()` opens each receipt-listed check with `O_RDONLY|O_NOFOLLOW`, then obtains its type/mode with `fstat()` and its bytes with `read()` through that same descriptor. The exact returned buffer is written to the private snapshot and hashed against the receipt; execution subsequently globs only `${SNAPSHOT_DIR}/checks.d`. Exact attack input → outcome: replace `checks.d/hard-50-lint.sh` with a symlink before `open()` → `O_NOFOLLOW` refuses it; replace the file or its parent directory after `open()` → the open file description continues yielding the selected inode and the resulting buffer is both validated and executed; substitute non-receipt bytes before `open()` → their digest mismatches and the runner exits before any check executes. There is no canonical-check reopen between the authoritative read and execution. The earlier static-injection and mutable re-glob attacks remain closed.

2. **PASS — ADVISORY residual risk A is acceptable under the declared trust model** (`templates/factory-gate-bundle/gate-runner:82-125,368-399,560-608`). The execution path now hashes and parses the same `manifest_bytes`; only the metadata-only preflight and post-run defense-in-depth copies retain separate manifest hash/parse reads. Exact attack input → outcome: swap `MANIFEST.json` after its hash read to add or remove a check name → `expected == actual == receipt_expected` fails unless the parsed name set remains identical, in which case execution scope is unchanged; check bytes must still match receipt records. The receipt is parsed from one `read_text()` result, not hash-read and then parse-read. Replacing the receipt itself together with matching manifest/check records could mint a forged set, but that is direct replacement of the declared trust anchor and succeeds without this race; it is not a residual validate/copy TOCTOU introduced or left open by v3. No pre-landing change is required for this slice.

3. **PASS — INFO residual risk B is not a new production privilege boundary** (`templates/factory-gate-bundle/gate-runner:443-456`). Exact attack input → outcome: leave `FACTORY_GATE_POST_SNAPSHOT_HOOK` unset → no subprocess is started; set it to a non-executable path → no subprocess is started; set it to an executable → it is invoked directly as one argv element, without a shell, and both stdout and stderr are redirected to `/dev/null`, so normal hook output cannot contaminate `SNAPSHOT_DIR` command-substitution capture. A caller able to supply this environment variable can already select executable code in the runner's process environment and has equivalent code-execution authority through existing `FACTORY_*` seams; the test seam does not elevate trust.

4. **PASS — mode rejection is complete for the specified POSIX mode-bit class** (`templates/factory-gate-bundle/gate-runner:73-79,333-336,437-440,554-590`; `scripts/ai/lib/factory_gate_install.py:494-526`). `(mode & 0o7000) == 0` rejects setuid, setgid, and sticky, while `(mode & 0o022) == 0` rejects group/world write. Exact inputs → outcomes: `0777`, `4755`, `2755`, `6755`, and `0666` are rejected before execution; `0750` retains the receipt's owner-exec class and passes as permitted umask noise. Owner-exec drift remains rejected by `_git_mode`; no requested mode-bit class is omitted.

5. **PASS — regressions remain covered** (`scripts/testing/test-factory-gate-readiness.py:460-515,551-583,585-609,611-676`; `templates/factory-gate-bundle/gate-runner:531-640`). Exact attack inputs → outcomes: injected manifest/check without receipt provenance is refused before execution; a source check swapped after snapshot cannot fire its sentinel; a receipt-recorded check with mismatching static bytes is refused before any check runs; the post-run canonical-scope validation remains present and fail-closes evidence recording. The focused readiness fixture passed with all 27 evidence flags true, including `injected_check_refused_before_execution`, `toctou_execution_uses_verified_snapshot`, `special_and_group_write_mode_rejected`, `boundary_mismatch_source_refused_before_execution`, and `boundary_snapshot_first_hook_proof`. Streamed syntax checks also passed for the reviewed Bash runner and both reviewed Python files.

No blocking findings. The v3 implementation closes the previously reported validate/copy TOCTOU by construction and corrects the mode over-normalization. The orchestrator can land commit `8aefee7d`.

VERDICT: PASS — implementation satisfies all acceptance criteria
