# Independent confirmatory re-review: evidence-scope TOCTOU v2

VERDICT: REQUEST_REVISION

Reviewed subject: `737bb428..review/evidence-scope-fix-v2` (`a3dbee29f921dd444113d1c1c8ecfe440bd648d0`)

Delta identity: `git diff de84c9e3..review/evidence-scope-fix-v2 | sha256sum` = `0bb8dfc231cfbb8affff40cf57320deabab37ae62d7df94d0d48754561017bba  -`

1. **HIGH — the validation-to-snapshot TOCTOU remains open** (`templates/factory-gate-bundle/gate-runner:343-346,367-390`). `canonical_scope()` hashes each canonical mutable pathname with `read_bytes()` at line 345, returns, and the snapshot loop later reopens the same pathname with a second `src.read_bytes()` at line 389. Those are not the same verified bytes or the same already-open descriptor. Exact attack input -> outcome: begin with a receipt-bound benign executable `checks.d/hard-50-lint.sh`; after its line-345 hash read succeeds, atomically replace that pathname with an ordinary executable payload such as `#!/usr/bin/env sh\ntouch /tmp/sentinel\n`; let line 389 copy the replacement; restore the original receipt-bound bytes before the post-run scope check at lines 477-522. The private snapshot contains and executes the unverified payload at line 430, the sentinel is created, and the restored repository can pass the post-run provenance check. The snapshot therefore closes only the later-check re-glob race, not the prescribed verification/copy race.

   Bounded follow-up: read each check once into bytes (or retain an already-open descriptor), validate the digest and mode associated with that read, and populate/execute the snapshot from those exact bytes without reopening the canonical pathname. A snapshot populated first and then receipt-validated from the snapshot bytes is also sufficient if execution is coupled to that validated object.

2. **MEDIUM — `_git_mode()` suppresses security-relevant mode drift, not only umask noise** (`scripts/ai/lib/factory_gate_install.py:480-502`; `templates/factory-gate-bundle/gate-runner:313-346,469-505`; `scripts/testing/test-factory-gate-readiness.py:514-545`). Exact attack inputs -> outcomes: changing a receipt-recorded executable check from `0755` to `0777`, `2755`, `4755`, or `6755` still normalizes to `0755`; unchanged content therefore passes mode provenance (and `0777` also passes the separate executable check), despite becoming group/world-writable or gaining special bits. Changing a non-executable managed file from `0644` to `0666`, `2644`, or `4644` likewise normalizes to `0644`. A check changed from executable to non-executable is still caught by both normalization and `os.access(..., X_OK)`, but setuid/setgid/sticky and group/world-write changes are no longer detected. The regression test exercises `0640` (which is group-readable, not group-writable) and owner-exec drift only, so it cannot support the broader safety claim.

   Bounded follow-up: retain owner-exec normalization for Git portability but independently reject security-sensitive bits (at minimum `mode & 0o7000` and `mode & 0o022`) in every canonical-scope copy and installer-side record comparison; add explicit `0777`/special-bit negative fixtures.

3. **INFO — sibling symlinks preserve an existing unbound helper exposure; they do not create a new reachable trust boundary** (`templates/factory-gate-bundle/gate-runner:373-393`; `templates/factory-gate-bundle/checks.d/hard-10-repo-structure.sh:3-4`). Exact attack input -> outcome: replace the live `scripts/governance/repo-structure-lint` sibling after check-scope validation; the snapshotted `hard-10-repo-structure.sh` resolves `../repo-structure-lint` through the snapshot symlink and executes the replacement. Before this change, the same receipt-bound check executed that same mutable sibling directly from the live bundle, and arbitrary receipt-bound checks could already resolve any `../<sibling>` path. Thus the exposure is real and remains unbound, but is not newly reachable because of the snapshot layout. The “no new trust boundary” claim is correct in that limited comparative sense.

4. **MEDIUM — the race regression test is not proof of the claimed closure** (`scripts/testing/test-factory-gate-readiness.py:457-512`). Its mutator sleeps 0.5 seconds and swaps `hard-50-lint.sh` while a snapshotted `hard-10-*` is executing for two seconds. A missing sentinel proves that the execution loop no longer reopens the repository path after snapshot construction. It never synchronizes a swap between line-345 validation and line-389 snapshot copying, which is the surviving defect. It is also timing-sensitive: a sufficiently delayed validation/snapshot phase can move the 0.5-second mutation into construction and make the fixture fail or exercise a different race. Keep this test for the re-glob regression, but add a deterministic validation/copy-boundary test (for example, a controlled hook/barrier in a test-only copy primitive or a snapshot-first/validate-snapshot design that removes that boundary by construction).

5. **INFO — no regression found in the original static-injection refusal or defense-in-depth post-run check** (`templates/factory-gate-bundle/gate-runner:296-401,454-553`; `scripts/testing/test-factory-gate-readiness.py`, injected/symlinked inventory fixtures). A pre-existing added `checks.d/*.sh`, symlinked check, substituted manifest, external scope, or receipt/hash mismatch is still refused before the check loop. The canonical repository scope is still checked after execution. Those controls do not cure Finding 1 because a transient replacement can be copied and executed, then restored before the post-run read.

Validation evidence: `bash -n templates/factory-gate-bundle/gate-runner` passed; `python3 -m py_compile scripts/ai/lib/factory_gate_install.py scripts/testing/test-factory-gate-readiness.py` passed; the complete `test-factory-gate-readiness.py` fixture passed from an isolated checkout of `a3dbee29`, including its current TOCTOU and mode assertions. That green result is consistent with Findings 1, 2, and 4 because the current fixtures do not exercise those attack inputs.
