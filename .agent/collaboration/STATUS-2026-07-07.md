# Session Status Checkpoint — 2026-07-07

Owner: hyperd · Orchestrator: claude-opus-4-8 · Purpose: checkpoint before re-entering the canonical
agentic workflow (intent → fan-out → expert-team consensus → PRD → plan).

## Where we are (foundation-hardening epic)
Origin: the 4/4-ratified factory critique said the collaborative-factory core was "too ad-hoc → promote to
explicit primitives." Roadmap = F1 → F2 → F3. Then a model-requirements pass + a critical learning-loop finding.

### DONE + committed
- **F1 — round.json state machine: COMPLETE** (5 slices, 33/33 tests, live in production). `scripts/ai/lib/
  round_state.py` + `round_contribution.py` (never-skip-local extractor) + `round_aggregate.py` (idempotent +
  durable AMEND). `aq-collab-round` migrated non-breaking; all 4 ratified rounds collect to CONSENSUS_LOCKED,
  0 mutation. Commits 2c83b5f1→9fb612ec.
- **F2 Phase A — scheduler modules: COMPLETE** (4 pure rebuild-free modules, 27/27 tests). `scheduler.py`
  (MLFQ+aging+bounded-starvation), `grammar_cache.py` (canonical GBNF key), `backpressure.py` (SLO +
  never-skip-local MECHANIZED via F1 quorum contract), `model_tier.py` (3-tier routing). Commits →e96a2993.
- **F2 session-mode design: RATIFIED 3/4** (commit 51f7b18f) — codex controller-owned design is the F2.5 spec
  (gen-slot state machine, aq-model-switch+unload, LOCAL_DELAYED cooling, 180s idle, memory-pressure eviction).
- **Model selection requirements: CODIFIED** (commit 51430f86) — `config/local-model-requirements.md` (4 tiers
  of eligibility gates: Tier0 protocol/runtime, Tier1 capability-by-tier, Tier2 lifecycle/trainability, Tier3
  governance) + `eligibility_gates` in `bench-promotion-criteria.json`.
- **Granite 4.1 8B — first bench candidate** (commit 51bf0cd3) — arch/license/quant GREEN, runtime gates PENDING.

### PENDING / not started
- **F2.5 Nix** (fastLaneServer :8082 default-off + dormant unit + aq-model-switch --unload + gen-slot-controller)
  — authored NOT YET; rebuild-gated; the design is ratified and ready to author.
- **F2.6** 35B-on-CPU A/B benchmark (measure-before-adopt).
- **F3** CapabilityLease + OTel + signed A2A envelope — designed (4/4 ratified earlier), not implemented.
- **Granite gate completion** — needs model download + bench run (user-gated).

## CRITICAL FINDING (drives the re-entry) — local is NOT improving from failures
Evidence:
- The learning loop is DORMANT: `training-loop-results.jsonl` last modified 2026-05-27 (5+ weeks); only 3 runs;
  every run reported `ingest: {samples_added: 0, dataset_total: 0}` — ZERO training samples ever captured.
- This session local emitted the IDENTICAL text-instead-of-tool-call failure 3× (f1-plan-consensus,
  f2-plan-consensus, f2-session-mode) + truncation (f3) + scaffolding-only (factory-critique). A working loop
  would have learned after the first.
- We have been building RESILIENCE (salvage via extract_contribution, never-skip-local) rather than FIXING the
  producer or CLOSING the improve-from-failure loop. Partially violates "fix the producer" (anti-gaming).
- The one genuine producer-fix in flight (F2.2 GBNF constrained decoding) is only the CACHE — not yet wired into
  live dispatch requests.

Implication: the harness's stated meta-goal — "the whole point is to improve the locally hosted models'
capabilities" — is not currently being served by the build order. This is the intent driving the re-entry.

## Ambient
- Other agent has uncommitted `ai-stack/switchboard/switchboard.py` + a switchboard test — NOT ours, left untouched.
- No Nix changed in any of this session's commits (all runtime-read Python/YAML/JSON/docs) → no rebuild pending from our work.
- antigravity contributions to f1-plan-consensus / f2-plan-consensus / f2-session-mode still pending in the IDE inbox (late-admissible; rounds ratified at 3/4).
