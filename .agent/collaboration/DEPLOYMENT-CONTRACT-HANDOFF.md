# Portable deployment contract — implementation evidence

Objective: deployed factory instructions and structure policy must be usable in
legitimate brownfield layouts, not assume nonexistent src/tests directories.

Renderer snapshots existing roots plus installer-created roots into target policy.
Preview confirmation binds rendered non-bundle bytes, bundle source digest, planned
directories, hooks, preservation records and target state. Collaboration seed files
initialize only when absent; unsafe/symlink destinations refuse. Agent-facing notices
are non-executable; required checks remain fail-closed until configured.

Implementer: factory_deployment_contract (aq_implementer). Final independent review
and canonical staged validation remain pending; no accepted or deployed claim.
Focused install/retrofit tests pass, including valid RESUME and other state seeds,
preexisting PULSE preservation, nested symlink refusal, layout confirmation drift,
new undeclared root rejection and no raw agent command placeholders.
Source bundle copying, hook composition, backups and collision behavior are retained.

Authority: owner's 2026-09-18 factory replication sequence. Functional naming applies
to code/tests/APIs/branches. Consumer evidence access is read-only; no consumer edits,
service activation, model/budget/auth changes or remote account configuration.
Next gates: independent exact-subject review, canonical Tier0, atomic integration,
then FT-5 execution-evidence readiness. Do not claim consumer readiness from metadata.
