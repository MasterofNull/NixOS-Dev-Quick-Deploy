# Finish and preserve current worktree changes

Owner directive: finish current reviews and commit existing changes before new
development. Codex is the sole integrator; Claude supplies independent review.

## Delivery sequence

1. CS-1 corrective: eight frozen paths, actual wrapper failure fixture, exact
   independent review and stable Tier-0. Preserve foreign edits.
2. Local elapsed-time monitor: existing four-path PRD/registry/dashboard/fixture
   candidate; independent acceptance and separate commit. Pipeline elapsed is
   observed; queue/prefill/generation remain unavailable in historical receipts.
3. Generated artifact hygiene: ignore local synced bundles, VM disk images,
   coordination mutex and interrupted snapshot writes. Retain all files on disk.
4. Current planning, collaboration receipts and archive evidence: inspect,
   correct active design contradictions, preserve historical verdicts, validate
   references and secret hygiene, then bounded documentation commits.
5. Existing restore-tool residue: inspect independently and retain on a holding
   branch if acceptance/activation gates are unmet. The prior roadmap pause is
   preserved; tests are disposable and production stores are never restored.

## Acceptance

Use explicit paths for every staging transaction and verify baseline, full-index
binary diff hash and staged/working consistency. Review each source commit by a
non-author. Run required Tier-0 and commit hooks; record actual tests and reviewers.
Never bulk-stage, alter another lane's source, reset history or remove evidence.
Archival moves must preserve bytes and inbound references. Runtime caches stay
local; ignoring them is not deletion. Report retained/unaccepted files truthfully.

## Separate authorized follow-up

The owner authorized producer metadata for queue wait, request start, first
visible content and completion in the frozen local dispatcher. This is a
distinct timing slice after existing work is committed; the elapsed-time monitor
does not implement these producer timestamps. No prompt or reasoning capture,
model replacement, retry/budget changes or sleep changes are part of that grant.

## Completion evidence

Record commit IDs, exact reviewed hashes, validation results and remaining held
paths in the handoff. Accepted source, live activation and local coding quality
are separate outcomes. No push/deployment authority is inferred from committing.

## Archive integration follow-up

The no-replay hook landed independently in f2b1d207. Staging byte-identical archive
copies then demonstrated a distinct existing bypass: Git reports renames rather
than D, so deletion-only discovery omits the original path. Use --no-renames at
both hook discovery and exact scanner verification. Disposable referenced and
unreferenced rename cases must scan the old path and emit no creation events.
Worker owns only the same scanner/hook/fixture; one fresh exact combined review
and gate cover this small follow-up plus evidence preservation. No old acceptance
is replayed or source-cleanup authority broadened.
Test-enforcement scope extension: add the fixture to the existing path-gated
validation-check registry and existing Ubuntu repo-structure CI job. Worker owns
those two files as well; no new actions, permissions, services or dependencies.
