# Brownfield Product Development Record (PDR)

Generated: 2026-04-19T03:07:03Z
Mode: /brownfield

## Objective
- Integrate strands-agents patterns (SDK, SOP workflows, tool architecture) into local AI harness to enhance agent orchestration, workflow management, and tool composition capabilities

## Discovery Snapshot
- tracked_files: 3129
- changed_files: 10
- key_directories:
- .
- ./.githooks
- ./archive
- ./archive/patches
- ./archive/podman-legacy-2026-01
- ./archive/temp-artifacts
- ./archive/ai-stack-ralph-tasks
- ./archive/backups
- ./archive/ai-stack-tests
- ./archive/test-fixtures
- ./archive/agent-onboarding-package-v2.0.0
- ./archive/scripts
- ./archive/deprecated-scripts
- ./archive/templates
- ./archive/temp-docs-20251120
- ./archive/deprecated
- ./nix
- ./nix/patches
- ./nix/pins
- ./nix/hosts

## Constraints
- Preserve existing MCP bridge, hybrid coordinator, and skill system; maintain backward compatibility; follow declarative-first policy

## Out Of Scope
- 

## Acceptance Checks
- Successfully integrated strands patterns with validation tests; enhanced tool registration system; implemented SOP workflow structure; created example workflows

## Execution Slices

### Slice 1: Create `.agents/` Directory Structure (Strands SOP Pattern)
**Objective**: Implement strands-style `.agents/` directory structure for workflow artifacts
**Tasks**:
- Create `.agents/` with subdirectories: `summary/`, `planning/`, `tasks/`, `scratchpad/`
- Update `.gitignore` to exclude `scratchpad/` while tracking other subdirectories
- Create directory structure documentation
- Add helper functions to `mcp_client.py` for artifact management
**Acceptance**: Directory structure created, documented, and integrated with MCP client

### Slice 2: Implement Decorator-Based Tool Registration System
**Objective**: Add strands-style `@tool` decorator pattern for simplified tool creation
**Tasks**:
- Create `ai-stack/local-orchestrator/tool_decorators.py` with `@tool` decorator
- Implement automatic schema generation from function signatures and docstrings
- Add tool registry with hot-reloading capability
- Create provider-based tool registration pattern
- Write example tools using new decorator system
**Acceptance**: Decorator system working with at least 3 example tools; hot-reloading functional

### Slice 3: Add SOP (Standard Operating Procedure) Workflow Support
**Objective**: Implement markdown-based SOP workflow pattern with RFC 2119 constraints
**Tasks**:
- Create `ai-stack/local-orchestrator/sop_engine.py` for SOP parsing and execution
- Implement RFC 2119 keyword detection (MUST, SHOULD, MAY, etc.)
- Add SOP template directory: `ai-stack/sop-templates/`
- Create 3 example SOPs: `codebase-analysis.sop.md`, `deployment-check.sop.md`, `test-validation.sop.md`
- Integrate SOP execution with hybrid-coordinator via new `/workflow/sop/execute` endpoint
**Acceptance**: SOP engine parsing SOPs correctly; example SOPs executable via MCP tools

### Slice 4: Enhance Agent Interface with Strands Patterns
**Objective**: Create unified agent interface similar to strands `Agent` class
**Tasks**:
- Create `ai-stack/local-orchestrator/agent_interface.py` with `LocalAgent` class
- Implement unified interface: `agent(query)` callable pattern
- Add model provider abstraction layer supporting multiple LLM backends
- Integrate with existing `MCPClient` and hybrid coordinator
- Add streaming support for real-time interactions
**Acceptance**: `LocalAgent` class functional; supports local llama-cpp and remote models; streaming works

### Slice 5: Implement Graph-Based Workflow Orchestration
**Objective**: Add DAG-based workflow orchestration for complex multi-step tasks
**Tasks**:
- Create `ai-stack/local-orchestrator/workflow_graph.py` with DAG implementation
- Add node types: task, decision, parallel, sequential
- Implement dependency resolution and execution scheduling
- Add visualization endpoint for workflow graphs
- Create example multi-step workflow using graph pattern
**Acceptance**: DAG workflows executable; parallel execution working; visualization functional

### Slice 6: Integration Testing and Documentation
**Objective**: Validate all integrations and create comprehensive documentation
**Tasks**:
- Write integration tests for each new component
- Create end-to-end test: SOP → Agent → Graph workflow
- Update CLAUDE.md and docs/ with new capabilities
- Create migration guide for existing workflows
- Add examples to docs/agent-guides/
**Acceptance**: All tests passing; documentation complete; examples functional

## Slice Discipline
- After each slice: commit + push + review comment.
- Keep each slice small to isolate mistakes and improve rollback speed.
- If inputs become ambiguous, pause and ask clarifying questions before proceeding.
